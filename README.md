# IAMAI

Read-only, browser-only Microsoft Entra Conditional Access rollout planner: pick a baseline, connect a tenant, get a phased roadmap with predicted impact — no server, no write access.

Status: design complete (`SPEC.md`), baseline adapter built and tested (`src/baseline/`). SPA scaffold, Graph client, engine, and roadmap are next.

```
npm test
npm run analyze -- /path/to/Jhope188-ConditionalAccessPolicies
```

# Unresolved work

Append: timestamp · segment · finding · exact blocker · preserved state · next evidence needed.

- 2026-09-13 21:35 MDT · S1 · C02 (critical, owner decision) · Lifecycle strategy for correcting an existing **On** policy is unresolved. Authored content moves an enforced policy to report-only before any correction: block-legacy-auth CONTENT.md:13 ("If the policy is currently On (Enforced), switch it to Report-only"), :21, :25, :33 ("Leave the policy in Report-only"), script :112-118 (`StageForCorrection` PATCHes state to report-only; other modes refuse while On); admin-session CONTENT.md:20, :24 ("If it is On, move it to Report-only first") against :32 ("Do not change the policy state (leave it On)") while its script :99-100 refuses correction while On — the channels disagree. Microsoft: report-only evaluates but does not enforce (concept-conditional-access-report-only), so staging removes the existing block/grant for everyone in scope during the correction; correcting in place applies the new scope immediately. Neither is established as intended: the baseline interpretation file carries no lifecycle intent, and the pinned policies' own states (Legacy Authentication, Device Code, Admin Persistence enabled; Authentication Transfer, AllUsers MFA report-only) describe the author's tenant, not a migration strategy. · Preserved: authored content and scripts unchanged (no blanket lifecycle string replacement). · Next evidence: owner decision per control kind (block / grant / session) on "correct in place while On" vs "stage in report-only first", then S3 aligns Entra, JSON and PowerShell for each affected package.
- 2026-09-13 21:35 MDT · S1 · C02 (high) · Admin strength difference is not explained where it matters. Pinned `IAC - GLOBAL - GRANT - MFA - AllAdmins` (enabled) requires the author's custom strength "Modern MFA + TAP" (windowsHelloForBusiness, fido2, x509CertificateMultiFactor, temporaryAccessPassOneTime, temporaryAccessPassMultiUse; modifiedDateTime 2026-05-04), so TAP is the baseline's deliberate choice and is not stripped. Microsoft's built-in phishing-resistant strength admits no TAP (concept-authentication-strengths). admins-phishing-resistant CONTENT.md:26 ("Remove a non-canonical built-in MFA or different strength"), :142 ("Do not substitute Microsoft's generic built-in strength") and :148 (describes the strength as "only accepts phishing-resistant methods and Temporary Access Pass") do not say that replacing a tenant's built-in phishing-resistant strength admits TAP where it was not admitted. · Engine (S1, fixed): an enforced admin policy that already meets the floor no longer receives grant/session/state sections from another candidate's reasons, so its correction is Users-only (policyIdentity.test.ts). Whether the rendered package still selects `entra.correct-grant` for that state is NOT VERIFIED (S3 runtime). · Next evidence: S3 rendered package check for a strong built-in-strength admin policy; S3/S4 wording in the existing prose that names the TAP difference; owner confirmation that TAP bootstrap for admins is intended.
- 2026-09-13 21:50 MDT · S1 · C01 (remaining gaps after fix; not blockers for the narrower invariant) · (1) The exact live payload was never available; the fix establishes the invariant on a reconstructed synthetic shape (role-scoped phishing-resistant policy, All-users-minus-guests MFA policy, role-scoped session-only policy; listed, reversed, swapped names; with/without exclusions carve-out; report-only admin; admin-only tenant) — `src/roadmap/policyIdentity.test.ts`. (2) Two own-scope candidates in the same tier (e.g. two All-users MFA policies both lacking the exclusions group) still resolve in scan order; no ambiguity-review mechanism was added (that would be new gate architecture). (3) A drifted own policy that no longer matches the goal's signature (drift audit A1: MFA-for-all changed to compliant device), with no plan tag and no exact name match, is not recognised on a fresh derivation; the all-users step now proposes a create for its own policy instead of an update against the admins policy (tracking.drift.test.ts A1 expectation changed, see commit 76320e8). (4) Probed (`node docs/preview-corrections/probes/s1-gap4-reportonly-weak.ts`, `logs/s1/gap4.txt`): a tenant whose only admin policy is report-only with plain MFA (below the phishing-resistant floor) gets coverage status `absent` with no reasons (coverage.ts adds report-only people only when the floor is met), so the step is a `create` of a new report-only admin policy beside it while its tracking follows that same tenant policy (lifecycle report-only); nextSafeAction `create-report-only`, not executable while blocked. The weak grant is not enforced by the plan, but a duplicate is proposed beside the tenant's own below-floor policy. The target selection S1 changed is not involved (the policy is own-scope and was already the fingerprint pick), so this predates S1; not fixed. · Preserved: all three commits local; no tabs, validators or baseline touched. · Next evidence: R1 adversarial review of the identity rule (roles+groups mixed assignments, member-group all-users policies, workload goals); owner/R1 decision whether a below-floor report-only own policy is corrected (grant) rather than duplicated.

# Unresolved work

Append: timestamp · segment · finding · exact blocker · preserved state · next evidence needed.

- 2026-09-13 21:35 MDT · S1 · C02 (critical, owner decision) · Lifecycle strategy for correcting an existing **On** policy is unresolved. Authored content moves an enforced policy to report-only before any correction: block-legacy-auth CONTENT.md:13 ("If the policy is currently On (Enforced), switch it to Report-only"), :21, :25, :33 ("Leave the policy in Report-only"), script :112-118 (`StageForCorrection` PATCHes state to report-only; other modes refuse while On); admin-session CONTENT.md:20, :24 ("If it is On, move it to Report-only first") against :32 ("Do not change the policy state (leave it On)") while its script :99-100 refuses correction while On — the channels disagree. Microsoft: report-only evaluates but does not enforce (concept-conditional-access-report-only), so staging removes the existing block/grant for everyone in scope during the correction; correcting in place applies the new scope immediately. Neither is established as intended: the baseline interpretation file carries no lifecycle intent, and the pinned policies' own states (Legacy Authentication, Device Code, Admin Persistence enabled; Authentication Transfer, AllUsers MFA report-only) describe the author's tenant, not a migration strategy. · Preserved: authored content and scripts unchanged (no blanket lifecycle string replacement). · Next evidence: owner decision per control kind (block / grant / session) on "correct in place while On" vs "stage in report-only first", then S3 aligns Entra, JSON and PowerShell for each affected package.
- 2026-09-13 21:35 MDT · S1 · C02 (high) · Admin strength difference is not explained where it matters. Pinned `IAC - GLOBAL - GRANT - MFA - AllAdmins` (enabled) requires the author's custom strength "Modern MFA + TAP" (windowsHelloForBusiness, fido2, x509CertificateMultiFactor, temporaryAccessPassOneTime, temporaryAccessPassMultiUse; modifiedDateTime 2026-05-04), so TAP is the baseline's deliberate choice and is not stripped. Microsoft's built-in phishing-resistant strength admits no TAP (concept-authentication-strengths). admins-phishing-resistant CONTENT.md:26 ("Remove a non-canonical built-in MFA or different strength"), :142 ("Do not substitute Microsoft's generic built-in strength") and :148 (describes the strength as "only accepts phishing-resistant methods and Temporary Access Pass") do not say that replacing a tenant's built-in phishing-resistant strength admits TAP where it was not admitted. · Engine (S1, fixed): an enforced admin policy that already meets the floor no longer receives grant/session/state sections from another candidate's reasons, so its correction is Users-only (policyIdentity.test.ts). Whether the rendered package still selects `entra.correct-grant` for that state is NOT VERIFIED (S3 runtime). · Next evidence: S3 rendered package check for a strong built-in-strength admin policy; S3/S4 wording in the existing prose that names the TAP difference; owner confirmation that TAP bootstrap for admins is intended.
- 2026-09-13 21:50 MDT · S1 · C01 (remaining gaps after fix; not blockers for the narrower invariant) · (1) The exact live payload was never available; the fix establishes the invariant on a reconstructed synthetic shape (role-scoped phishing-resistant policy, All-users-minus-guests MFA policy, role-scoped session-only policy; listed, reversed, swapped names; with/without exclusions carve-out; report-only admin; admin-only tenant) — `src/roadmap/policyIdentity.test.ts`. (2) Two own-scope candidates in the same tier (e.g. two All-users MFA policies both lacking the exclusions group) still resolve in scan order; no ambiguity-review mechanism was added (that would be new gate architecture). (3) A drifted own policy that no longer matches the goal's signature (drift audit A1: MFA-for-all changed to compliant device), with no plan tag and no exact name match, is not recognised on a fresh derivation; the all-users step now proposes a create for its own policy instead of an update against the admins policy (tracking.drift.test.ts A1 expectation changed, see commit 76320e8). (4) Probed (`node docs/preview-corrections/probes/s1-gap4-reportonly-weak.ts`, `logs/s1/gap4.txt`): a tenant whose only admin policy is report-only with plain MFA (below the phishing-resistant floor) gets coverage status `absent` with no reasons (coverage.ts adds report-only people only when the floor is met), so the step is a `create` of a new report-only admin policy beside it while its tracking follows that same tenant policy (lifecycle report-only); nextSafeAction `create-report-only`, not executable while blocked. The weak grant is not enforced by the plan, but a duplicate is proposed beside the tenant's own below-floor policy. The target selection S1 changed is not involved (the policy is own-scope and was already the fingerprint pick), so this predates S1; not fixed. · Preserved: all three commits local; no tabs, validators or baseline touched. · Next evidence: R1 adversarial review of the identity rule (roles+groups mixed assignments, member-group all-users policies, workload goals); owner/R1 decision whether a below-floor report-only own policy is corrected (grant) rather than duplicated.
- 2026-09-13 22:00 MDT · S2 · C03/C04 (not blockers for the fixed invariants; review items) · (1) C04 narrower rule: an undated credential still accepts proof from this scan's own sign-in window, so a replacement registered inside the window without a date is not detected; retained proof no longer stands without known chronology. Side effect for review: registration-report-only inventories and certificates (never dated by the method rows) keep only this scan's proof, so a person whose sole qualifying proof is retained moves Ready → Needs proof. No expiry added; `mfaHistory` keeps the proof. (2) `readGroup` (onDemand.ts) count/sample shape checks have no unit test: the module imports browser MSAL; typecheck only. (3) Scan-level propagation verified at collector/section boundaries (`collectConfigSection`, `runLaneB`, `collectMethodsForUsers`), not through worker.ts end to end. (4) A malformed later users page leaves earlier pages' methods in `authMethods` while `users` is `error` (pre-existing for any mid-paging failure). · Preserved: commit e6a73a3; no display, tab or history removed. · Next evidence: R1 decides whether (1)'s certificate/registration-report consequence is acceptable or whether same-scan continuity should be recorded in history; a live-shaped method payload would settle which date spelling Graph returns.
- 2026-09-13 22:25 MDT · S3 · C06/C02 (critical) · Policy scripts still not callable with the page's values (143 of 490 renders, `logs/s3/matrix-summary.txt`). Ten single-policy packages — admin-session, azure-management-mfa, block-auth-transfer, block-device-code, block-legacy-auth, block-unsupported-platforms, geo-restriction, mobile-app-protection, register-info-protected, require-managed-device — ship `kind: template` scripts with mandatory `-TargetPolicyJson`, and every partial correction module runs `StageForCorrection` (PATCHes an enabled policy to report-only) before `Correct*`, which refuses while On. Declaring the invocation would make the unresolved staging step one copy away; withholding that mode would remove the partial script output. Three of them (azure-management-mfa, block-unsupported-platforms, mobile-app-protection) also throw in Enforce without `-ReadinessApproved`, a switch no prerequisite attests. Also templates: guests-mfa pair (`-TargetPoliciesJson`), shared-devices, service-accounts-trusted-network, user-risk-medium. · Preserved: authored scripts unchanged; the mechanism is in place (binding `policy.target.json`, invocation.ts `$true` fix, all-users/admins converted, 6e3682f). · Next evidence: the owner's lifecycle decision (BLOCKED 21:35); then per package an invocation with its staging or in-place Correct path, and a declared prerequisite or withheld reason for `-ReadinessApproved`.
- 2026-09-13 22:25 MDT · S3 · C05 (critical) · Passkey setup (s-prereq-passkey-settings) shows no implementation in any fixture: `missing → packageFault` ×20, all tabs unavailable. Causes: (1) required bindings `authenticator.target.configuration` and `tap.target.configuration` have no source — the package pins only the FIDO2 target (`baselineAuthority.passkeyTarget`, owner decision 9) and its Entra prose leaves the TAP lifetime to the organisation, so no complete Authenticator/TAP body can be bound without inventing values; (2) `json.fido2`, `json.authenticator`, `json.tap` declare no method/endpoint (Graph v1.0 fido2authenticationmethodconfiguration-update, read 2026-09-13: `PATCH /policies/authenticationMethodsPolicy/authenticationMethodConfigurations/fido2`, `@odata.type` required), and three requests cannot be one JSON channel (project.ts); (3) JSON is the only channel carrying an IAMAI value, and it is not drawn on a non-policy step (stepBody `machine`), yet its hold holds Entra/PowerShell/AI ("not offered alone"). Safety: the FIDO2 target enforces attestation and allows only the two Microsoft Authenticator AAGUIDs; the package's own (compile-withheld) readiness tile and troubleshooting say removing an allowed authenticator stops existing keys signing in, and IAMAI binds no registered-AAGUID evidence (`passkey.current.registeredAaguids` unbound). Offering the restriction for copy without that evidence could lock out existing security-key users, admins on phishing-resistant policies included. · Preserved: package and runtime unchanged for this step. · Next evidence: owner targets for Authenticator and TAP (or that only FIDO2 is IAMAI's to configure); whether registered AAGUIDs must be read before the key restriction is offered; then request metadata per block and a projection whose JSON is one request.
- 2026-09-13 22:25 MDT · S3 · C01/C06 (high, review) · An all-users correction of a policy that excludes guests: the engine update and the package PATCH both send a `conditions.users` without `excludeGuestsOrExternalUsers`, but `changedFieldsOf` (roadmap/changedFields.ts) walks only submitted leaves, so `policy.current.changedFields` is `[conditions.users.excludeGroups]` and Entra says only "adds the exclusions group". If Graph replaces the users object on PATCH, guests come into scope (baseline-shaped and stronger, but unstated). Graph's nested PATCH semantics for conditionalAccessPolicy not established from a primary source; no live call. · Probe: `ENGINE=1 NOEX=1 ADMIN=enabled node docs/preview-corrections/probes/s3-c01-packaged.ts` (`logs/s3/c01-packaged-engine.txt`). · Next evidence: R1 decides whether a removed leaf is a changed field; primary documentation for PATCH replacement of complex properties.
- 2026-09-13 22:25 MDT · S3 · C06 (verification gap) · Rendered PowerShell not syntax-parsed: the local Windows PowerShell `Parser::ParseFile` run (no execution) was denied in this session and not retried. Checked instead: call lines, argument values, quote doubling (`O''Brien`), the script's param block against the declaration (invocation.ts validator; `channelParity.test.ts`; `logs/s3/ps-render.txt`; files in `logs/s3/ps/`). · Next evidence: R2 parses `logs/s3/ps/*.ps1`, syntax only, if permitted.
- 2026-09-13 22:25 MDT · S3 · C07 (wording for S4; not changed) · Package prose that disagrees with its rendered state: admin-session `ai.correct` says "The correction on this step adds the exclusions group" on a session-controls correction; all-users/admins `entra.correct-open` "This policy already exists and is enforced" also renders for a report-only policy's correction; admin-session `entra.correct-session` "If it is On, move it to Report-only first" beside verify "leave it On" (lifecycle, 21:35); `s-ladder-operator-passkey`, `s-prereq-per-user-mfa`, `s-prereq-security-defaults` render every tab unavailable (package authors no projection for the runtime state and the step has no engine channels).
- 2026-09-13 22:27 MDT · S3 (found) · C03 follow-up (required check failing, from S2) · `npm test` at 889ba71: `src/roadmap/foundationB.test.ts:73` "nothing but lifecycle.ts assigns a status" fails with `src/graph/collect/http.ts: now 3 assignments, was 2 (the HTTP error classes carry the response status)`. Introduced by S2's e6a73a3: the new `GraphResponseShapeError` constructor sets `this.status = status`. S2 ran focused suites only, so it was not seen there. Not an S3 change and not fixed in this S3-only session. · Preserved: http.ts and the guard unchanged. · Next evidence: S5/R1 choose between recording the third assignment in the guard's allowance with its reason (an HTTP error class carrying the response status, the reason the guard already gives for http.ts) or reusing an existing error class, then re-run the full suite.
- 2026-09-13 22:30 MDT · S3 · C05 (high) · Session corrections (s-goal-session-lifetime, drawn on s-goal-all-users-no-persistence) are a non-copyable preview in all 20 renders. Causes (`node docs/preview-corrections/probes/s3-session.ts demo-week2|curated`, `logs/s3/session.txt`): (1) the package names two members, `browser` (stable id ea9459a9…, the pin's "All Users Persistence (9-12 Hours)") and `unmanaged` with `memberStableId: null`; the current pin carries no unmanaged-device session policy, so `policies.session.unmanaged.target.displayName` (required by `missing`) can never bind — package and pin disagree on the member set; (2) `policy.target.excludeUsers` resolves to the baseline's `[]` ("nobody"; stepPackage.ts binds it deliberately), but `requires` and `bindText` use conditions.ts `present`, which reads an empty list as absent, so the preview asks for "excluded people". Loosening `present` for lists globally would also let an empty `excludeGroups` bind (a policy without the exclusions group), so it was not changed. (3) The preview's "Values still to resolve" lists both members' current policy IDs for a create (required bindings used in drawn blocks), which a create never has. · Preserved: package, pin, binding semantics unchanged. · Next evidence: owner/R1 decide whether the unmanaged member is out of scope for this pin (projection without it) or needs a source; a per-binding rule for which lists may be legitimately empty.
- 2026-09-13 22:30 MDT · S3 · C05 (medium) · register-info-protected: Entra create withheld in 17 executable renders because it requires `policy.target.mode` (IAMAI's own semantic mode: block outside trusted locations, or the MFA fallback without a trusted network — META `baselineAuthority.canonicalSemantics`; no pinned source member). stepPackage.ts leaves it unbound as a value with no meaning IAMAI can supply; binding it would invent a mapping. JSON/PowerShell/AI/Email still draw. The block also exposes `blockOutsideTrusted` (S4 wording). · Next evidence: owner/R1 decide whether the mode is derived from the resolved target's locations/grant or the Entra create stops naming it.
- 2026-09-13 22:45 MDT · S4 · C07 (high, remaining; not fixed) · (1) Technician-facing blocks still cite STEP.md, which the technician never sees: 24 lines in 7 packages (admin-session, azure-management-mfa, block-unsupported-platforms, geo-restriction, mobile-app-protection, require-managed-device, device-registration-mfa; list `logs/s4/step-md-refs.txt`). 18 of them are the `entra.correct-grant/-session` "If it is On, move it to Report-only first" lines, the `entra.observe` "Leave the policy in Report-only" lines and the report-only evidence prompts — rewriting them sets the lifecycle meaning that BLOCKED 21:35 leaves to the owner. The `entra.create` "exactly as described in STEP.md" lines (5) need the grant/session named from the resolved target; the remaining one is in device-registration-mfa's CONTENT.md:931 verification text. Not attempted in this time box. (2) Not reproduced or located this session: "IN PROGRESS for unstarted work" (no such string in content.json or src; rendered labels not walked); "nothing enforced despite On policies" (content.json `critical.verificationOnly` "… nothing is enforced yet" renders when the schedule holds no enforcement step, schedule.ts:969-971 — no tenant with On policies and no enforcement step was rendered); "Ready while prerequisites remain"; nonexistent crosslinks (not checked). (3) Noticed, not changed: admins `ai.correct` "… every time they sign in" describes an authentication-strength grant as a per-sign-in prompt. · Preserved: commits c85f773, f3ea04d change only the sentences named in RESULTS C07. · Next evidence: owner lifecycle decision, then per-block wording for the STEP.md lines; an R1 render of the listed labels against a synthetic tenant.
- 2026-09-13 22:45 MDT · S4 · C08 (verification limit) · Not reproduced: every synthetic route measured well under a second (RESULTS C08). The runs cannot exercise a real MSAL session: with Microsoft/Graph blocked in the browser a token or group read fails fast, while a real tenant's Plan waits on `readGroup` for every policy-referenced group in sequence (planData.ts:222-242: `await readGroup(...)` per id, each a Graph object read, a `$count` and a member page walk, unless the group-members cache is fresh) before `computed` can exist. That is a plausible dependency for the audited 20 s / no-rows reports but is an unverified hypothesis, not a reproduced cause; nothing was changed (no timeout, token or cache change). · Preserved: no code change for C08. · Next evidence: a run with a real session and timings of `readGroup` per group (owner or R2 with authorisation), or a mocked Graph that answers slowly, to decide whether the sequential reads are the cause.
- 2026-09-13 22:48 MDT · S4 · C07 (correction to the 22:45 entry, item 2) · (a) "IN PROGRESS for unstarted work" IS located, the 22:45 entry was wrong to say no such string exists (that search was case-sensitive): planBoard.ts:280 `PREREQUISITE_STATE` maps a prerequisite step in the Ready lane to "Prerequisite · In progress" (decision 12 / content review R3, which replaced "Ready"), so an actionable prerequisite nobody has started reads In progress — the audited symptom. Not changed: the word is pinned by contentReview.test.ts:75-87, riskDeviceCampaignContentSpecs.test.ts:139/:202 and about 100 `docs/qa/step-snapshots/**` labels, and replacing R3's chosen word (e.g. with one that claims nothing about starting, such as "To do") needs the owner's sign-off on that review decision plus an inspected snapshot regeneration that did not fit this time box. (b) "nothing enforced despite On policies": FIXED for the plan-length reason (`critical.verificationOnly` → "no enforcement is left to schedule"; its condition is the plan's, schedule.ts:969-971); `reasons.ts:25` "Covered only by a report-only policy, so nothing is enforced yet" is true for its condition and unchanged; not rendered against a tenant with On policies. (c) Crosslink "Consolidate Overlapping Policies" (audit 4.5): exists in source as a Cleanup row title (content.json:1856), referenced from content.json:69 and :5258 — not reproduced as a nonexistent target in source; whether that row renders in the audited fixture was not checked. · Preserved: planBoard.ts, tests and snapshots unchanged. · Next evidence: owner wording for a Ready-lane prerequisite; R1 render of the Cleanup row in the fixture the "Why IAMAI says this" dialog links from.

# BLOCKED — Plan Actionability + Trust Correction

Format: `segment · task · root cause · what was tried · smallest next step`

## Blocked / deferred

- S0 · task 1 (code map, A3) · the audit findings file containing A1–A6 is not in the repository · searched docs/, docs/audit, docs/audits, root *.md and untracked files for A1…A6 markers; nearest in-repo material is `docs/audit/042-product-truth-decision-integrity.md`, `docs/audits/guidance-audit-01.md`, `docs/audits/steps/*.md`; the per-item findings exist only in the session memory file `iamai-audit-2026-09-11-plan-library.md` outside the repo · owner drops the audit findings file into `docs/product/actionability/` and updates the A3 line in RUN-CONTEXT.md; until then S1 and the §10 pre-enforcement classification treat A3 items as deferred.
- S0 · task 4 (V11 scope) · `interpretation.json` holds ten further `decisionRequired` references (`e663a7ce`, `9ee031a3`, `902993ed`, `5628ad67`, `2d25c298`, `cc7f9bb7`, `8d0564e5`, `1178bb5d`, `5f96c57d`, `0de51b52`) that V11 does not name · not mapped, per "apply only the listed outcomes" · owner decides whether each becomes a `sourceMapping` row (same rule as §10.7) or stays Cleanup material; the runtime today already holds a policy on any unexplained excluded group.
- S1 · task 4 (tests named after audit items A1–A6) · A3 is still not in the repository, and the session memory file it points to (`iamai-audit-2026-09-11-plan-library.md`) holds page defects, not ownership/drift items numbered A1–A6 · re-checked `docs/product/actionability/` and the memory file at segment start; wrote `src/roadmap/tracking.drift.test.ts` with the invariants and one test per drift kind (`drift:grant`, `drift:client-app`, `drift:include`, `drift:exclusion`, `drift:location`, `drift:platform`, `drift:report-only-before-enforcement`) · owner drops A3 in; the six audit-item tests are added to that file, one per id, against the ownership reading `matchMembers` stage 0 / `correctionOf` / `driftOutcomeOf` in `src/roadmap/tracking.ts`.

## Choices

- S1 · task 1 (where ownership lives) · the member's own observation record (`observations[step.id].members[key].artifact`, roadmap/observation.ts) already names the object each member watched, and nothing read it when matching · made it stage 0 of `matchMembers` in `src/roadmap/tracking.ts`: the member keeps that object while it is on the tenant, whichever candidate the regeneration now prefers; reported as `owned` only where no tenant-side proof (operation target, tag) still ties it · none required; S2/S3 read `MemberTracking.correction` and `driftOutcomeOf` rather than re-deriving.
- S1 · task 2 (shared satisfier) · "no other goal claims as satisfier" read literally as another goal's `satisfaction.policyIds` containing the owned policy (coverage/types.ts) · on every fixture no adjust step trips it today, so the rule is enforced as written and raises `review-required` with the `correctionShared` note · if a live tenant's broad policy (all-users MFA also counted for guests) puts real steps into review, the owner narrows the rule to `sufficientId` only; one-line change in `correctionOf`.
- S1 · task 1 (condition rank) · `raiseCondition` never lowers a `blocked` or `needs-decision` step to `review-required` (lifecycle.ts CONDITION_RANK), so a drifted policy on a blocked step keeps `blocked` · `driftOutcomeOf` reads the members (`reviewRequired`, `correction.safe === false`) instead of the condition alone, so the drift still reads Review required · none required.
- S1 · existing test `pair 12` (foundationB.test.ts) · its second half asserted that a renamed pair with the last scan's record carried in claims nothing, which the ownership invariant contradicts (the record names the very objects; a rename is not a change) · asserted the new reading there (each member keeps its recorded object, `owned`, history continues) and kept the no-record half as it was · none required.

- S0 · V8 · the shared-devices package patches every plan policy that prompts a person (runtime-derived from `promptsPeople` in `src/roadmap/strand.ts`, listed in `src/ui/surfaces/stepVars.ts`), not a fixed pair · chose the smallest reversible reading: the two `@created` rows (require-managed-device, session-lifetime) are confirmed as the baseline-known pair and no rows were added for the other prompting policies · S2 may derive the completion dependency from the runtime set instead of the two static rows if the lane engine needs it; that is an engine choice, not a table edit.
- S0 · V1 (`s-goal-azure-management-mfa`) · no pinned object implements the goal (`goalMap` has no entry; the nearest-named pinned policy `IAC - GLOBAL - GRANT - MFA - WindowsAzureAD-BaselineScopes` targets Windows Azure AD, not Azure management) · left the exclusions-group edge on `enforce` as the "does not reference it" outcome · none required.
- S0 · V2 (`s-goal-unmanaged-browser`) · no pinned object implements the goal, so the "pinned target does not exclude the emergency identities" branch was taken and a `baselineSafetyConflict:unmanaged-browser-emergency-exclusion` was recorded on `enforce` · raise with the baseline author; if the author supplies an object that excludes the exclusions group, the row becomes `create ← s-prereq-exclusion-group@complete`.
- S0 · pinned-refs.tsv · added a trailing `goal` column from `pinned.json` `goalMap` beyond the seven fields the segment names, so V1/V2/V4/V11 could be read from one file · none required.

- S2 · task 1 (which §10 tables) · the task names §10.1–10.5, but §10.7 (the V11 `sourceMapping` rows) is part of the canonical §10 table and example 14 needs it · the parser reads every `gated_action` table under any `### 10.N` heading, so §10.7 is in and a later §10.8 would be too · none required.
- S2 · task 1 (placeholder row) · `<every CA policy step in §11 E–H>:enforce` is one row for 26 steps · expanded from the §11 group headings `### E.`–`### H.` and their `#### \`id\`` entries, each edge carrying `expandedFrom`; the parser reads §11 headings only for this · none required.
- S2 · task 2 (deferred propagation scope) · §4 step 3 evaluates blockers on the next action, yet examples 5 and 10 hold an observing policy on an enforce-side Deferred prerequisite while example 13 keeps an enforce-side conflict off the lane until enforce is next · a Deferred prerequisite on any applicable edge of the step holds it (§8.3: never makes the dependent Ready); every other blocker is scoped to the next action · owner narrows to next-action-only if examples 5/10 were meant to read with the evidence predicate satisfied.
- S2 · task 2 (§8.2 completion) · "answering no completes the owning step" needs a mechanical reading · a step is Completed when it owns a condition the scan resolved not-applicable and nothing exists for it (`isComplete` in lanes.ts); the caller must not resolve `campaign-targets-passkey` not-applicable for an absent campaign (that condition gates no edge today) · none required.
- S2 · task 2 (reciprocal cutover pair) · `s-prereq-security-defaults:start ← policy@ready-to-enforce` and `policy:enforce ← security-defaults@complete` are mutual when `sd-enabled` · ready-to-enforce ignores the enforce gate owned by the requesting step; a lane cycle reads as healthy Up Next, never a hold · none required.
- S2 · task 2 (unstated inputs) · the engine has to read what the scan did not say · an unlisted non-step prerequisite is `blocked` (§8.1 never silently satisfy), an unlisted condition is `unresolved` (edge participates), a step's ladder comes from its gated actions then `effort_kind`, overridable per observation with `kind` · none required.
- S2 · task 3 (`iamai_order` placement) · read from the step index; when two rows both carry a value it orders them immediately before the step-id fallback, and it is empty everywhere so it has no effect today · none required.
- S2 · task 3 (unlock counts) · §12.1 excludes the Security Defaults cutover edges · `unlockCounts(graph, { excludeConditions })` leaves the exclusion to the caller; the §12.1 numbers reproduce with `['sd-enabled']` (asserted) · S3 passes the option when it wires the lanes.
- S2 · task 4 (examples 13 and 15 against the frozen §10) · example 13 names `s-goal-require-managed-device` but §10.3 (V2, S0) records the `baselineSafetyConflict` on `s-goal-unmanaged-browser:enforce`; example 15 lists `exclusion-group@complete` on `s-goal-sign-in-risk:enforce` but §10.5 gates `create` · the tests follow §10, the authority, with the same outcomes · owner realigns the §17 prose (a doc edit, not an engine change).

- S3 · task 1 (row label beside the status word) · the walk and smoke read the row's status word (`.plan-row .status`: In place, Enforced, Report-only, Ready to enforce…) as product truth in a dozen checks, and S7 owns state-word agreement · the lane label is a second line under the state word (`.plan-row .lane`, `Lane · substatus/reason`), the status chip stays · S7 decides whether the status word folds into the lane label; the walk then reads `.lane`.
- S3 · task 1 (steps the graph does not know) · the runtime draws rows the playbook index does not carry (`s-prereq-source-references`, `s-goal-all-users-no-persistence`, the alerting / naming / consolidation / hardening Cleanup rows; the index calls two of them `runtime-source-reference-decision` and `cleanup-harden-emergency-access`) and `deriveLane` throws on an unknown id · such a row takes its lane from the Plan's own state (`planLanes.ts fallbackOf`: complete → Completed, skipped → Deferred, held → On Hold, sequenced → Up Next, else Ready with the nearest substatus) and sorts after the engine's rows by id; no id is named in code · S4 removes the source-references step; the owner adds the remaining ids to §10.0 or confirms they stay runtime rows.
- S3 · task 1 (graph steps this plan lacks) · a baseline that does not hold a goal, or a fixture without it, leaves graph steps with no runtime step, and an unobserved step would queue every dependent behind work that is not on this plan · read as `{ complete: true }` (nothing to do here); a step the person said does not apply reads the same · none required.
- S3 · task 1 (the shared `sourceMapping:62d67e66` edge) · §10.7 puts the mapping on every CA policy create, but the scan knows which policies actually name the group (`Action.missing[].decision`), and one global prerequisite state would hold a policy that does not name it (`s-goal-admin-session` on week two) · the edge reads `resolved` and the pending mapping is an observed `sourceMapping` blocker on each step whose body waits on it (also the ten V11 references S0 deferred, by the same field) · none required; if the owner wants the graph edge to hold, `prerequisiteOf` reads the gated step's `sourceReferences`.
- S3 · task 1 (a missing object the graph has no edge for) · the runtime holds a `correct` whose body names an object the tenant lacks (`s-goal-admins-phishing-resistant` on messy), while §10 gates only `create` on the maker step, so the engine read it Ready · Correct · an `Action.missing` entry whose maker step the graph does not queue this step's next action behind is an observed `missingObject` blocker (On Hold), never a silent Ready; where the graph carries the maker it stays healthy Up Next · owner adds `correct ← <maker>@complete` edges to §10 if Up Next is the intended reading; the observation then drops out on its own.
- S3 · task 1 (conditions) · the runtime records no state for the §8 conditions (mail devices, partner accounts, travel, shared devices, sd-enabled, campaign method) · every condition stays unresolved, so its edges participate (§8.1 conservative) and no question step is Completed by §8.2 · S7 or the owner wires the recorded answers (`mapping` decisions) into `TenantState.conditions`.
- S3 · task 1 (Cleanup rows the graph knows) · `cleanup-drill` and `cleanup-notAssessed` are graph steps; the others are not · a graph Cleanup row is observed as `{ exists: false, complete }`; a runtime-only one is Ready · Create until done (what its row said before) · none required.
- S3 · task 2 (the floor group) · the Plan drew Microsoft's recommendations as their own group after the phases; a lane has no such group · a floor step is a row of its lane like any other; the printed document keeps the named group (planRows.ts) · a later pass may carry `floor` on the row as provenance text if the owner wants it visible on screen.
- S3 · task 4 (SCHED as a secondary projection) · read as: the lane adapter imports nothing from the schedule and a test proves lanes survive every phase/wave/date moving; the When column, the Waiting tile, the rail, the print and the calendar still read `stepSchedule.ts` unchanged · the row carries its phase as `data-wave` for the walk's day-0 reading; nothing else changed · none required.
- S3 · walk and smoke · the board draws one lane at a time, so a check that reads every row reads the three tabs in turn (`readRows` / `showLane` / `acrossLanes` in the scripts, each row kept with its tab and its index inside it) and `Show completed` presses `Show deferred` too · edited blind (the walk is CI-only); if CI reports a P0 the fix is in those helpers · none required.
- S4 · task 1 (the plan-record key) · the mappings were persisted under the removed step's id (`stepDecisions['s-prereq-source-references']`, one answer per source id), and a record saved before S4 must still answer · the key is kept, as `BASELINE_MAPPINGS_KEY` in `src/roadmap/sourceMappings.ts`, and it names no step (`PREREQ_STEP_ID.sourceReferences` is gone; `DECISION_STEPS.sourceReferences` points at the key) · none required; a later migration may rename the key if the owner wants the record to read `baselineMappings`.
- S4 · task 1 (the content step) · `s-prereq-source-references` in content.json was the row's words and the mappings' words in one entry · the entry is deleted; its `decision.references` words moved to `pages.plan.settings.mappings` (same option strings, so a saved answer still parses), `pages.plan.blocked.sourceReferences` became `pages.plan.blocked.sourceMapping` = "Baseline references an unmapped group", `jsonWaitsDecision` no longer takes `{steps}` (with no step to name, the slot would have printed the author's id), `whoUnknownDecision` no longer says "unidentified groups", and `shared.contract.fixMapping` is the opened step's Fix line · none required.
- S4 · task 2 (where the role lives) · the role was computed only on the removed row · `generate.ts` annotates every open policy's `action.sourceReferences` with role / baselinePolicies / baselineTotal from `referenceUsage`, `planLanes.ts observe` puts the role on the `sourceMapping` observed blocker, and the engine's `Blocker` carries an optional `role` (`SourceRole` in `lanes.ts`); `sourceMappingsOf(steps)` reads the record across the plan · none required.
- S4 · task 2 (the reason on the row) · the board's On Hold heading was "Baseline mapping" and the row's reason came from the step it waited on · `BOARD.blockers.sourceMapping` is `BLOCKED_REASON.sourceMapping`, so the group heading, the lane label and the row's own blocked reason all read "Baseline references an unmapped group"; a runtime-only row the graph does not know (`s-goal-all-users-no-persistence`) carries the same blocker as its reason from the fallback path · S7 decides whether the heading should be shorter than the reason.
- S4 · task 2 (the graph edge) · §10.7 still puts `sourceMapping:62d67e66` on every CA policy create · unchanged from S3: the edge reads `resolved` and the observed blocker holds only the policies whose bodies name a pending reference; the ten V11 references S0 deferred appear in the mappings surface by the same field (the scan already asks them) · owner decides whether to prune §10.7 or keep the edge as documentation.
- S4 · task 3 (naming a reference) · A1 §18.1 names a reference by its short id; the content had "Group {n}" · rows read `Group 62d67e66` / `Named location 1267ac22` (kind + short id, `pages.plan.settings.mappings.groupLabel`), with the full id under a disclosure; no running number anywhere · none required.
- S4 · task 3 (the picker) · the surface reuses the opened step's `Options` (radios + single picker) exported from `ContentStep.tsx`, with the tenant's groups / named locations from `pickerUniverse` · none required; `BaselineMappings.tsx` is the one component, `baselineMappings.ts` its rows.
- S4 · `dependency-data.json` · the index still carries `runtime-source-reference-decision` as a step (A1 §10.0) · left as is: no code reads the id and the engine's `observed` marks it complete like any graph step the plan lacks · owner removes it from §10.0 when the playbook is next regenerated.


## Chrome checks

- (none in S0)
- (none in S1)
- (none in S2)
- S3 · live look at the demo Plan (dev server, Chrome) · PASSED: Ready 12 / Up Next 3 / On Hold 15 tabs with counts, `Lane · substatus` under every state word, Work type select, Show completed / Show deferred toggles; On Hold grouped as Baseline conflict (1) and Baseline mapping (12); no console errors.
- S4 · live look at the demo Plan (dev server, Chrome) · PASSED: no "Decide What the Baseline's Unidentified Groups Stand For" row; On Hold groups Baseline conflict (1) / Baseline references an unmapped group (13) / Not supported (1), each held row's lane label reads the reason; Plan settings → Baseline mappings lists Group 62d67e66, Group e663a7ce, Group 2d25c298 … with "Unmapped · Excluded from its policies", the count line and the policies named; no "Group N" text on the page; leaving one out and Save moved the tabs Ready 11 / Up Next 3 / On Hold 15 → 14 / 7 / 8 and the row read "Left out"; Clear this answer put it back to Unmapped and 11 / 3 / 15; no console errors on load or after.

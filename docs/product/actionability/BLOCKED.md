# BLOCKED — Plan Actionability + Trust Correction

Format: `segment · task · root cause · what was tried · smallest next step`

## Blocked / deferred

- S0 · task 1 (code map, A3) · the audit findings file containing A1–A6 is not in the repository · searched docs/, docs/audit, docs/audits, root *.md and untracked files for A1…A6 markers; nearest in-repo material is `docs/audit/042-product-truth-decision-integrity.md`, `docs/audits/guidance-audit-01.md`, `docs/audits/steps/*.md`; the per-item findings exist only in the session memory file `iamai-audit-2026-09-11-plan-library.md` outside the repo · owner drops the audit findings file into `docs/product/actionability/` and updates the A3 line in RUN-CONTEXT.md; until then S1 and the §10 pre-enforcement classification treat A3 items as deferred.
- S0 · task 4 (V11 scope) · `interpretation.json` holds ten further `decisionRequired` references (`e663a7ce`, `9ee031a3`, `902993ed`, `5628ad67`, `2d25c298`, `cc7f9bb7`, `8d0564e5`, `1178bb5d`, `5f96c57d`, `0de51b52`) that V11 does not name · not mapped, per "apply only the listed outcomes" · owner decides whether each becomes a `sourceMapping` row (same rule as §10.7) or stays Cleanup material; the runtime today already holds a policy on any unexplained excluded group.
- S1 · task 4 (tests named after audit items A1–A6) · A3 is still not in the repository, and the session memory file it points to (`iamai-audit-2026-09-11-plan-library.md`) holds page defects, not ownership/drift items numbered A1–A6 · re-checked `docs/product/actionability/` and the memory file at segment start; wrote `src/roadmap/tracking.drift.test.ts` with the invariants and one test per drift kind (`drift:grant`, `drift:client-app`, `drift:include`, `drift:exclusion`, `drift:location`, `drift:platform`, `drift:report-only-before-enforcement`) · owner drops A3 in; the six audit-item tests are added to that file, one per id, against the ownership reading `matchMembers` stage 0 / `correctionOf` / `driftOutcomeOf` in `src/roadmap/tracking.ts`.
- S5 · task 3 (pre-enforcement checks) · A3 is still not in the repository, so the "authored Readiness items A3 names" cannot be classified · re-checked `docs/product/actionability/` and the session memory file (page defects only); the mechanism already exists and gates enforcement only — a package's human-validation prerequisites (`META.json prerequisites`, `project.ts pendingPrerequisites`) hold the enforce transition's artifacts while Report-only creation projects (correction batch 1.1, owner), and they are Readiness tiles with a Confirm control · owner drops A3 in; each named item becomes a package prerequisite on the enforce transition, nothing else becomes a gate.
- S7 · task 1 (the reviewed step's next action) · a step generation read as delivered (coverage `inPlace`) carries no operation; when tracking then withdraws `satisfied` because the enforced policy's meaning drifted (S7), the row, badge, export and calendar all read "Enforced · Review required" and the fix names the review, but `unavailableReason` reads the empty operation list as `no-operation`, so What to do and Done when say "no policy to write; scan again" and the review milestone does not lead · reproduced on demo-week2 `s-goal-admins-phishing-resistant` with the grant drifted to block and the prior record `enforced` (`stateAgreement.test.ts` S7.2 asserts the state words and that no delivery claim is made; it does not assert the action) · owner decides whether a step held for review answers before a `no-operation` reason in `stepContract.ts actionOf` / `doneWhenOf` (the comment there keeps every unavailable reason ahead of the review so confirmed-unsafe is never softened), or whether generation builds the correction operation for a satisfied policy step.
- S7 · task 3 (stale page-contract/walk allow-list entries, LIBRARY validation status, PowerShell parsing in CI, false implementation README claim, row accessible-name separators, empty lead text) · not taken: each needs the walk report, CI or a README audit the segment forbids reading, or a UI change with no direct unit acceptance · ICS UTF-8 folding was the one direct item and is done (`roadmap/ics.ts foldIcsLine`, `ics.test.ts`) · the rest wait for a segment that owns them.
- S8 · task 1 (JSON requests on the bespoke blocks) · 34 JSON blocks in 15 packages still carry no `method`/`endpoint` (the guests-mfa and unmanaged-browser pairs, the named-location create and patch, the authentication strength, break-glass role and group membership, the exclusions and service-accounts groups, passkey settings, security defaults, the partner patches, the shared-devices people patches, verify-mfa configure, cleanup-notAssessed source), so under S6's rule their JSON channel is withheld · S8 authored only the single-policy Conditional Access family (89 blocks in 15 packages: create → `POST …/identity/conditionalAccess/policies`, correction and enforce → `PATCH …/policies/{policy.current.id}`, the pair the pilot already declares); the rest address other Graph objects and need the author · owner authors each remaining block's request; the runtime rule needs nothing else.
- S8 · task 1 (S7's withdrawal of the delivery claim) · with S7's `needsReview` withdrawal, the demo's week-two rows Require Phishing-Resistant MFA for Admins, Block Device Code Sign-in, Block Legacy Authentication and Require MFA for Everyone read "Enforced · Review required" and offered to create the baseline's version over the tenant's own policies, and the walk's invariant (the admins row reads In place; the consolidation row exists only where a step found existing coverage) failed: the week-two policies moved onto the plan's own asked-for exclusions, but `observe()` calls a semantic move expected only against the member's operation of this scan, which is gone once the correction has landed · tried carrying the ask on the observation record (`StepObservation.intent`, read from the prior sighting): the week-one ask fingerprints `conditions.users` as 4e812460 and week two holds 028a32c2, and the admins ask was enable-only (`{}`), so it did not settle it; reverted, and the withdrawal reverted with heldForReview / planState / tracking / `tracking.reviewWithdrawn` restored to their S6 reading and test S7.2 removed · owner decides whether a review-required enforced member withdraws In place; if so, `expected` needs the goal's target (the translator's policy, not the member's operation) to recognise the plan's own change landing.
- S8 · task 3 (the original brief §28) · the brief is not in the repository (the only §28 in docs/ is the authoring guide's whole-library lint) · searched docs/, the segment logs and the memory directory · the live check ran against eighteen items composed from the Chrome checks S3–S6 recorded here (listed under Chrome checks) · owner drops the brief in; the eighteen are re-run against its list.
- S8 · task 1 (walk P1/P2) · 347 P1 and 10 P2 remain after the P0s: page-contract allow lists (the Why summaries, Microsoft Learn links, Close buttons, "1 satisfied"), sentences over 25 words, the throttled first load at 4.6 s · not touched: `docs/qa/page-contracts.json` is reviewer-owned and a P1 gates nothing · reviewer updates the allow lists; the first-load budget is a separate item.
- A2 · CI walk (deploy-pages) · the A2 push was the first CI run to carry A1c's blind walk edits; after the ReferenceError and the Connect-tile wording were fixed (3545b15) the walk still reports 4 P0 on the demo and demo-week2 · tried: none further (second failure on the one root cause, A1c's unrun walk edits) · smallest next steps, for A6: (1) `walk.mjs` step check `Exclude from rollout` → `Defer this step` (decision 3; both demo snapshots, step "Use Separate Accounts for Admin Work"); (2) the started check waits on `.plan-started` reading `Started <date>` — `Plan.tsx` has no `.plan-started` element since A1b's tiles (decision 11) — so read the Started tile (`.plan-progress-tile` whose dt is Started and dd is a day) instead; (3) demo-week2 "Intune Enrollment does not wait on the device decision while it is open" is an engine/lane reading (A1a/A1b), not a check wording: the row is neither On Hold nor `after: Decide How Devices Are Managed` while the decision is open — owner/A6 decides whether the lane engine or the walk is right. The `ci` job (unit suite + smoke) is green at 3545b15.

## Choices

- S1 · task 1 (where ownership lives) · the member's own observation record (`observations[step.id].members[key].artifact`, roadmap/observation.ts) already names the object each member watched, and nothing read it when matching · made it stage 0 of `matchMembers` in `src/roadmap/tracking.ts`: the member keeps that object while it is on the tenant, whichever candidate the regeneration now prefers; reported as `owned` only where no tenant-side proof (operation target, tag) still ties it · none required; S2/S3 read `MemberTracking.correction` and `driftOutcomeOf` rather than re-deriving.
- S1 · task 2 (shared satisfier) · "no other goal claims as satisfier" read literally as another goal's `satisfaction.policyIds` containing the owned policy (coverage/types.ts) · on every fixture no adjust step trips it today, so the rule is enforced as written and raises `review-required` with the `correctionShared` note · if a live tenant's broad policy (all-users MFA also counted for guests) puts real steps into review, the owner narrows the rule to `sufficientId` only; one-line change in `correctionOf`.
- S1 · task 1 (condition rank) · `raiseCondition` never lowers a `blocked` or `needs-decision` step to `review-required` (lifecycle.ts CONDITION_RANK), so a drifted policy on a blocked step keeps `blocked` · `driftOutcomeOf` reads the members (`reviewRequired`, `correction.safe === false`) instead of the condition alone, so the drift still reads Review required · none required.
- S1 · existing test `pair 12` (foundationB.test.ts) · its second half asserted that a renamed pair with the last scan's record carried in claims nothing, which the ownership invariant contradicts (the record names the very objects; a rename is not a change) · asserted the new reading there (each member keeps its recorded object, `owned`, history continues) and kept the no-record half as it was · none required.

- S0 · V8 · the shared-devices package patches every plan policy that prompts a person (runtime-derived from `promptsPeople` in `src/roadmap/strand.ts`, listed in `src/ui/surfaces/stepVars.ts`), not a fixed pair · chose the smallest reversible reading: the two `@created` rows (require-managed-device, session-lifetime) are confirmed as the baseline-known pair and no rows were added for the other prompting policies · S2 may derive the completion dependency from the runtime set instead of the two static rows if the lane engine needs it; that is an engine choice, not a table edit.
- S0 · V1 (`s-goal-azure-management-mfa`) · no pinned object implements the goal (`goalMap` has no entry; the nearest-named pinned policy `IAC - GLOBAL - GRANT - MFA - WindowsAzureAD-BaselineScopes` targets Windows Azure AD, not Azure management) · left the exclusions-group edge on `enforce` as the "does not reference it" outcome · none required.
- S0 · V2 (`s-goal-unmanaged-browser`) · no pinned object implements the goal, so the "pinned target does not exclude the emergency identities" branch was taken and a `baselineSafetyConflict:unmanaged-browser-emergency-exclusion` was recorded on `enforce` · raise with the baseline author; if the author supplies an object that excludes the exclusions group, the row becomes `create ← s-prereq-exclusion-group@complete`.
- S0 · pinned-refs.tsv · added a trailing `goal` column from `pinned.json` `goalMap` beyond the seven fields the segment names, so V1/V2/V4/V11 could be read from one file · none required.

- S2 · task 1 (which §10 tables) · the task names §10.1–10.5, but §10.7 (the V11 `sourceMapping` rows) is part of the canonical §10 table and example 14 needs it · the parser reads every `gated_action` table under any `### 10.N` heading, so §10.7 is in and a later §10.8 would be too · none required.
- S2 · task 1 (placeholder row) · `<every CA policy step in §11 E–H>:enforce` is one row for 26 steps · expanded from the §11 group headings `### E.`–`### H.` and their `#### \`id\`` entries, each edge carrying `expandedFrom`; the parser reads §11 headings only for this · none required.
- S2 · task 2 (deferred propagation scope) · §4 step 3 evaluates blockers on the next action, yet examples 5 and 10 hold an observing policy on an enforce-side Deferred prerequisite while example 13 keeps an enforce-side conflict off the lane until enforce is next · a Deferred prerequisite on any applicable edge of the step holds it (§8.3: never makes the dependent Ready); every other blocker is scoped to the next action · owner narrows to next-action-only if examples 5/10 were meant to read with the evidence predicate satisfied.
- S2 · task 2 (§8.2 completion) · "answering no completes the owning step" needs a mechanical reading · a step is Completed when it owns a condition the scan resolved not-applicable and nothing exists for it (`isComplete` in lanes.ts); the caller must not resolve `campaign-targets-passkey` not-applicable for an absent campaign (that condition gates no edge today) · none required.
- S2 · task 2 (reciprocal cutover pair) · `s-prereq-security-defaults:start ← policy@ready-to-enforce` and `policy:enforce ← security-defaults@complete` are mutual when `sd-enabled` · ready-to-enforce ignores the enforce gate owned by the requesting step; a lane cycle reads as healthy Up Next, never a hold · none required.
- S2 · task 2 (unstated inputs) · the engine has to read what the scan did not say · an unlisted non-step prerequisite is `blocked` (§8.1 never silently satisfy), an unlisted condition is `unresolved` (edge participates), a step's ladder comes from its gated actions then `effort_kind`, overridable per observation with `kind` · none required.
- S2 · task 3 (`iamai_order` placement) · read from the step index; when two rows both carry a value it orders them immediately before the step-id fallback, and it is empty everywhere so it has no effect today · none required.
- S2 · task 3 (unlock counts) · §12.1 excludes the Security Defaults cutover edges · `unlockCounts(graph, { excludeConditions })` leaves the exclusion to the caller; the §12.1 numbers reproduce with `['sd-enabled']` (asserted) · S3 passes the option when it wires the lanes.
- S2 · task 4 (examples 13 and 15 against the frozen §10) · example 13 names `s-goal-require-managed-device` but §10.3 (V2, S0) records the `baselineSafetyConflict` on `s-goal-unmanaged-browser:enforce`; example 15 lists `exclusion-group@complete` on `s-goal-sign-in-risk:enforce` but §10.5 gates `create` · the tests follow §10, the authority, with the same outcomes · owner realigns the §17 prose (a doc edit, not an engine change).

- S3 · task 1 (row label beside the status word) · the walk and smoke read the row's status word (`.plan-row .status`: In place, Enforced, Report-only, Ready to enforce…) as product truth in a dozen checks, and S7 owns state-word agreement · the lane label is a second line under the state word (`.plan-row .lane`, `Lane · substatus/reason`), the status chip stays · S7 decides whether the status word folds into the lane label; the walk then reads `.lane`.
- S3 · task 1 (steps the graph does not know) · the runtime draws rows the playbook index does not carry (`s-prereq-source-references`, `s-goal-all-users-no-persistence`, the alerting / naming / consolidation / hardening Cleanup rows; the index calls two of them `runtime-source-reference-decision` and `cleanup-harden-emergency-access`) and `deriveLane` throws on an unknown id · such a row takes its lane from the Plan's own state (`planLanes.ts fallbackOf`: complete → Completed, skipped → Deferred, held → On Hold, sequenced → Up Next, else Ready with the nearest substatus) and sorts after the engine's rows by id; no id is named in code · S4 removes the source-references step; the owner adds the remaining ids to §10.0 or confirms they stay runtime rows.
- S3 · task 1 (graph steps this plan lacks) · a baseline that does not hold a goal, or a fixture without it, leaves graph steps with no runtime step, and an unobserved step would queue every dependent behind work that is not on this plan · read as `{ complete: true }` (nothing to do here); a step the person said does not apply reads the same · none required.
- S3 · task 1 (the shared `sourceMapping:62d67e66` edge) · §10.7 puts the mapping on every CA policy create, but the scan knows which policies actually name the group (`Action.missing[].decision`), and one global prerequisite state would hold a policy that does not name it (`s-goal-admin-session` on week two) · the edge reads `resolved` and the pending mapping is an observed `sourceMapping` blocker on each step whose body waits on it (also the ten V11 references S0 deferred, by the same field) · none required; if the owner wants the graph edge to hold, `prerequisiteOf` reads the gated step's `sourceReferences`.
- S3 · task 1 (a missing object the graph has no edge for) · the runtime holds a `correct` whose body names an object the tenant lacks (`s-goal-admins-phishing-resistant` on messy), while §10 gates only `create` on the maker step, so the engine read it Ready · Correct · an `Action.missing` entry whose maker step the graph does not queue this step's next action behind is an observed `missingObject` blocker (On Hold), never a silent Ready; where the graph carries the maker it stays healthy Up Next · owner adds `correct ← <maker>@complete` edges to §10 if Up Next is the intended reading; the observation then drops out on its own.
- S3 · task 1 (conditions) · the runtime records no state for the §8 conditions (mail devices, partner accounts, travel, shared devices, sd-enabled, campaign method) · every condition stays unresolved, so its edges participate (§8.1 conservative) and no question step is Completed by §8.2 · S7 or the owner wires the recorded answers (`mapping` decisions) into `TenantState.conditions`.
- S3 · task 1 (Cleanup rows the graph knows) · `cleanup-drill` and `cleanup-notAssessed` are graph steps; the others are not · a graph Cleanup row is observed as `{ exists: false, complete }`; a runtime-only one is Ready · Create until done (what its row said before) · none required.
- S3 · task 2 (the floor group) · the Plan drew Microsoft's recommendations as their own group after the phases; a lane has no such group · a floor step is a row of its lane like any other; the printed document keeps the named group (planRows.ts) · a later pass may carry `floor` on the row as provenance text if the owner wants it visible on screen.
- S3 · task 4 (SCHED as a secondary projection) · read as: the lane adapter imports nothing from the schedule and a test proves lanes survive every phase/wave/date moving; the When column, the Waiting tile, the rail, the print and the calendar still read `stepSchedule.ts` unchanged · the row carries its phase as `data-wave` for the walk's day-0 reading; nothing else changed · none required.
- S3 · walk and smoke · the board draws one lane at a time, so a check that reads every row reads the three tabs in turn (`readRows` / `showLane` / `acrossLanes` in the scripts, each row kept with its tab and its index inside it) and `Show completed` presses `Show deferred` too · edited blind (the walk is CI-only); if CI reports a P0 the fix is in those helpers · none required.
- S4 · task 1 (the plan-record key) · the mappings were persisted under the removed step's id (`stepDecisions['s-prereq-source-references']`, one answer per source id), and a record saved before S4 must still answer · the key is kept, as `BASELINE_MAPPINGS_KEY` in `src/roadmap/sourceMappings.ts`, and it names no step (`PREREQ_STEP_ID.sourceReferences` is gone; `DECISION_STEPS.sourceReferences` points at the key) · none required; a later migration may rename the key if the owner wants the record to read `baselineMappings`.
- S4 · task 1 (the content step) · `s-prereq-source-references` in content.json was the row's words and the mappings' words in one entry · the entry is deleted; its `decision.references` words moved to `pages.plan.settings.mappings` (same option strings, so a saved answer still parses), `pages.plan.blocked.sourceReferences` became `pages.plan.blocked.sourceMapping` = "Baseline references an unmapped group", `jsonWaitsDecision` no longer takes `{steps}` (with no step to name, the slot would have printed the author's id), `whoUnknownDecision` no longer says "unidentified groups", and `shared.contract.fixMapping` is the opened step's Fix line · none required.
- S4 · task 2 (where the role lives) · the role was computed only on the removed row · `generate.ts` annotates every open policy's `action.sourceReferences` with role / baselinePolicies / baselineTotal from `referenceUsage`, `planLanes.ts observe` puts the role on the `sourceMapping` observed blocker, and the engine's `Blocker` carries an optional `role` (`SourceRole` in `lanes.ts`); `sourceMappingsOf(steps)` reads the record across the plan · none required.
- S4 · task 2 (the reason on the row) · the board's On Hold heading was "Baseline mapping" and the row's reason came from the step it waited on · `BOARD.blockers.sourceMapping` is `BLOCKED_REASON.sourceMapping`, so the group heading, the lane label and the row's own blocked reason all read "Baseline references an unmapped group"; a runtime-only row the graph does not know (`s-goal-all-users-no-persistence`) carries the same blocker as its reason from the fallback path · S7 decides whether the heading should be shorter than the reason.
- S4 · task 2 (the graph edge) · §10.7 still puts `sourceMapping:62d67e66` on every CA policy create · unchanged from S3: the edge reads `resolved` and the observed blocker holds only the policies whose bodies name a pending reference; the ten V11 references S0 deferred appear in the mappings surface by the same field (the scan already asks them) · owner decides whether to prune §10.7 or keep the edge as documentation.
- S4 · task 3 (naming a reference) · A1 §18.1 names a reference by its short id; the content had "Group {n}" · rows read `Group 62d67e66` / `Named location 1267ac22` (kind + short id, `pages.plan.settings.mappings.groupLabel`), with the full id under a disclosure; no running number anywhere · none required.
- S4 · task 3 (the picker) · the surface reuses the opened step's `Options` (radios + single picker) exported from `ContentStep.tsx`, with the tenant's groups / named locations from `pickerUniverse` · none required; `BaselineMappings.tsx` is the one component, `baselineMappings.ts` its rows.
- S4 · `dependency-data.json` · the index still carries `runtime-source-reference-decision` as a step (A1 §10.0) · left as is: no code reads the id and the engine's `observed` marks it complete like any graph step the plan lacks · owner removes it from §10.0 when the playbook is next regenerated.

- S5 · task 1 (what a tile is) · the region had summarised the fixes as one "Prerequisites · N remaining" tile beside Fix before continuing · `readinessOf(step, contract, blockers)` now returns `tiles` (unresolved: the state's own tile, the emergency boundary, an unestablished reach, one tile per `contract.fix`, one per engine blocker the fixes do not already name, the hardening last) and `satisfied` (good/info facts, behind "{n} satisfied"); the engine's `LaneResult.blockers` reach the step as `LaneReading.blockers` → `planBoard.ts readinessBlockersOf` (labels from `BOARD.blockers`, titles from the board) → `ContentStep blockers` prop, and the printed step reads the same `laneReadings` · none required.
- S5 · task 1 (dedupe) · an engine blocker and a contract fix can name one thing (the emergency step, a mapping) · the fix's tile wins by key (`step:`/`missing:` vs the engine's step id, `mapping` vs `sourceMapping`, `baseline` vs a conflict, a `decision` on a step whose own condition is needs-decision, `missing:` vs `missingObject`); two engine references pending on one policy are one mapping tile · none required.
- S5 · task 1 (links) · a step tile links `#/plan/<id>` (`shell/routes.ts returnToStep`); a mapping tile is a button that opens Plan settings → Baseline mappings (`Plan.tsx openSettings`, handed down as `onOpenMappings`; print has none) · a link into another lane found nothing on screen, because the board draws one tab at a time, so `TabFollowsOpenStep` switches the tab to the opened step's lane (`TAB_OF`) when the open id changes · none required.
- S5 · task 1 (widths) · A2's `@media(max-width:940px){.readiness-strip{grid-template-columns:1fr}}` was written for three tiles · four across at desktop, two at 940, one at 650 (`app.css`), so a wrapping strip never squeezes a sentence into a quarter column; `planAnatomy.test.ts` asserts the new rules · owner may restore one-per-row at 940 if the pack is updated.
- S5 · task 1 (tile disclosure) · "expandable explanation/evidence" · each tile with a note, a link or evidence carries a `<details>` (summary word `readiness.tiles.detail` = "Why"), open while printing; the hardening's recommendations, unchecked count and Defer/Undo are the Resilience tile's evidence (`HardeningBody`), its lead sentence the tile's note · none required.
- S5 · task 2 (what was removed) · Fix before continuing (`FixBeforeContinuing`) and the Hardening recommendations section (`HardeningRecommendations`) restated the tiles; the planning preview's "Checks to confirm first are under Readiness." pointer restated the confirm tiles · all three gone from the step; the preview's own label, its "not ready to run" line and its unresolved-values line stay because they explain the missing Copy, not the state; the baseline-conflict attention (A2 V5 `.attention.danger`) stays because it carries the source explanation no tile does; the Needs attention board filter is a board control, not a step container · `pages.app.plan.stepContract.fixHeading` stays for the export's section (`roadmap/artifactLines.ts`); the `readiness.tiles.open` key is deleted; new keys `readiness.tiles.prerequisite/check/review/mapping/satisfied/detail/openStep/openMappings` and `implementation.withheld.several`.
- S5 · task 4 (Microsoft Learn · Troubleshooting) · A2 draws Learn → inside Why on every variant and the pack is hash-guarded; Troubleshooting is the Implementation region's support line · left where A2 puts them (Why → Readiness → account selection → Implementation, with Troubleshooting → and Source checked under it → Done when); no second Learn line was added under Implementation · owner says whether the Learn link moves to the Implementation support line for every step.
- S5 · task 4 (accounts "not held") · with two selected accounts each owing work, the package's one-account JSON was withheld as "IAMAI does not hold emergency account" (`emergency.target.userId` binds only when exactly one confirmed account owes work, stepPackage.ts) · `severalBindings(step, ctx)` names the bindings left unbound because the plan holds a set where the channel takes one (several trusted locations, several emergency accounts), and the withheld line for such a channel reads `withheld.several`: "JSON is not shown yet: it is written for one emergency account at a time, and the plan holds several."; the binding rule itself is unchanged (S6 owns channel content) · S6 may project the one-account JSON once per account.
- S5 · task 5 (Why / Done when) · the only boilerplate the simplified anatomy exposed was the words "Fix before continuing" in content: `doneBlocked`, `implementation.empty.blocked`, `hardening.leadBlocked`, the emergency step's `whatToDo.lead` and its first `doneWhen` line ("Every minimum safety check passes on the next scan.") · reworded to name Readiness; Session Lifetime (`s-goal-all-users-no-persistence`, content `session-lifetime`) and MFA-for-all (`s-goal-mfa-all-users`) read Why → Readiness → Implementation → Done when with nothing duplicated, so no edit · none required.

- S7 · task 1 (the export's one state) · `ExportStep` carried `stage` and `condition` separately and `artifactLines.ts stateLine` joined them, so a prerequisite row reading Needs attention (no stage, condition healthy) left in the calendar entry and the bundle as "Healthy", which the badge never says · `ExportStep.state` is the badge (`stepContract.ts badgeLabel`, the same string the opened step's header shows), `stage`/`condition` are gone from the view and the bundle (`prompts.ts` writes `state`), `stateLine` returns it unjoined · none required; the lane label (`Ready · Create`) is not in the export, because the export view is per step and the lane is read over the whole plan (`planLanes.ts`); an owner who wants it in the bundle adds a `lane` field fed from `laneReadings` at the Export page.
- S7 · task 1 (review-required on an enforced policy) · tracking raised `review-required` on a member whose meaning drifted and then advanced `satisfied` on the coverage verdict, so the step was `done`: the row said In place, Done when said "Already satisfied", and only the export's condition said Review required · `tracking.ts` withdraws `satisfied`/`inPlace` when any member needs review (`reviewRequired` or an unsafe correction), with a history note (`engine.tracking.reviewWithdrawn`, new key), and never advances `satisfied` while the review stands; `lifecycle.ts heldForReview` covers `enforced` as well as report-only and ready-to-enforce; `planState.ts` reads a held enforced review as `Enforced · Review required` (kind `correction`, `withStage`) and a held ready-to-enforce review as `Ready to enforce · Review required`, the same shape the report-only row already used · the lane engine reads it Ready · Correct / On Hold from `drift` (S3 choice: row word and lane label stay two lines).
- S7 · task 1 (Fix before continuing scope) · every readiness blocker with a binding ("when 1 trusted location exist (now 0)") was listed under Fix on a step whose What to do said to create the policy in report-only today · `stepContract.ts fixOf` drops readiness-kind blockers while the step's one scheduling result is the dated report-only create (`stepSchedule.ts` `createsWhileGated`, transition `createReportOnly`); undated, they are listed as before; step blockers ("Finish … first") stay, because the schedule still reads a held step blocker as holding the create · none required; if the owner wants step blockers scoped to the enforce too, the same `creating` guard applies to `kind === 'step'`.
- S7 · task 2 (what counted as tenant-derived) · content.json example values named the product's own tenant ("GetIAMAI", `breakglass@getiamai.onmicrosoft.com`, `getiamai.onmicrosoft.com`, a real tenant-id prefix) and the owner as operator/admin/guest; `content/render.ts` carried the same UPNs; the `getiamai` fixture's generated initial domain was literally `getiamai.onmicrosoft.com` (the seed is the fixture name) · examples now read Contoso Pty Ltd / Alex Morgan / `contoso.onmicrosoft.com` / `1a2b…`; the fixture generator's initial domain is `${seed}-fixture.onmicrosoft.com` for every fixture (no name is special-cased; `${seed}.example.com` UPNs stay, example.com being reserved); the owner's Home attribution stays (`home.test.ts` asserts it); device names in fixtures (`DESKTOP-7Q2`, `LAPTOP-ADMIN`, `MacBook`, `Boardroom`, the `SM-S918U` model code the inventory copy also uses as its example) and the fictional names (Kaladin Stormblessed, Shallan Davar) were judged synthetic and kept; the fixture name `getiamai` stays (a dozen tests name it) · `syntheticIdentities.test.ts` guards the examples, the generators and every fixture snapshot.
- S8 · recovery · the runner stashed S7's work uncommitted (its session ended while the suite ran) and S6's BLOCKED.md lines · applied both (`auto-stash after S7 (exit 0)`, `auto-stash after S6 (exit 0)`), typechecked and tested green, committed as S7; `auto-stash after S5 (exit 0)` (run-overnight.ps1: session-limit patterns and the resume note) is a runner edit, not a segment deliverable, and stays in the stash list · owner applies or drops it.
- S8 · HEAD red at S6 · three correctionProjection tests failed at 4494d37: S6's request-or-nothing rule withheld the correction bodies the tests read as Copy's text · authored the requests (above) rather than asserting the withholding; the bare-body rule is still proven in library.test on a stripped clone of the same package · none required.
- S8 · walk helpers · the walk read S4's "Baseline mappings first (Plan settings): …" as a step that can write (its regex wanted " first: the baseline names …"), so eleven checks about counts, platforms, emails and manager lines fired on held steps; a closed Readiness Why left the drill's who-and-why line out of innerText; a row marked done earlier (the drill) moved its lane's indices, so the loop opened the drill where it labelled Intune · the regex follows the wording, every `details` in the step is opened before it is read, and a row is opened by its title before its index · none required.
- S8 · smoke · the board keeps the lane the last opened step chose and a loaded plan starts with neither toggle pressed, so the text before and after a plan-file load differed by lane and by the Deferred group · both readings are taken on Ready with Show completed and Show deferred pressed · none required.
- S8 · support line · `.impl-support-links` was an inline-flex row, so innerText put "·" on a line of its own and the walk read fifteen steps as carrying an empty value · the separator is text inside an inline span · none required.

- A1a · task 5 (id count) · decisions 7 and 8 leave 44 ids in `dependency-data.json` (48 − the three goals not in the pinned baseline − the removed `runtime-source-reference-decision` row); the DONE-WHEN says 45 · the data carries 44 and the doc-vs-data test is green; the §10.0 `generated` column and the parser skip every edge naming a `no` step on either side (`s-prereq-security-defaults:start ← s-goal-azure-management-mfa` among them) · owner confirms 44 or names the 45th.
- A1a · task 5 (§12) · §12.1 says "do not hand-edit" and no generator exists · counts regenerated with `sorting.ts unlockCounts` (break-glass 3/24, exclusion-group 19/21, device-plan 1/3, service-accounts-group 2/2) and the dependents column edited by hand for the three removed goals · owner may add a generator.
- A1a · task 1 (`unavailable` reasons) · one legacy kind, eight reasons · unmatched-pair and no-operation → `unsupported`; unsafe-emergency-access → `baselineSafetyConflict` (§15: the target is unsafe as authored); unverified-emergency-exclusion → `fact` (a required fact not in evidence); missing-object, baseline-conflict, readiness-unmet and escape-hatch-unverified are read by their own counterparts (Action.missing, the condition, the threshold gate, the gate's step edge) · none required unless the owner wants all four under `unsupported`.
- A1a · task 1 (which legacy waits become step edges) · a legacy step blocker to the emergency gate (break-glass / exclusions group) gates `enforce` on a policy (A3 B3; §18.3: exclusions gate enforced CA, never report-only); any other step blocker (a maker, `trusted-location`, `device-decision`) gates the next action; `blockedBy` sequencing with no blocker is not mapped, the graph owns ordering · the device-decision wait moves `s-goal-intune-enrollment-reauth` to Up Next after `s-prereq-device-plan` on large and demo-week2 (R-FIX updated with the reason) · owner adds the edge to §10.3 or drops the legacy blocker.
- A1a · task 1 (the Observing reason on the adapter) · the engine's Ready · Observing reason is an `evidence` blocker, and the board's label map (`planBoard.ts BOARD.blockers`) is a renderer A1a may not touch · `LaneReading.reason` and `.blockers` are `HoldBlocker` (every kind but `evidence`); the gate reaches the reading on `LaneReading.gates` with its text and time part · A1b renders the Observing reason from `gates` with `pages.plan.lanes.*` words.
- A1a · task 3 (condition sources) · the adapter reads steps, not the plan record · `sd-enabled` is the cutover step the scan put on the plan (not done), `shared-devices-exist` its step, `campaign-targets-passkey` true, a carve-out its step when the answer produced one, else the recorded answer through an optional `answers` argument (`laneReadings(steps, rows, answers)`), else unresolved; a step marked does-not-apply resolves its condition not-applicable · the renderers still call it without answers · A1b passes `mapping.questionAnswers`.
- A1a · task 4 (one window) · `observationDaysFor` (roadmap/schedule.ts) reads the package's `observation.minDays` first (content/implementation/observation.ts), so the schedule, tracking's time gate and the evidence gate state one window; `RUNTIME_META_KEYS` carries the field; no package authors it yet · none required.
- A1a · task 6 (`packageStateOf` order vs correction batch 2) · `correctionProjection.test.ts` asserted that an enforced legacy-authentication block missing its exclusions reads `blocked` until emergency access is sorted (correction batch 2: "the Plan releases it only once emergency access is sorted"); task 6 puts the safe correction (`correctionFieldsOf` non-empty, Foundation A writable) before the hold, so the same step reads `partial` · the assertion follows task 6 and says so in place; `nextSafeAction` still reads `correct · not executable` and the emergency gate still holds enforcement · owner confirms the reversal or narrows "safe" (e.g. exclusions-only corrections) in `stepPackage.ts packageStateOf`.
- A1a · task 7 (R-FIX) · the Plan-page and MOCK rows need App.tsx's chain · recomputed the runFixture rows with the scratch script (outside the repo), derived the rest from the per-row moves and marked them · A6 recomputes on the page.

- A1b · task 3 (When words beyond the five named) · `rowWhen` also puts a readiness threshold, "held until reviewed", "held until the records clear", "ready now" and "ready {date}" in the column, none of them a day · the board (`planBoard.ts boardWhenOf`) reads the day the plan's one scheduling result gives the step for every non-day value, else the placeholder; the words stay on the reason line and in the lane label; `pages.plan.when.notScheduled` / `complete` are gone and `cleanupExport.ts cleanupWhen` reads `when.none` for an undated Cleanup row (print reads it too); a deferred step keeps the scheduler's day and reads the placeholder like a finished one · none required.
- A1b · task 6 (`lanes.ts` literals) · the DO says the engine's literals read content.json and the DON'T says do not touch the engine · the engine's `Substatus` literals stay identifiers; `planBoard.ts SUBSTATUS_WORD` maps them to `pages.plan.substatus.*` and the comment that kept the words out of content.json is gone · none required.
- A1b · task 8 (emergency access with failing checks) · `planUsability.test.ts` asserted "no Ready now with fixes outstanding" (owner, 2026-09-11); the engine reads the step as Ready · Create and decision 1 / task 2 map Create → Ready now · the test follows the decision (the fixes are Readiness's and What to do's) · owner confirms, or the engine reads an existing object with failing checks as `Correct`.
- A1b · task 8 (forbidden words) · `Blocked` and `Held` are matched as capitalised state words (a Done-when sentence says an account's sign-in is blocked); the three phrases in any case · "This step is set aside." (`shared.engine.milestone.setAside`, `stepContract.implementation.empty.setAside`) now reads "This step is deferred." · none required.
- A1b · legacy word · `planState.ts` still produces the row word for the export view (`stepExport.ts status/state`) and the lane adapter's fallback; nothing on the Plan reads it · A1c moves the exports to the lane and deletes the word fields.

- A1c · task 1 (where the lane comes from) · `stepExportView` is per step and the lane is read over the whole plan (S7 note) · `stepExportView(step, ctx, lane)` takes the board's `LaneView`; Export.tsx builds `laneReadings(steps)` once and hands each step its `laneViewOf`, ContentStep hands the AI Info tab the `laneView` it already holds, and a caller with none reads `laneViewFor(step)` (the step alone, as the opened step does) · none required.
- A1c · task 1 (the word fields) · `ExportStep.status` (the row word) and the bundle's `statusWord` restated the state in a retired vocabulary · both deleted; `ExportStep` carries `state` (the lane label), `lane`, `substatus`, `reason`, `fact`, and the bundle repeats the five · none required.
- A1c · task 1 (ICS SUMMARY) · the SUMMARY's fallback was the milestone sentence (`v.next`) where the rail had no transition word · the fallback is the lane label; the transition words (Create in report-only, Turn the policy on, …) stay first · none required.
- A1c · task 1 (plan file, CSV) · the plan file carries raw steps (`fileStep`: status, dates, ids) and no state word; the CSV tables are the readiness and inventory tables (people, groups, policies) and carry no step · neither changed · none required.
- A1c · task 1 (redaction) · the redacted bundle rewrites "Not supported" to "Not [a department 5]ed" on the demo, whose departments include Support: `redactSnapshot.ts` substitutes vocabulary inside words · the export test compares the unredacted bundle; the redactor is untouched (not this segment's root cause) · owner decides whether the redactor should match whole words only.
- A1c · task 2 (undated rows) · "Waiting on something else" is removed and the undated rows still print · they print grouped by their own lane word (Ready / Up Next / On Hold, `LANE_ORDER`), each step in full as before; `app.plan.held` is deleted from content.json · none required.
- A1c · task 2 (Completed / Deferred) · the DO adds two sections · each is a list of `title · lane label` under the lane word (the cover already names the finished goals; a deferred step no longer prints under its phase: `planRows.ts deferredRows`/`completedRows`); the print's Cleanup rows read their lane from the same `laneReadings(steps, cleanupRows)` and `statusWord.ts cleanupStatusOf` is deleted; the cover's posture line reads "Completed (n):" · none required.
- A1c · task 3 · Connect's tile said "N done" from `stepFacts` · `PlanInput.counts` is `{ steps, completed }`, `completed` from `planLanes.ts laneCountsOf(laneReadings(...)).Completed` over the same rows Plan.tsx draws; `stateCounted` reads "{steps} steps, {completed} completed" · none required.
- A1c · task 4 (walk) · the walk read `.status` as the row's state; the row's state is `.lane` and `.status` is the fact chip · `readRows` returns `label` and `chip`; every check that keyed on In place / Enforced / Blocked / Held / Ready to enforce / Skipped keys on the lane label or the chip; "held until the records clear" has no row reading any more (the gate lives on Done when, unit-tested in readyWhen.test.ts), so that walk check is gone; the week-two "no row reads Enforced" check is replaced by "every chip is Report-only, Enforced or nothing" because decision 2 makes Enforced a tenant fact; `RE.rowWhen` left contentChecks.ts (readyWhen.test.ts holds the row's own three forms) · edited blind (CI runs the walk); A6 reads the result.
- A1c · task 4 (contracts) · `plan` and `plan.step` allowed the retired chips, the Roadmap/Status tabs and the old toggles · headings are the lane words and the On Hold blocker groups, tabs the three lanes, tiles the four, chips the two facts (and, on the step, the lane-label and Prerequisite · <lane> patterns), buttons Show completed / Show deferred and Defer this step; a `lane` list documents the row's label pattern (no script diffs it) · none required.
- A1c · task 5 · CLAUDE.md said sessions never run the walk · one sentence names the batch's closing segment (A6) as the exception · none required.
- A2 · task 1 (tile) · `estimate` is `schedule.estimate.targetEnd` (the placement end before withdrawal) and `committed` is `planFinish().finish` (the last ring end extended to Cleanup, null while held), so on an unheld plan the two differ by the Cleanup window and both lines show · `derive/finish.ts projectedFinish(finish, estimate)` is the one pair; the same day (by `absoluteDate`) is said once · none required.
- A2 · task 2 (tip) · the length InfoTip beside the tiles and the tile's tip would have said the same thing twice · the tip moved onto the Projected finish tile: `title` on the tile for hover, the InfoTip beside the date in the value cell (not the label cell the smoke and the walk read the tile's name from, where its "i" read as "Projected finishi"); unheld it reads `derivation.criticalPath` plus `relaxed`, held it reads `lengthTipEstimate` from `estimate.reason` because the withdrawn schedule's chain no longer measures the estimate the tile shows · none required.
- A2 · task 3 · Cleanup rows already read `pages.plan.when.none` (—) since A1b (`cleanupExport.ts cleanupWhen`, asserted in planUsability.test.ts); `notScheduled` has no consumer · nothing changed · none required.
- A2 · task 4 (freeze) · no new file: `freezeInputOf(from, to)` sits in `roadmap/schedule.ts` beside `ChangeFreeze`; `from` is stored at T00:00 and `to` at T12:00 (the spec names `to`; a one-day freeze is therefore accepted); the days as typed are Settings-local state, and a rejected freeze stores `null` on the record with `freezeNeedsTo` / `freezeOrder` (new content keys) shown under the inputs · none required.
- A2 · task 5 (print) · `line1` reads "finishes {finish} at pace" and `line1Committed` adds "committed {committed}"; the cannot-finish branch remains only when no estimate exists, and a settled plan always has one, so the printed line no longer says "cannot finish until …" (the cover's Dates cell still names the constraint) · owner may want the constraint clause back on the line for a held plan.
- A2 · suite · two A1c-stale checks failed on the untouched tree: `ladder.test.ts` expected the walk's `mfaInPlace` line to read In place|Enforced (the walk reads the Completed lane label, decision 1) and `accessibility.test.ts` filtered a `$comment` string A1c put inside `allow` · both checks updated to A1c's state, nothing reverted; `scripts/smoke.mjs headerOf` reads the tile up to the Started label because the tile now has three lines · none required.
- A3 · task 1 (one source) · which sections a step draws and which channels it offers were decided inside `ContentStep.tsx`, which Node cannot import (JSX), so no script could state them without a second copy · the block moved verbatim to `src/ui/surfaces/stepBody.ts` (`stepBodyOf`, `headingsOf`, `channelTabsOf`); the component destructures it and the Implementation tabs read `channelTabsOf`; the eleven source-text tests that read the body's decisions read `ContentStep.tsx` + `stepBody.ts` · a new product file where the run context says edit-only: the smallest option that keeps one producer; none required.
- A3 · task 1 (clock) · `runFixture` let the tracking stamp its history with `new Date()` (tracking.ts `trackExecution` default) · a fourth parameter `now`, passed as the fixture's `snapshot.asOf` by the snapshot module only and never memoised; every other caller unchanged · none required.
- A3 · task 1 (time zone) · dates format in the process's zone (copy/dates.ts `displayTimeZone` unset), so a laptop at +10 and the UTC runner would write different days · the snapshot module sets the display zone to UTC while it computes and restores it after · none required.
- A3 · task 1 (shape) · `reason` is the row's reason line (planBoard.ts `boardReasonOf`); `tiles` are the unresolved strip tiles (`readiness.tiles`, label and value) and not the satisfied ones behind the disclosure; `rail` is the metric; Cleanup rows join the lane readings as the Plan composes them but have no file (they are not steps); the chain is `runFixture`'s, so `demo-week2` records 27 steps where the Plan page draws 30 (the seeded week-two answers are not applied) · none required.
- A3 · task 3 (registry) · a package edit recompiles `src/content/implementation/registry.generated.json`, which would make every package commit a src/ change and demand [snapshots] · the generated registry does not count as src/; where src/ and a package change in one commit the tag governs, since the src/ change may move any step · none required.
- A3 · task 3 (range) · the check needs history · `actions/checkout` fetches the whole history; a push checks `event.before..HEAD` (`HEAD^..HEAD` where `before` is unknown), a pull request `origin/<base>..HEAD`; merge commits are skipped and the commits they merge are checked one by one; the unit test runs the same rules over constructed commits and over `HEAD^..HEAD` · none required.
- A4 · task 1 (A1, A6) · both already hold on the ownership code: A1's foreign-target update reads `shared-satisfier` and hands nothing over; the audit's `user4` is one of the demo's two guests, whom Guests MFA covers, so excluding them from the all-users policy leaves the goal delivered (the test excludes an ordinary member and asserts the gap) · the tests exist and pass · none required.
- A4 · task 1 (A3, A4, A5) · one root cause: the plan compared a deployed policy to its target only along the dimensions its patch writes · an update carries the whole policy the plan writes (`PolicyOperation.intent`); `observation.ts unwrittenDifferences` reads the deployed object against it less the patch, and less the scope and controls coverage already judged for a candidate (a narrower role list that still reaches every admin the tenant has is not a difference a person is sent to); tracking raises review with a note naming the dimensions and a `manual` correction; `operations.ts` names `manual-correction` where the update has nothing to submit, with its own words on the row, the step, the export and the milestone; a live policy carrying the step's tag or the plan's own name is corrected, never re-created as "(2)" · a policy still waiting on a source reference is not read against an intent it has not resolved (the token would read as a difference), so on the uncurated demo the review names nothing until the mapping is answered; none required.
- A4 · task 1 (A2) · `generate.ts` skipped a goal coverage could not settle, so one unread group dropped five steps · the goal keeps its step with no operation, held on a `Blocker.evidence` carrying `unverified: true` and the group's short id; the lane adapter reads it as a `fact` hold; `observationsOf` therefore carries the step's window through the blind scan instead of preserving a stale record (rescanDurability 043.13 asserts the window carries) · a new blocker kind would have rippled through eleven consumers, so the flag rides on the existing kind; none required.
- A4 · task 1 (adapter) · `planLanes.ts` read every adjust step with an existing policy as drift, so no report-only policy could ever read Ready · Observing from the engine · drift is a review, or an update that writes something other than the state (`submitsEnforcementOnly`); two snapshots (messy, midflight admins policy) moved from Ready · Correct to Ready · Observing · none required.
- A4 · task 2 (fixture) · only two goals are free of the baseline's unmapped-group hold on the product demo, and register-info is frozen · the Intune enrollment sign-in-frequency policy (3 days, 20 people, undrifted from the pinned body) is the third week-two policy; it reads Ready · Observing with the device decision and the session-loop readiness item as enforcement gates · its legacy condition is `blocked` (the gates), which 042.5 now exempts for a report-only policy whose blockers are all readiness or step gates, and its milestone reads observe with no date (`lifecycle.ts`, a readiness-held report-only policy); R-FIX counts and the A3 snapshots updated; none required.
- A4 · task 3 · every one of the 13 packages that author `verifiedSources` already carries `checkedOn` on every entry; the line was withheld on 8 of them because `sourceUpdatedOn` dated only `userFacing` entries · it dates every entry (decision 10); pilot.test.ts and implementationChannels.test.ts updated to the decision; no package edited · none required.
- A4 · task 4 · §7 already carried the 7/3-day default and `observation.minDays`, and §10.0 already documented `generated` (A1a) · §17 example 13 now follows §9.4/§10.3 (require-managed-device is outcome 1, the conflict case is unmanaged-browser, which is not generated) and example 15 follows §10.5 (the exclusions group gates `create`, verify-mfa gates `enforce`, no licence edge) · none required.
- A4 · task 5 · `reference/goal-gaps.md` was already committed at d8bcf53 with no working-tree change · nothing to commit · none required.

## Chrome checks

- (none in S0)
- (none in S1)
- (none in S2)
- S3 · live look at the demo Plan (dev server, Chrome) · PASSED: Ready 12 / Up Next 3 / On Hold 15 tabs with counts, `Lane · substatus` under every state word, Work type select, Show completed / Show deferred toggles; On Hold grouped as Baseline conflict (1) and Baseline mapping (12); no console errors.
- S5 · live look at the demo Plan (dev server, Chrome; `.step-main > section`, `.readiness-strip.unresolved .readiness-tile`, `.readiness-clear`, `.readiness-satisfied`) · PASSED: Emergency Access (Initial scan) is Why → Readiness → What to do (account picker with Break-glass 1 and 2) → Implementation → Done when, three tiles Emergency access · Not available (Why opens each account's standing) / Check (the failing minimum check) / Resilience · Needs attention (Why opens the hardening lead and recommendations), no Fix before continuing, no Hardening section, the JSON line reads "written for one emergency account at a time, and the plan holds several"; Require MFA for Everyone and Limit How Long Sessions Last (On Hold) draw Threshold / Affected people / Baseline mapping (Open Baseline mappings opens Plan settings at the mappings) / Prerequisite step (Open Create or Correct Emergency Access Accounts switches to Ready and opens it); Follow-up scan Emergency Access reads "✓ Clear · Nothing outstanding changes the next action." with "3 satisfied" (Available / Meets recommendations / In place) behind its disclosure; widths 896 → two tiles across, 596 → two across at 255px with no overflow before the 650 rule (added after), body one column, bar stacked; no console errors.
- S4 · live look at the demo Plan (dev server, Chrome) · PASSED: no "Decide What the Baseline's Unidentified Groups Stand For" row; On Hold groups Baseline conflict (1) / Baseline references an unmapped group (13) / Not supported (1), each held row's lane label reads the reason; Plan settings → Baseline mappings lists Group 62d67e66, Group e663a7ce, Group 2d25c298 … with "Unmapped · Excluded from its policies", the count line and the policies named; no "Group N" text on the page; leaving one out and Save moved the tabs Ready 11 / Up Next 3 / On Hold 15 → 14 / 7 / 8 and the row read "Left out"; Clear this answer put it back to Unmapped and 11 / 3 / 15; no console errors on load or after.
- (none in S7: the segment names no Chrome check)
- S8 · live check (dev server 5173, Chrome; the brief's §28 is not in the repository, so the eighteen items are the segments' recorded checks; the extension's window resize did not take on the maximised 2560×1215 window, so the narrow readings came from a same-origin 596×896 iframe of the same URL, as S6 did; four CDP evaluates timed out at 45 s on clicks that open a panel or a dialog, the plan-file round trip was reproduced by hand to find the toggle race, and one narrow item could not be measured) · 1 PASS tabs Ready 11 / Up Next 3 / On Hold 15 with counts · 2 PASS `Lane · substatus` under all 29 rows · 3 PASS Show completed reveals the one In place row · 4 PASS On Hold grouped Baseline conflict / Baseline references an unmapped group / Not supported, no unidentified-groups row · 5 FAIL not verified: the Plan settings panel (header button and the mapping tile's Open Baseline mappings) opened once (S8 item 8) and hung the CDP evaluate on every later attempt, so the Group <short id> rows were not read this run (S4 read them) · 6 PASS Require MFA for Everyone reads Why → Readiness → Implementation → Done when · 7 PASS four Readiness tiles each with a Why disclosure, no Fix before continuing, no Hardening section · 8 PASS the Baseline mapping tile's button opens Plan settings at Baseline mappings · 9 PASS the Prerequisite step tile's link switches to Ready and opens Create or Correct Emergency Access Accounts · 10 PASS Follow-up scan Emergency Access is In place and reads "✓ Clear · Nothing outstanding changes the next action." with "3 satisfied" · 11 PASS Follow-up Intune step offers Entra / PowerShell / JSON / AI Info with no withheld stand-in line (the JSON channel is back since the request was authored) · 12 PASS support line "Microsoft Learn · Troubleshooting" on one line, left-aligned, no source line where the package records no date · 13 PASS Expand implementation opens a 1400×1167 viewer in a 2560×1215 viewport with Copy implementation, Entra selected, code 13px · 14 PASS the opened step's badge carries the row's word (Needs correction → Enforced · Needs correction) · 15 PASS 596 px: 15 rows on the board, scrollWidth 581, no horizontal overflow · 16 PASS 596 px: Readiness strip one column (519 px tiles), step body one column · 17 FAIL not verified: the Expand click inside the 596 px iframe opened no dialog and hung the evaluate (S6 measured 580×880 with 14 px padding at 596) · 18 PASS no console errors on load or across the session · plan-file round trip by hand: decision + skip + start date, Save plan file, Load → same lanes, Completed and Deferred groups once both toggles are pressed one at a time.
- (none in A1c: the segment names no Chrome check; the export = badge agreement is stateAgreement.test.ts over every demo step, and the walk's rewritten row checks run in CI)
- A1b · live look at the demo Plan (dev server 5174, Chrome; `.plan-progress-tile`, `.plan-controls [role=tab]`, `.plan-row .lane`, `.plan-row-status .status`, `.plan-row .when`, `.step-head-state .status`, `.readiness-bar-head`, `.step-side .metric`) · PASSED: Initial scan — tiles Steps 30 / Completed 1 / Projected finish — / Started —; tabs Ready 11 / Up Next 3 / On Hold 15; Show completed 1 / Show deferred 0; every row's chip is Report-only, Enforced or empty; every When is a day or —; lanes read `Ready · Create`, `Ready · Needs decision`, `Up Next · After …`, `On Hold · Baseline conflict` / `… unmapped group` / `Not supported`, `Completed`; the opened conflict step's badge, bar and rail read On Hold · Baseline conflict / Baseline conflict / On Hold · Baseline conflict · Resolve prerequisites, its prerequisite tile `Prerequisite · Up Next: Create or Correct Exclusions Group`, footer `Defer this step` · `Scan to update the plan`; Follow-up scan — Steps 33 / Completed 10, Ready 11 / Up Next 2 / On Hold 10, same chip and When readings; no Blocked / Held / Needs attention / Skipped / Set aside anywhere in main on either snapshot; no console errors.
- A2 · live look at the mock and the demo Plan (dev server 5175, Chrome; `.plan-progress-tile dt/dd`, `[title]`, `.infotip-btn`) · PASSED: mock — Steps 21 / Completed 1 / Projected finish "Oct 11, 2026 · at pace" (held plan, no committed line) / Started —, tile title reads "Once nothing is held, the plan is about 4 weeks because two changes prompt the same people, so Require MFA to Register a Device cannot run in the same window as Shorten Admin Sessions."; demo (entered by Try it with sample data) — Steps 30 / Completed 1 / Projected finish "Oct 4, 2026 · at pace" / Started —, tile title names the Intune / registration soft conflict; the smoke's `progressOf` regex and the rewritten `headerOf` regex both match the live text; no "Not scheduled" on either page; no console errors. Not checked live: the freeze rejection message (Plan settings hung CDP in S8; `freezeInputOf` is unit-tested) and a committed line (both fixtures are held).
- (none in A3: the segment names no Chrome check; the acceptance is stepSnapshots.test.ts and changeScope.test.ts)
- (none in A4: the segment names no Chrome check; the acceptance is tracking.drift.test.ts A1–A6 and the demo-week2 Intune row reading Ready · Observing in planLanes/stepSnapshots)

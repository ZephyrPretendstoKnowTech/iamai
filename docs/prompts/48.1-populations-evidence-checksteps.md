# 48.1 — What the live Plan showed: populations, evidence, check-step Do it

Precondition: 48 committed and green (08c73d3). Walked on getiamai.com against
GetIAMAI on 31 Aug. The structure passes: the first row sits a quarter-screen down, one
status per row, the step opens in place, the assumptions strip and its editors work,
both themes render. Four things do not pass, and they are the ones the target state
exists for. Do this before 49.

## Part 1 — One denominator, on every row and every step

Today says `4 active people of 12 enabled · MFA proven 2 · unproven 2 · no method 0`.
The Plan says `11 people` on almost every row, `11 people · 3 active · 2 admins · 1
guest` on the campaign step, `covers 1 of 13 people` and `covers 12 of 13 people` as gap
suffixes, and the campaign's Do it tells the operator to issue Temporary Access Passes
to `8 users without a method` — the eight dormant test accounts that never signed in
and that the plan's own second row says to disable. Three denominators on one page,
none of them Today's.

1. Every population a row or a step renders is `activeUsers` from `src/derive/sets.ts`
   (target-state §8.1). The who-line says `4 people`, not `11`; `nobody affected` when
   the evidence shows nobody; names when ≤3. The full in-scope count may appear once,
   inside the step's population line, as `covers 11 enabled` after the active count —
   never as the headline number.
2. Never-signed-in and disabled accounts are in no step's population except `Decide on
   N dormant accounts`. The campaign step's Do it is rebuilt from the Today states of
   active people only: the named unproven people and what each does (one MFA sign-in),
   the named no-method people and what each does (register; TAP when off a trusted
   network); the break-glass account is never in it (it has its own drill).
3. Gap suffixes (`covers 1 of 13 people`) use the same set: `covers 1 of 4 active`.
4. The agreement test gains the pair Today tiles ↔ every step's who-line and population
   line, on every fixture and on the GetIAMAI-shaped one, and fails the build on any
   number Today does not also produce.

## Part 2 — Names, and the evidence lines

5. `an account IAMAI could not name` renders four times in admin who-lines. Every
   holder resolves to a display name, then a sign-in name, then `a service principal
   (<app name>)`; the phrase never renders. Find the holder it was (likely the Breakglass
   account or a role assignment to a non-user) and add the fixture row.
6. The campaign step shows no evidence lines on GetIAMAI although Today shows two
   registered-but-unproven people. The unproven population line (`2 registered but
   unproven: <names> — one MFA sign-in each before <date>`) is scenario 12 and must
   fire wherever Today's tile is non-zero. Same for the no-method line. Add the
   GetIAMAI-shaped fixture rows so the property test proves it.
7. The what-changes line quotes the catalogue name (`applies "Security-info registration
   requires a trusted context" to 10 people`). The catalogue name does not render
   anywhere (§8.4); the line names the control in plain words: `A new policy blocks MFA
   registration from outside your trusted locations, report-only first.`
8. Portal steps still say `Exclude groups: your exclusions group — Setup question 2`.
   Setup no longer exists. The line names the assumption and its state: `Exclude groups:
   the exclusions group (created by the step above)` or the group's name once it
   exists.

## Part 3 — Do it for check steps

9. The emergency-access step's Do it is the old validation block: `finding → rule text
   (portal path)` pairs, `Add a second account above`, `could not be checked … no
   migration state`, and two former secondaries rendered as failed checks. That is the
   Setup block moved, not instructions. A check step's Do it is a numbered list of
   actions in the order to do them, each one imperative with its portal path, generated
   from the failing must-fix checks:
   1. Create a second emergency access account — Entra admin center → Users → New user
      (cloud-only, onmicrosoft.com, no licence, Global Administrator, permanent).
   2. Register a passkey or FIDO2 key for each — Users → the account → Authentication
      methods.
   3. Exclude both from every Conditional Access policy — Protection → Conditional
      Access → Policies: `Core - Allow - MFA for Guests`, `Grant - Defender for Cloud
      Apps Test` (the policies that currently include them, by name).
   4. Move Breakglass off the shared Authenticator device SM-S918U.
   "Worth fixing too" items become Done-when lines or More, never Do it. Migration-state
   and other could-not-run checks are Housekeeping only (46 item 21) and never render on
   a step. The two former secondaries (where the credential is kept; sign-in alert) are
   Done-when lines, tickable, stored in the plan file, never listed as failed checks.
   Apply the same generator to every check and create step (dormant accounts, named
   locations, the operator's passkey, TAP, SMTP relay, shared devices).
10. `Who this touches` on a step with nobody affected says `nobody affected`, matching
    its row, not `no people · 0 active`.
11. `If it goes wrong` does not repeat the what-changes line; for a check step it is
    `Nothing to undo. Recovery card →`.

## Part 4 — Presentation

12. Question chips carry their whole explanation as the label. The chip is label, value
    and verb only — `mail-sending devices none seen · answer` — and the explanation
    (`none seen in 30 days; answering adds an SMTP-relay step`) is the editor's first
    line. Weak-detection chips the same: `emergency access Breakglass · confirm`, the
    signals inside the editor as now. The reviewer has cut the contract's chip pattern
    to 60 characters to hold this.
13. `Enforced` rows sit in `Before anything else` with the date `now` — six policies
    that already exist, nothing to do. A step whose engine state is enforced or in place
    is not in a wave: it is in the footer's `Already in place`, with `Enforced ·
    verified on the next scan` as its status when verification is pending. Waves hold
    work only. The header's `7 in place` and the footer count agree after this.
14. The same binding reason repeats under twenty rows (`after: Sort out emergency access
    before anything else`). When every blocked row in a wave shares one reason, the wave
    header carries it once (`Wave 1 · Sep 8 → Sep 13 · Admin hardening · after emergency
    access`) and rows carry a second line only when theirs differs. The row's reason
    text uses the step's short form: `after: emergency access`.
15. Header line order and arithmetic: `31 steps · 7 in place · finishes Sep 20 · 3 weeks
    · 1 admin step waits for admin readiness · 3 device steps wait for device
    readiness`. Weeks derive from the finish date, not from the last blocked wave (it
    said `finishes Sep 20 … 4 weeks`).
16. Wave names: two waves are both `Advanced`. Name from the dominant family; when two
    families tie, name both (`Sessions and risk`); never the same name twice.
17. Signed out at `#/plan` renders a Plan page saying "Connect a tenant first". Signed
    out, Connect is the page (§2): redirect.
18. The editor's `Save` is the editor's one primary button.

## Finishing

`npm test && npm run smoke && npm run lint-mutations && npm run inventory && npm run
layout-audit`, `vite build`, one commit per part, push, confirm CI green. Report the
four parts; include the campaign step's full first-open text from the GetIAMAI-shaped
fixture and the emergency-access step's Do it, verbatim, so the reviewer can read them
before the walk.

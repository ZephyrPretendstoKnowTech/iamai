# 24 — Roadmap v2: rings, sequencing, scale, step content

Precondition: 23 committed. Read docs/design/roadmap-v2.md in full. This prompt implements sections 1 through 4 and 7. Prompt 25 implements 5 and 6.

Work in this order and commit after each numbered group.

## A. Fixtures first (§7)

1. Build `src/roadmap/fixtures/` with the eight synthetic tenants described in §7: micro, small, mid, large, huge, messy, midflight, hostile. Each is a `TenantSnapshot` plus mapping answers, generated by a seeded builder so they are deterministic and small in the repo (a generator function, not committed JSON). Document the builder in `docs/design/fixtures.md`.
2. Write the property assertions from §7 as tests that run over every fixture. These are the specification: they must fail against the current engine before the work below, and pass after. Record the initial failures in `docs/qa/roadmap-v2-baseline.md`.

## B. Rings (§1)

3. Add the Ring model to the plan types. Ring count, sizes, and soak days by band exactly as the table specifies.
4. Ring targeting proposes a group name in the tenant's convention and a suggested member list from readiness data: verified users first, one admin, never a break-glass account, spread across departments when `department` exists. At over 500 affected users, propose a filter in Entra's terms instead of a member list.
5. Rings apply only to steps that can deny access (grant requirements, device requirements, session controls, blocks). Prerequisites, report-only creation, and verification steps have one ring.
6. Entry and exit criteria per ring, with this tenant's numbers filled in.

## C. Sequencing (§2)

7. Build the dependency graph: hard dependencies as listed, soft dependencies as listed, calendar rules (no enforcement on Fridays, per-band weekly enforcement cap, an optional change-freeze range the user can set in Setup).
8. Every date derives from the graph plus band durations. No hard-coded wave dates.
9. Policy-count awareness: state the tenant's policy count, how many the plan adds, and warn as the total approaches the Entra cap, naming consolidation candidates from the housekeeping report.
10. Name collisions: a proposed name matching an existing policy gets a suffix and a note.
11. The Overview states the critical path in one sentence, naming the constraint that sets the length.

## D. Populations at scale (§3)

12. Implement the three population presentation modes (under 25, 25 to 500, over 500) with the basis sentence, the 10 riskiest by name, and CSV export in every mode.
13. Remove every place a name list can render unbounded.

## E. Step content (§4)

14. Restructure the step body to the twelve-part order in §4.
15. Add "What could go wrong" as a first-class section: per goal family, the real-world failure modes, each annotated with this tenant's evidence ("3 accounts sign in with IMAP4: they will break" / "no legacy-protocol sign-ins in 30 days: low risk").
16. Add "How to verify" with the exact portal location and, where applicable, the exact sign-in log filter to paste.
17. Rollback stores and shows the previous policy body for change steps.
18. Comms per goal and per ring, dated, plus a help-desk version ("what people will call about, and what to say").
19. Owner and scheduled date per step, editable, stored in the plan.

## F. Finishing

Run the property tests over all eight fixtures; every assertion in §7 must pass. Record before-and-after in `docs/qa/roadmap-v2-baseline.md`, including render timings for the huge fixture. Run npm test and vite build, commit, push. Report which assertions failed initially, what changed to fix them, and anything in roadmap-v2.md you disagree with.

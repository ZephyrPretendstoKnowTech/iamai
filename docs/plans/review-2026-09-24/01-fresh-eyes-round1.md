# Fresh-eyes round 1 (stopped by the owner, 2026-09-24)

These are the round 1 auditors' findings on the unpushed build (main 4e9fd544). The 3.x/4.x fixer (branch fix/fresh-b) fixed 25 of its issues. The 1.x/2.x/shared fixer (fix/fresh-a) was stopped mid-work; its work is saved as WIP. Neither branch is merged.

## Auditor 1
- **REGRESSION** Steps held on a Direction answer: 4.1 Block Legacy Authentication (demo, small, mid), Require MFA for Guests (small, mid); the same root gives 4.2 on demo and Require MFA for Inforcer Access 'After …' (1.x #3 and template rule 2 (headline in words, never a lane word), on the On Hold steps of 2.x #18) · src/ui/surfaces/stepBody.ts:726 `waitWords = … laneView.waitingFor` (fix/s4 commit 4f6c3987, not the merge): "Rail reads NEXT MILESTONE / "Waiting on your answers", the row's own label, which also sits in the bar and on the tile "Confirm What You Use · Waiting on your answers". I saw this on screen for small, mid and demo 4.1 and for small and mid guests. In the 27 drifted snapshots it appears 13 times, and" → Delete waitWords, so a headline never takes laneView.waitingFor. If the step's only open item is a Direction wait, the headline reads "Approve your answers in {step}" (new key pages.app.plan.direction.railWaiting, {step} = directionTitleOf). If it waits on another step, the headline reads "Finish {s
- **CONTENT** 2.1 Confirm What You Use (#37 About (after 4.x #5(b), which changed #31)) · docs/design/content.json pages.app.plan.direction.steps.use.why: ""IAMAI filled these in from your tenant. Approve them or correct any that are wrong: a service you don't use takes its policies off your plan, and the accounts and partners you name are kept out of the policies that would stop them." This is no longer true for accounts. Under 4.x #5(b) no pinned pol" → "IAMAI filled these in from your tenant. Approve them or correct any that are wrong: a service you don't use takes its policies off your plan, and the partners you name are kept out of the policies that would stop them."
- **CONTENT** 2.3 Decide How and Where People Sign In (#59 with #61 (Office network evidence while the saved answer is Everyone works remotely)) · src/roadmap/direction.ts officeNetworkQuestion evidence (lines 313-318): ""1 named location is marked trusted: Head office. Head office is marked trusted in Entra; this answer doesn't use it as your office." The same fact is stated twice. It shows on the first scan of small, mid and getiamai and on demo-week2's week-two view." → When the savedRemoteUnused line shows, leave out the seen line. The card then reads "Head office is marked trusted in Entra; this answer doesn't use it as your office."
- **CONTENT** 2.3 Decide How and Where People Sign In (#61 wording with more than one trusted location) · docs/design/content.json pages.app.plan.direction.questions.officeNetwork.savedRemoteUnused: "With two trusted locations, fillText gives "Head office, Branch is marked trusted in Entra; this answer doesn't use it as your office."" → Add savedRemoteUnusedMany: "{names} are marked trusted in Entra; this answer doesn't use them as your office." Use it when more than one trusted location is named. Keep the singular line for one.
- **NOT-LANDED** 2.1 Confirm What You Use (demo-week2, the public demo's week-two view) (#32 The mail picker leaves out the emergency access accounts and guests) · src/roadmap/fixtures/index.ts:312, where the demo's printer is picked from all users: "MFP Reception (mfp-reception@demo.example.com) has userType guest. demo-week2's saved mail answer still picks it, so the card shows it as an Approved chip even though the picker can no longer offer it. It is also in Identify Service and Shared Accounts' service-account chips, in 4.1's Completion Cri" → Pick the printer from member accounts only: `users.find((u) => u.userType !== 'guest' && u.lastSuccessfulSignIn !== null && …)`. demo-week2's saved mail and service-account answers then name a pickable member. Regenerate the demo snapshots.

## Auditor 2
- **REGRESSION** 4.1 Block Legacy Authentication (s-goal-block-legacy-auth), demo. Round-1 lead file docs/qa/step-snapshots/demo/s-goal-block-legacy-auth.json (4.x #33 (show who uses legacy authentication)) · src/roadmap/blockSignIns.ts signInsCard: the `if (on) return null` branch that fix/s4 commit e70c7f9f added (settleBlockSignIns now passes enforced(goalId)). The merge did not cause this: in-memory snapshots from fix/s4's own head (89fe84a5) match fix/fresh byte for byte.: "On demo the policy is already On but does not match the plan, and Tasks Remaining shows only "Conditional Access policy · Core - Block - Legacy authentication · On · Configure Emergency Exclusions adds Core - Exclusions to it." plus three prerequisite cards. The card "Legacy sign-ins · 3 accounts si" → Keep the card while the policy is On and drop only the turn-on clause. Give legacyMove (and deviceCodeMove) an On variant, as the mail card already has with moveMany and moveBeforeMany: "Legacy sign-ins · 3 accounts signed in with legacy authentication in the last 30 days · Move Jamie Singh, Morgan 
- **REGRESSION** 4.1 Block Legacy Authentication on demo, small, mid, large, midflight and hostile; Require MFA for Guests on small, mid, large, midflight, hostile and demo-week2 (frozen); Block Device Code Sign-in on demo and Require MFA for Inforcer Access on 5 fixtures show the "After …" form (Template rule 2 and 1.2–1.4 #3 (the milestone headline is in words, never a date or a lane word); 4.x #18 (the rail names the next task)) · src/ui/surfaces/stepBody.ts waitWords (about lines 721–727, added by fix/s4 commit 4f6c3987): a policy step with no task of its own left puts laneView.waitingFor, the row's hold label, into the rail headline.: "The rail reads "NEXT MILESTONE · Waiting on your answers". That is the row label for a policy held on a Direction answer (content.json direction.waiting). It names neither the question nor the step, and on demo 4.1 it is one of three waits. Before 4f6c3987 the rail read "Correct the policy" (demo) o" → Where the only wait is a Direction answer, the headline names the answer in the words 3.6 already uses for its held What to do (content.json line 4054): "Answer Mail-sending devices in Confirm What You Use." Never draw BOARD waiting or the "After …" row label as the milestone. Where the wait is anot
- **REGRESSION** 4.4 Require MFA for Everyone (s-goal-mfa-all-users), demo. Round-1 lead file docs/qa/step-snapshots/demo/s-goal-mfa-all-users.json (4.x #10 (dormant accounts and the 90% gate) and #2 (the readiness fact of an enforced policy is stated as a fact)) · fix/s4 commit 4f6c3987 dropped the Threshold card on a policy already On, but not the Dormant Accounts card that exists only for that gate. Seen in the Tasks Remaining subjects of policyTasks.ts.: "demo 4.4 (policy Enforced, Up Next · Correct the policy) shows "Dormant Accounts · 1 account hasn't signed in for 90 days and has no method · Disable Kai Nguyen (user30@demo.example.com) in Disable or Confirm Dormant Accounts." as an open card. There is no Threshold card, and "21 of 30 people have a" → Draw the Dormant Accounts card only where the Threshold card is drawn. Its job is 3.1's work, and here it holds nothing. On a policy already On, state the reading as 4.x #2 does on Completed steps: Satisfied "Readiness · 21 of 30 people have a method it accepts".
- **CONTENT** 3.4 Prepare Your Team for MFA (s-verify-mfa) (#51 Completion Criteria) · docs/design/content.json steps[s-verify-mfa].doneWhen[1]: ""Every admin has a passkey or security key registered." The step reads Completed when an admin without one is on Turn On Without Them. Tested: on demo with everyone not ready marked, Quinn Ivanova (admin, no passkey) and the step is Completed; the same on getiamai with Kai Brown. So the second line " → "Every admin has a passkey or security key registered, or is on the Turn On Without Them list."
- **CONTENT** 3.4 Prepare Your Team for MFA (s-verify-mfa) (#48 task line (Temporary Access Pass) and #47 card) · docs/design/content.json steps[s-verify-mfa].whatToDo.steps[2]; ui/surfaces/prepareSteps.ts teamVars (it reads no Temporary Access Pass state): ""If they can't sign in to register: open [Microsoft Entra admin center](https://entra.microsoft.com/) → Entra ID → Users → the person → Authentication methods → Add authentication method → Temporary Access Pass." On a tenant where the scan reads Temporary Access Pass off (every fixture; 3.2 on the s" → When the scan reads Temporary Access Pass off, put 3.2's existing line (steps[s-check-separate-admin-accounts].procedure.tapPolicy) before this one: "Open **Entra ID → Authentication methods → Policies → Temporary Access Pass**, select **Enable**, include **All users**, and select **Save**." Read it
- **CONTENT** 3.3 Register Your Own Passkey (s-ladder-operator-passkey) (#14 and #41 Satisfied fact, with the owner's item-5 change (Windows Hello counts)) · ui/surfaces/prepareSteps.ts operatorReading; content.json steps[s-ladder-operator-passkey].card.signedIn and signedInOn (line 4661): "When the operator signs in with Windows Hello only, the card reads "Your passkey · Sam Patel (user0@small.example.com) · Signed in with it on your computer, Aug 28, 2026." There is no passkey, so "it" says something IAMAI did not see." → Name the method IAMAI saw. For a Windows Hello proof: "Signed in with Windows Hello on your computer, Aug 28, 2026." For a passkey: "Signed in with your passkey on your computer, Aug 28, 2026." This adds a content key; say so in the commit.
- **CONTENT** 3.3 Register Your Own Passkey (s-ladder-operator-passkey) (#42 task line and #44 AI Focus, after the owner's item-5 change (no every-kind-of-device requirement)) · docs/design/content.json line 4622 (procedure) and line 4654 (aiFocus): ""Sign out, then sign in with the passkey on each device you use: your computer and your phone." The step reads Completed after one sign-in on one device (tested: small and getiamai, computer only). So the task asks for more than the step completes on. The AI Focus says "…signing in with it on each d" → "Sign out, then sign in with the passkey." AI Focus: "Walk me through creating a passkey in Microsoft Authenticator, or registering an approved security key, and signing in with it." The owner rules, because #42's words were approved before the item-5 change.
- **CONTENT** 3.4 Prepare Your Team for MFA (s-verify-mfa) (#50 Today line with #4 campaign line) · docs/design/content.json steps[s-verify-mfa].whatToDo.steps[6] and [7]; prepareSteps.ts campaignToday: ""Today: the registration campaign is on." and then "Turn on the registration campaign: open … State: Enabled. …", on every fixture. A real tenant reads "on, nudging Microsoft Authenticator to all users" and is still told to "Turn on" something already on. The bare "on" comes only from a fixture shap" → Line 8: "Set the registration campaign: open [Microsoft Entra admin center](https://entra.microsoft.com/) → **Entra ID → Authentication methods → Registration campaign → Edit**. State: Enabled. …". Delete the bare W.on branch, which no real tenant reaches.
- **CONTENT** 3.4 Prepare Your Team for MFA (s-verify-mfa) (#48 admins line) · docs/design/content.json steps[s-verify-mfa].whatToDo.steps[4]: ""Admins: register a passkey or a hardware security key with Quinn Ivanova (user1@demo.example.com)." "with {names}" reads as registering together with, or using, that person." → "Admins who still need a passkey or a hardware security key: Quinn Ivanova (user1@demo.example.com)."
- **CONTENT** 3.4 Prepare Your Team for MFA (s-verify-mfa) (#47 Turn On Without Them card) · docs/design/content.json lines 2141–2142 (campaign, campaignOpen): "With one person: "The policies go ahead without Kai Brown (user0@getiamai.example.com). Give each a Temporary Access Pass when they can register."" → "… Give them a Temporary Access Pass when they can register." This reads right for one person or many.
- **TEMPLATE** 3.4 Prepare Your Team for MFA (s-verify-mfa) (Template rule 2 (headline reads Completed once done; instruction once, where the step takes a choice) and #17) · ui/surfaces/prepareSteps.ts teamReading: the instruction shows while `team.missing.length > 0 || team.marked.length > 0`: "On a Completed 3.4 (demo, everyone not ready marked) the rail reads "NEXT MILESTONE · Completed" and then "Pick anyone not ready who can't register soon, such as someone on leave: the policies go ahead without them.", with nobody left to pick. 3.1 drops its instruction once no account is open." → Show the instruction only while someone not ready is still unmarked, as 3.1 does. Keep the picker so a mark can be undone.
- **TEMPLATE** 3.2 Use Separate Accounts for Admin Work, and 3.5 Create the Baseline's Authentication Strength (Uniformity (the same kind of line is phrased the same way) against #31, #57 and 4.x #51/#57's approved form "As at least a …, open …") · content.json steps[s-check-separate-admin-accounts].procedure.create; docs/implementation-content/s-prereq-auth-strength/s-prereq-auth-strength/CONTENT.md lines 2 and 19: "3.2: "Open [Microsoft Entra admin center](https://entra.microsoft.com/) → **Entra ID → Users → New user → Create new user**, as at least a User Administrator." 3.5: "Open [Microsoft Entra admin center](https://entra.microsoft.com/) → **Entra ID → Authentication methods → Authentication strengths** a" → 3.2: "As at least a User Administrator, open [Microsoft Entra admin center](https://entra.microsoft.com/) → **Entra ID → Users → New user → Create new user**." 3.5: "As a Security Administrator, open [Microsoft Entra admin center](https://entra.microsoft.com/) → **Entra ID → Authentication methods →
- **TEMPLATE** 3.3, 3.5 and 3.6 procedures (Uniformity: values bold, as in 1.1, 3.2, 3.7, 4.x and content.json shared "name": "Name: **{name}**.") · content.json line 4620; s-prereq-auth-strength CONTENT.md lines 4, 5 and 21; s-prereq-trusted-location CONTENT.md line 3: ""… sign in as user0@demo.example.com and answer the prompt there." / "Name: Modern MFA + TAP." / "Select exactly these methods: Windows Hello for Business, Passkeys (FIDO2), Certificate-based authentication (multifactor) and Temporary Access Pass (one-time use)." / "Name: Core - Trusted - Head offic" → "… sign in as **user0@demo.example.com** and answer the prompt there." / "Name: **Modern MFA + TAP**." / "Select exactly these methods: **Windows Hello for Business**, **Passkeys (FIDO2)**, **Certificate-based authentication (multifactor)** and **Temporary Access Pass (one-time use)**." / "Name: **C

## Auditor 3
- **REGRESSION** 4.1 Block Legacy Authentication (4.x #4 (mail half completes from sign-in data) and #2 (a Completed step shows no open cards)) · small, after Approve on Confirm What You Use (Noor Nguyen picked as mail sender) and before Configure Passkey Authentication completes (probe j2/small-01-direction.txt). Root cause: src/actionability/lanes.ts:271 isComplete treats any enforced, undrifted policy as complete and ignores step.state.satisfied=false, which settleBlockSignIns set because mailAccountsToMove is non-empty. The same state is Completed on fix/s4 but read 'Ready · Review' on pre-merge main b8bb3436.: "Badge 'Completed', with open cards 'Mail accounts · Noor Nguyen still signs in with legacy authentication · Move it to a supported mail route.' and 'Configure Passkey Authentication · Prerequisite · To do'. The Satisfied card reads 'Conditional Access policy · On · Blocks Exchange ActiveSync and Oth" → Stop isComplete from completing a policy step whose own state is unsatisfied (a named mail account still to move). 4.1 then reads 'Ready' with rail headline 'Move each mail account to a supported mail route', as it does one scan later.
- **REGRESSION** 4.1, 4.2 (the same code also reaches 4.4 and the frozen guests and Inforcer steps) (1.2–1.4 #3 (the milestone headline is words, never a date or lane word) and 4.x #18 (the rail names the next task)) · Rail headline of a policy step with no open task of its own that is still waiting: stepBody.ts waitWords, commit 89fe84a5. demo 4.1 and 4.2; small and mid 4.1 on first scan; owner-like 4.4 after its exclusion is removed. In the 27 drifted snapshots: 7 files for 4.1, demo 4.2, 6 guests-mfa and 5 inforcer-mfa.: "NEXT MILESTONE 'Waiting on your answers' (demo, small and mid 4.1), 'After Prepare Emergency Access Accounts' (demo 4.2), 'After Configure Emergency Exclusions' (4.4). This repeats the row sub-line and the card state word for word." → Name what is left, as an action in words, the way 3.6 does while it is held (headline 'Create the trusted network', bar 'Answer Office network in …'). demo 4.1 and 4.2: 'Add Core - Exclusions in Configure Emergency Exclusions'. small and mid 4.1: 'Approve Confirm What You Use'. owner-like 4.4: 'Add 
- **REGRESSION** 4.1 Block Legacy Authentication (4.2 uses the same rule) (4.x #33 (show who uses legacy authentication)) · src/roadmap/blockSignIns.ts signInsCard returns null once the policy is On. This came from fix/s4 commits 4f6c3987 and e70c7f9f, made after that branch last regenerated its snapshots (f9f6b1ec), not from the merge. Seen on demo (the policy is On but not Completed: its exclusion is missing and 2.1 is unanswered) and on small.: "No card on screen names who uses legacy authentication. Only AI Info says '3 accounts signed in with legacy authentication in the last 30 days: Jamie Singh, Morgan Wilson, svc-mailer-1' (demo). On small, Jamie Singh, Noor Nguyen and Kai Ivanova are unnamed. On f9f6b1ec the card read 'Legacy sign-ins" → Keep the open card while the step is not Completed, whatever the policy's state. With the policy On, drop 'before you turn this on': 'Legacy sign-ins · 3 accounts signed in with legacy authentication in the last 30 days · Move Jamie Singh, Morgan Wilson and svc-mailer-1 to a modern mail app.' Apply 
- **REGRESSION** 4.4 Require MFA for Everyone (4.x #2 (readiness under Satisfied) and #48 (threshold words); one of the 27 drifts (demo/s-goal-mfa-all-users)) · demo, where the policy is On but needs a correction (Up Next · After Prepare Emergency Access Accounts): "The Threshold card ('At least 70% MFA-ready' on f9f6b1ec) is gone and no readiness appears on screen. Only the hidden dialog and AI Info say 'Readiness: 21 of 30 people have a method it accepts. 1 registered only a phone for texts or calls…' and '7 people have no method and will be stopped until the" → Show the item 2 card whenever the policy is On, not only when the step is Completed: 'Readiness · 21 of 30 people have a method it accepts'.
- **CONTENT** 4.4 Require MFA for Everyone (4.x #26 (Completion Criteria: '… for {Include} except Core - Exclusions.'); CLAUDE.md rule that exclusions go through the group) · demo, all states until corrected (the tenant's policy also excludes Break-glass 1 directly): "'IAMAI sees Core - Grant - MFA for all users On, requiring MFA for All users except Core - Exclusions and Break-glass 1.'" → 'IAMAI sees Core - Grant - MFA for all users On, requiring MFA for All users except Core - Exclusions.'
- **CONTENT** 4.5 Turn Off Security Defaults with 4.1–4.4 (4.x #28 (row dates while held) and #8 (4.5 turns the four policies on)) · Owner-like tenant with security defaults on, after the report-only week (probe owner-sd2/04-week.txt): "The rows for 4.1–4.4 read 'Up Next · After Turn Off Security Defaults · Est. Sep 15, 2026', while 4.5 reads 'Ready · Est. Sep 7, 2026'. 4.1's own card says 'Turn Off Security Defaults turns security defaults off and this policy on together.'" → Give the four rows 4.5's day, 'Est. Sep 7, 2026', because the wait clears and the turn-on happens in the same change.
- **CONTENT** 4.5 Turn Off Security Defaults, 4.6 Finish Moving Off Per-User MFA (4.x #51 with #8, #57, and the shared first and last lines (4.x #16)) · Implementation Tasks, Entra tab, in every state: "4.5: '1. As at least a Conditional Access Administrator, open [Microsoft Entra admin center](https://entra.microsoft.com/) → Entra ID → Overview → Properties → Manage security defaults.' / '2. Select Disabled (not recommended), then Save.' / '3. Go to Entra ID → Conditional Access → Policies.' / '4." → 4.5: '…→ **Entra ID → Overview → Properties → Manage security defaults**.' / 'Select **Disabled (not recommended)**, then **Save**.' / delete the 'Go to' line / 'Open **Entra ID → Conditional Access → Policies → CA - Block - Legacy authentication**, set **Enable policy** to **On** and select **Save*
- **CONTENT** 4.1–4.4 (AI Info tab) (4.x #31 (the lead states the action with its values), #20 and #24 (deleted card words), 2.x #18 (the 'Answer it in' note deleted), 3.x #11 (Existing coverage deleted on every step)) · AI Info 'What to do', 'Fix before continuing' and 'Observed' lines on demo, small, getiamai and the owner-like tenant: "'What to do: After Prepare Emergency Access Accounts.' / 'What to do: Waiting on your answers.' / 'Fix before continuing: After Prepare Emergency Access Accounts. | Answer it in Confirm What You Use.' / 'What to do: Create the policy in report-only on Aug 31, 2026 (estimated); turning it on waits fo" → Make 'What to do' the rail's next task with its values (for example 'What to do: Correct the policy: under Users → Exclude add Core - Exclusions; under Target resources → Exclude add Microsoft Intune Enrollment.' or 'What to do: Set CA - Block - Legacy authentication to Report-only.'). Delete the 'F
- **CONTENT** 4.1–4.4 turn-on task while held (4.x #17 and #44 ('Wait for {step}; leave this policy in Report-only until then.')) · Owner-like first scan, 4.1: "'1. Wait for Confirm What You Use, Prepare Emergency Access Accounts and Verify Emergency Access; leave this policy in Report-only until then.' A Direction answer is the person's own action, not something to wait for." → '1. Approve Confirm What You Use, then wait for Prepare Emergency Access Accounts and Verify Emergency Access; leave this policy in Report-only until then.'
- **CONTENT** 2.3 Decide How and Where People Sign In (2.x #59 and #61 (office evidence)) · Office network card answered 'Everyone works remotely' while a trusted location exists: mid, small, getiamai, demo-week2, owner-like (53 states): "'1 named location is marked trusted: Head office. Head office is marked trusted in Entra; this answer doesn't use it as your office.'" → While #61's line shows, drop #59's line: 'Head office is marked trusted in Entra; this answer doesn't use it as your office.'
- **CONTENT** 3.1 Disable or Confirm Dormant Accounts (3.x #14 (Satisfied cards state their fact)) · Owner-like tenant: Completed on first scan with no dormant account ever listed: "'Dormant accounts · 0 kept · 0 disabled'" → 'Dormant accounts · No enabled account has gone 90 days without a sign-in.' Keep '{n} kept · {m} disabled' for a list the person worked through.
- **CONTENT** 4.6 Finish Moving Off Per-User MFA (1.1 #2 (Completed rows keep their Impact; planBoard.ts drawsImpact: 'finishing the work does not change what it touched')) · Owner-like tenant with two accounts on per-user MFA, after both are disabled (owner-sd2/07-per-user-off.txt): "The row Impact goes from '2 accounts' while open to 'no accounts' once Completed." → The Completed row keeps '2 accounts'.
- **TEMPLATE** 1.3 Configure Passkey Authentication (Template rule 6 and 4.x #2 (a Completed step shows no open cards)) · demo, small, mid, getiamai and owner-like after key restrictions are enforced while unapproved passkeys are still registered (j2/demo-02-foundations.txt): "Badge 'Completed', headline 'Completed', with an open card 'Existing passkeys affected · 5 accounts to prepare · Follow Prepare affected passkeys in Implementation Tasks.'" → While a registered passkey would be refused, 1.3 reads 'Ready · Correct' with headline 'Prepare affected passkeys'. It reads Completed only once no account is affected.
- **CONTENT** 1.3 Configure Passkey Authentication (AI Info) (Banned-string sweep ('does not prove') and template rule 12 (never ask the person to record what IAMAI reads)) · AI Info tab, 'Emergency passkey compatibility', every fixture: "'Break-glass 1: Targeting and the global model restriction allow at least one registered key. This does not prove recovery works; perform and record a controlled sign-in test.'" → Delete the second sentence. Verify Emergency Access reads the test from the sign-in.
- **CONTENT** 1.1 Prepare Emergency Access Accounts (Banned-string sweep ('cannot') and template rule 7 (no troubleshooting lines inside the procedure). The owner verified 1.1, so this needs the owner's call.) · Set up an approved passkey task and each of its variants, every fixture: "'7. Cannot complete registration sign-in? Use **Troubleshooting → Temporary Access Pass**.'" → Delete the line. The Temporary Access Pass scenario stays in Troubleshooting.
- **NOT-LANDED** 4.1–4.4 (every policy create) (4.x #16 ('Add "Description: **{tag}**." after the name')) · Create the policy in Report-only. It was dropped on purpose in commit 840e8f68: 'the Entra form has no Description field; the tag travels in the JSON and the script'.: "'2. Name: **CA - Block - Legacy authentication**.' is followed directly by '3. Under **Users**, include **All users** and exclude the group **Breakglass Exclusion**.' There is no Description line." → Owner to decide. Either confirm the drop (the Entra Conditional Access form has no Description field, and Graph documents the property as not used), or add 'Description: **[IAMAI:…]**.' after the Name line as approved.
- **TEMPLATE** 2.1, 2.2, 2.3 (and 4.1–4.4 for the owner to judge) (npm run template:check deviations on 1.1–4.6 (template rules 2 and 4)) · Template check on the demo Initial and Follow-up scans at 1440px (fresh-r1-cross/template.txt): "'2.1 control in main column | Questions: select ×6; Questions: picker'; '2.2 … select ×2; picker ×2'; '2.3 … select ×3; picker'. Owner to judge: '4.1–4.4 footer | beside the Scan: "Defer this step"' on the Initial scan, and '3.4 tabs | Email' (3.x #8 already approved that tab). There are no other de" → Answer controls inside a decision step's Questions cards are the Questions (Approve is already in the rail, per 2.x #8). Amend template rule 2, or make template-check exempt them, rather than moving the controls. Owner to decide whether 'Defer this step' stays beside Scan on open 4.1–4.4, as it was 
- **TEMPLATE** 3.7 Create or Correct Service Accounts Group (3.x #7 (a group picker in 3.7's rail 'like 1.2's Exclusions group') and template rule 2) · demo: first scan (no group exists yet) and after Save (Completed): "The picker and its instruction 'Select the group that holds your service accounts.' appear only while a matching unsaved group exists. They are absent on first scan, although task line 8 says 'Under **Service accounts group**, select **Core - Exception - Service accounts**, then **Save**.', and abse" → Draw 3.7's 'Service accounts group' picker and its instruction in the rail in every state, as 1.2 does.
- **CONTENT** 4.4 Require MFA for Everyone vs 3.1 (4.x #10 (Dormant Accounts card); uniformity of card labels) · 4.4 open card on demo, getiamai and owner-full: "The 4.4 card label is 'Dormant Accounts'. 3.1's card label is 'Dormant accounts', and every other card label is sentence case." → 'Dormant accounts'
- **CONTENT** 3.3 Register Your Own Passkey (3.x #43 (criterion) and template rule 10 (the same in every state)) · Completion Criteria in the open state (getiamai) and the Completed state (demo, owner-like): "Open: 'IAMAI sees Kai Brown (user0@getiamai.example.com) sign in with a passkey or Windows Hello.' Completed: 'IAMAI sees Casey Kim sign in with a passkey or Windows Hello.' The account name is dropped." → Use one form in every state, with name and UPN: 'IAMAI sees Kai Brown (user0@getiamai.example.com) sign in with a passkey or Windows Hello.'

## Auditor 4
- **REGRESSION** 4.1 s-goal-block-legacy-auth (4.x #33 (show who uses legacy authentication)) · demo, small and demo-week2 (any tenant whose legacy policy is already On), every state. Cause: fix/s4 commit e70c7f9f, src/roadmap/blockSignIns.ts signInsCard returns null when the policy is On. The merge did not cause it: the fix/s4 HEAD render is identical.: "On demo, Tasks Remaining holds only "Core - Block - Legacy authentication · On · Configure Emergency Exclusions adds Core - Exclusions to it." plus the prerequisite and "Confirm What You Use · Waiting on your answers" cards. There is no Legacy sign-ins card, but AI Info still says "3 accounts signed" → Keep the card when the policy is On, and drop only the words "before you turn this on": "Legacy sign-ins · 3 accounts signed in with legacy authentication in the last 30 days · Move Jamie Singh, Morgan Wilson and svc-mailer-1 to a modern mail app." Once the step is Completed, put it under Satisfied 
- **NOT-LANDED** 4.3 s-goal-admins-phishing-resistant (4.x #7 (a change that brings a policy to its own target never raises Review required) and #24 (one card per problem)) · demo: the person makes 1.2's edit (adds Core - Exclusions to the Report-only Core - Grant - Admins phishing-resistant), then scans. This is the scan after the change; it clears only on the scan after that.: "Badge "Ready · Correct", but no correction task exists. Two cards: "New evidence · Review required · the policy itself changed by Aug 30, 2026: what was watched before this is no longer what is deployed" and "Review · Review required · Core - Grant - Admins phishing-resistant is no longer the policy" → When a change brings the policy to its target, restart the report-only week with no Review card. Show what the next scan already shows: row "Up Next · Report-only until Sep 6, 2026" and card "Core - Grant - Admins phishing-resistant · Report-only until Sep 6, 2026". Delete both Review cards and that
- **CONTENT** 4.1, 4.2, 4.3 (policy already in the tenant) (4.x #18 (same procedure in every state) against intent Q1) · demo 4.1, 4.2, 4.3; mid 4.1; demo-week2 4.3; the stricter-4.2 variant. Implementation Tasks open on Create by default.: "demo 4.2 (policy On): "Create the policy in Report-only … Name: Core - Block - Device code flow." That is the name of the policy already On, so following it makes a second one. demo 4.3: "Name: Core - Grant - Admins phishing-resistant." with 46 directory roles and "Require authentication strength → " → Where the tenant already has the step's policy, both tasks describe that one policy: its name and the settings the Completion Criteria reads. demo 4.3 would read "Name: Core - Grant - Admins phishing-resistant." / "Under Users, exclude the groups Core - Exclusions and Core - Break glass, and include
- **REGRESSION** 4.1 s-goal-block-legacy-auth (Template rule 2 / 1.2–1.4 #3 (the milestone is words, never a lane word); lead's rail finding) · First scan on demo, small, mid and messy-style holds. Snapshots: demo, demo-week2, hostile, large, mid, midflight, small (4.1), and guests-mfa (frozen) on six fixtures. getiamai AI Info reads "What to do: Waiting on your answers." Source: stepBody.ts waitWords reads laneView.waitingFor.: "Next milestone: "Waiting on your answers"" → Next milestone: "After Confirm What You Use". This is the form the other held steps use ("After Prepare Emergency Access Accounts"). A Direction wait names its Direction step, and AI Info's What to do reads the same words.
- **REGRESSION** 4.4 s-goal-mfa-all-users (4.x #48 (Threshold detail) and #2 (readiness of an enforced policy is stated)) · demo first scan: Core - Grant - MFA for all users is already On and needs a correction. The snapshot drift is "Threshold · At least 70% MFA-ready" removed. Cause: fix/s4 commits after f9f6b1ec, not the merge.: "Tasks Remaining shows the policy card, "Dormant Accounts · 1 account…" and two prerequisite cards, and no readiness card. The fact survives only in the hidden Why dialog and in AI Info: "Readiness: 21 of 30 people have a method it accepts. 1 registered only a phone for texts or calls…" and "7 people" → Show it on screen as item 2's fact while the policy is On: "Readiness · 21 of 30 people have a method it accepts · 1 registered only a phone for texts or calls, which your Authentication methods policy turns off. Prepare Your Team for MFA gets them ready." Once Completed, put it under Satisfied, as 
- **REGRESSION** 4.5 s-prereq-security-defaults, 4.6 s-prereq-per-user-mfa (4.x #31 (AI Info) and uniformity with 4.1–4.4. Caused by the merge: section 3's 1899b745 (AI Info keeps a finished step's procedure) now reaches 4.5/4.6) · AI Info on a Completed 4.5 and a Completed 4.6 (small with security defaults and per-user MFA on, after both are turned off). fix/s4 read "What to do: Nothing left to do.": ""What remains: Completed / What to do: Turn security defaults off in the same change that turns on the policies that replace them. | As at least a Conditional Access Administrator, open …". 4.6 reads "What to do: Turn off per-user MFA for each account once Require MFA for Everyone is On. | …"." → "What to do: Nothing left to do: security defaults are off." and "What to do: Nothing left to do: per-user MFA is off for every account." This is the form Completed 4.1–4.4 use ("Nothing left to do: {policy} is On.").
- **CONTENT** 4.4 s-goal-mfa-all-users (4.x #15 (correction) and #26 (Completion Criteria) against intent Q2) · demo: the correction adds two exclusions, and the step completes after only one of them.: "The Correct task says "Under Users → Exclude, add the group Core - Exclusions." / "Under Target resources → Exclude, add Microsoft Intune Enrollment." Completion Criteria reads "IAMAI sees Core - Grant - MFA for all users On, requiring MFA for All users except Core - Exclusions and Break-glass 1." A" → Make completion and the criterion read the line the task asks for: "IAMAI sees Core - Grant - MFA for all users On, requiring MFA for All users except Core - Exclusions and Break-glass 1, across All resources except Microsoft Intune Enrollment." Otherwise delete the Intune line from the correction.
- **NOT-LANDED** 4.1 s-goal-block-legacy-auth (4.x #18 (Create and Turn the policy on stay in every state; the rail names the next task)) · messy, first scan (snapshot rail "Block Legacy Authentication"). This is a tenant policy that delivers the goal while excluding 116 of 122 people.: "Task "Block Legacy Authentication": "Open Entra admin center → Entra ID → Conditional Access → Policies." / "Review the policies that affect Block Legacy Authentication: their assignments, conditions, access controls and current state." Next milestone: "Block Legacy Authentication"." → Draw the same two tasks as on every other fixture ("Create the policy in Report-only", "Turn the policy on") and delete the review task. The rail headline becomes "After Confirm What You Use".
- **CONTENT** 4.1 s-goal-block-legacy-auth (4.x #22 (Satisfied cards state their fact)) · small after 2.1 names Noor Nguyen; demo after 2.1 names svc-mailer-1. In both, a delivered 4.1 is reopened by the mail half.: "Satisfied: "Conditional Access policy · On · Blocks Exchange ActiveSync and Other clients for All users except Core - Exclusions". The policy name is gone; on the first scan it read "… · Core - Block - Legacy authentication · On · …"." → "Conditional Access policy · Core - Block - Legacy authentication · On · Blocks Exchange ActiveSync and Other clients for All users except Core - Exclusions"
- **CONTENT** 4.1 s-goal-block-legacy-auth (4.x #36 (mail task names the accounts)) · small and demo after 2.1 names a mail account: ""Mail accounts · Noor Nguyen still signs in with legacy authentication · Move it to a supported mail route." Here "it" refers to a named account." → "Mail accounts · Noor Nguyen still signs in with legacy authentication · Move the device or app that signs in as Noor Nguyen to a supported mail route."
- **CONTENT** 4.3 s-goal-admins-phishing-resistant (4.x #17/#44 (held turn-on says what it waits for) and #27 ("Baseline safety conflict" retired)) · getiamai: the plan's own 4.3 policy lost Core - Exclusions and its grant changed. Seen in the turn-on task and the Entra tab.: ""Wait for Baseline safety conflict, Configure Emergency Exclusions and Verify Emergency Access; leave this policy in Report-only until then."" → "Wait for Configure Emergency Exclusions and Verify Emergency Access; leave this policy in Report-only until then."
- **CONTENT** 4.3 s-goal-admins-phishing-resistant (4.x #24 and #31 (AI Info)) · AI Info in the same state (plan policy lost Core - Exclusions): ""What to do: This policy would apply to Fixture getiamai's emergency access accounts. IAMAI writes nothing until the exclusions group carves them out: fix the exclusions-group step, then scan again." Also "Excluded groups: None." and "Focus for this step: Walk me through turning this policy on after" → "What to do: Configure Emergency Exclusions adds Core - Exclusions back under Users → Exclude; then correct the grant." / "Excluded groups: Core - Exclusions (000f4435-…)" / "Focus for this step: Walk me through correcting this policy."
- **CONTENT** 4.1 s-goal-block-legacy-auth, 4.2 s-goal-block-device-code (4.x #31 (AI Info lead states the action with its values; no fluff)) · AI Info in report-only and ready-to-enforce, on getiamai, where nobody used either flow and report-only blocked no one: ""Move any mail client, printer, scanner or job it would have blocked to modern authentication before it turns on." / "Move any tool or device it would have blocked to another sign-in method before it turns on."" → Delete both lines whenever report-only blocked no one. Where it did block someone, item 35's named line already says who to move.
- **NOT-LANDED** 4.6 s-prereq-per-user-mfa (4.x #30 (delete Troubleshooting, as 3.x #21) and template rule 7) · Completed 4.6 (package state inPlace), small with per-user MFA turned off: ""Microsoft Learn · Troubleshooting". The dialog holds three scenarios, among them "A user still reports Enabled or Enforced after the change." ("Read /users/{id}/authentication/requirements on Graph beta…"), "The beta authentication requirements call changes or fails…" and "A user can sign in withou" → Delete the troubleshooting.model block from docs/implementation-content/s-prereq-per-user-mfa/…/CONTENT.md, or draw no scenarios on any section 4 step (stepBody.ts spares only cs.kind 'policy').
- **TEMPLATE** 4.5 s-prereq-security-defaults, 4.6 s-prereq-per-user-mfa (4.x #8, #51, #57 (approved words carry bold) and uniformity with 4.1–4.4) · Implementation Tasks and the Entra tab, in every state: "Plain text throughout: "As at least a Conditional Access Administrator, open Microsoft Entra admin center → Entra ID → Overview → Properties → Manage security defaults." / "Select Disabled (not recommended), then Save." / "Go to Entra ID → Conditional Access → Policies." / "Open Core - Block - Legac" → "…→ **Entra ID → Overview → Properties → Manage security defaults**." / "Select **Disabled (not recommended)**, then **Save**." / "Open **Entra ID → Conditional Access → Policies**." / "Open **Core - Block - Legacy authentication**, set **Enable policy** to **On** and select **Save**." / "Return to 
- **TEMPLATE** 4.1–4.4 (switched-off policy) (4.x #12 and #16 (last line is the scan line)) · getiamai with an Off "CA - Block - Legacy authentication" that matches the target: "The task "Set the policy to Report-only" reads "Open … → CA - Block - Legacy authentication." / "Set Enable policy to Report-only and select Save." It ends with no scan line, while Create and Correct end with one." → Add the last line "Return to IAMAI and select **Scan to update the plan**."
- **NOT-LANDED** 4.1–4.4 (4.x #16 ("Description: **{tag}**." after the name)) · Every Create task. The builder dropped it on purpose: policyProcedure.ts createLines says Entra's policy form has no Description field.: ""Name: CA - Block - Legacy authentication." is followed directly by "Under Users, …". The JSON and PowerShell tabs still carry "description":"[IAMAI:plan-getiamai:s-goal-block-legacy-auth:285611fa]"." → No code change. The owner confirms that item 16's Description line is dropped because the Entra form has no field for it, and the walk list records it.

## Auditor 5
- **NOT-LANDED** 1.2 Configure Emergency Exclusions (1.2–1.4 #2 (rail names what's left once a group is chosen), #6 (no 'Could not verify' on 1.2), template rule 12; 4.x #7 (1.2 keeps the exclusion edit, the policy step waits on it)) · demo, product path (harness). Do exactly what 1.2 says (add Core - Exclusions to the 4 listed policies), or pick Core - Break glass (the group those 4 policies already exclude). Probes: fresh-r1-1.x/p12.mts and p12b.mts. The same at 5457a9fd, so not caused by the merge.: "The step stays Ready · Correct and shows 'Policy exclusions · Could not verify' with no instruction. The rail draws 'Next milestone' over an EMPTY headline, and the default task shown is 'Create an emergency exclusions group'. The only policy still lacking the group, Core - Grant - MFA for all users" → In exclusionsGroupPolicies, read a missing excludeGroups as no group excluded (outcome 'fail'), so the policy joins 1.2's list: '5 checks remaining', and 'Open Core - Grant - MFA for all users.' is added to Configure Conditional Access exclusions. Delete the 'Could not verify' value from the Policy 
- **NOT-LANDED** 1.2 Configure Emergency Exclusions (1.1 #11 (one reading of the unchosen state); 1.2–1.4 #9 (the membership task is the add procedure). Intent: nobody gets locked out.) · small, mid, demo, getiamai. In 1.1, save one of the two accounts (the 1.1 row reads 'Ready · Decision · Choose your second emergency access account.'). Same at 5457a9fd.: "1.2 reads Ready · Correct, not held on 1.1. Rail: 'Remove the members that aren't emergency access accounts from Core - Exclusions.' Card: 'Other members · Break-glass 2 is in the group without being an emergency account or an approved exclusion'. Task: 'Select Add members, add bg1@small-fixture.onm" → While 1.1 holds fewer accounts than it asks for, 1.2 waits on it: 'Up Next · After Prepare Emergency Access Accounts', as it already does with none chosen. Draw no 'Other members' check for a member 1.1 can still pick. The add line names only accounts that are not yet members. Delete 'Remove a few a
- **NOT-LANDED** 1.2 Configure Emergency Exclusions (1.2–1.4 #3 (the headline is words, never a lane word), with the 1.1 #11 reading) · A real first scan where the person picks the group before choosing accounts (fresh-r1-1.x/first2.mts small … group), and every fixture with 1.1 saved as none (bg-none). Same at 5457a9fd.: "Rail headline: 'Finish the steps this one waits on first.' (the engine's generic wait line that railOf's own comment says no fixture reaches). The membership card reads 'Emergency account membership · Core - Exclusions · 4 checks remaining · Other members · Break-glass 1 and Break-glass 2 are in the" → Headline: 1.2's own milestone, for example 'Exclude Core - Exclusions from 4 policies.' on demo, otherwise its next task title ('Manage emergency account membership'). Remove the engine milestone from railOf's candidates. Draw no membership card until 1.1 has chosen accounts, so no 'Other members' c
- **REGRESSION** 1.2 Configure Emergency Exclusions; 1.3 Configure Passkey Authentication (4.x #23 ('Delete the note Finish {step} first. everywhere') applied to section 1 by shared code; 1.2–1.4 #10 (1.2 empty card)) · A real first scan on small, demo, mid and getiamai (nothing saved), and 1.3 in every held state (demo open, getiamai open, *-bg-one, *-xg-none). Came in with fix/s4; HEAD equals fix/s4 for section 1.: "1.2: 'Exclusions group · 1 check remaining · Prepare Emergency Access Accounts'. 1.3: 'Passkey registration · 2 checks remaining · Prepare Emergency Access Accounts' (or '· Configure Emergency Exclusions'). Before, both lines read 'Finish Prepare Emergency Access Accounts first.' Now the check line " → 1.2 with no group chosen draws item 10's card: 'Exclusions group · No group selected · To create one, follow Create an emergency exclusions group in Implementation Tasks.' 1.3's Passkey registration card, whose four settings pass, folds under Satisfied as 'Passkey registration · Configured', as it d
- **NOT-LANDED** 1.4 Verify Emergency Access (Plan row) (1.1 #4 (When is always a date); template rule 11) · 1.4's row once 1.1–1.3 are done (small, mid, getiamai settled; demo-week2 after prepareEmergencyAccess), through the whole journey until the drill is recorded. Same class elsewhere on the page: demo and demo-week2 5.3 Require MFA for Guests, demo 8.2 Remove Emergency Accounts Excluded by Name. Same at 5457a9fd.: "'Ready · Review · Verify Emergency Access · 2 accounts · Review now'" → In cleanupExport.ts cleanupWhen, delete the branch `undated && readyReview ? schedulingWords.reviewNow`, so the row reads its estimate: 'Est. Aug 31, 2026'. In planBoard.ts boardTimingOf, a Ready · Review or Decision row with no day reads the phase's first day as an estimate, not 'Review now' or 'De
- **CONTENT** 1.4 Verify Emergency Access (1.2–1.4 #19 (1.4 filler lines removed); 1.1 #13 (one instruction, said once)) · Tasks Remaining when 1.4 is Ready (small, mid, getiamai settled; demo-week2 prepared): "'Sign-in evidence · bg1@… bg2@… · 2 checks remaining · Sign in with the prepared passkey · Sign in with this account’s passkey once the configuration checks pass. · Follow Verify emergency sign-in in Implementation Tasks.' The condition already holds in the only state that shows this line, and the c" → Delete 'Sign in with this account’s passkey once the configuration checks pass.' The line comes from the recovery finding's value, so the AI Info JSON's 'result' fields lose it too.
- **TEMPLATE** 1.4 Verify Emergency Access (1.2–1.4 #19; template rule 7 (name the accounts, no troubleshooting lines inside the procedure; the section 1 last line)) · Implementation Tasks → Verify emergency sign-in, in every state and on every fixture. When Completed, Tasks Remaining names nobody.: "'Retrieve the prepared passkey for the emergency account named in Tasks Remaining.' / 'Open a new private browser window and go to Microsoft Entra admin center. Any Microsoft sign-in counts.' / 'Sign in as that emergency account using its prepared passkey.' / 'Confirm the account and tenant.' / 'Sig" → Rewrite as (small): 'Open a new private browser window and go to [Microsoft Entra admin center](https://entra.microsoft.com/).' / 'Sign in as **bg1@small-fixture.onmicrosoft.com** with its passkey, confirm the account and tenant, then sign out and close the window.' / 'Repeat for **bg2@small-fixture
- **TEMPLATE** 1.3 Configure Passkey Authentication (Template rule 6 (a Completed step keeps 'No tasks remaining' and only Satisfied items); 1.1 #22; 4.x #2 (a Completed step shows no open cards). Intent Q2.) · demo, small and mid once the passkey target, key restrictions included, is applied (settled). Same at 5457a9fd.: "Badge 'Completed' over Tasks Remaining 'Existing passkeys affected · 5 accounts to prepare · Follow Prepare affected passkeys in Implementation Tasks.', then 'Satisfied · 2'. There is no 'No tasks remaining'. On demo the five include user2@demo.example.com, whom the open step said the allow list wou" → While an account the applied allow list locks out has no other way in, 1.3 is not Completed: it reads 'Ready · Correct' with 'Prepare affected passkeys'. Where every affected account keeps another way in, Tasks Remaining reads 'No tasks remaining' and the card folds under Satisfied as 'Existing pass
- **CONTENT** 1.3 Configure Passkey Authentication (1.2–1.4 #11 (1.3 tasks state their values in every state)) · demo open (and every open 1.3 on demo): "The Tasks Remaining card names only 'Existing passkeys affected · user24@demo.example.com · Affected passkey 1 · YubiKey 5 NFC · cb69481e-8ff7-4039-93ec-0a2729a154a8'. The procedure's first line says: 'Key restrictions stay off for now: they would lock out 1 account — user2@demo.example.com.' The ac" → The card lists each account the procedure names, locked-out accounts first: 'user2@demo.example.com · Passkey · No other way in', then user24's off-list key. It then reads '2 checks remaining'.
- **CONTENT** 1.3 Configure Passkey Authentication (1.2–1.4 #11; template rule 7 (no qualifier lines, no lectures)) · Prepare affected passkeys, in every state and on every fixture: "'Compatible alternative: sign in with the registered compatible alternative in a separate session, confirm the account, then continue to the final scan action.' / 'Replacement registration, only if needed: where no compatible alternative is registered, continue with the steps below to register a rep" → Delete the two labelled lines and those two lecture sentences, so the lead reads 'Key restrictions stay off for now: they would lock out 1 account — user2@demo.example.com. Register an approved passkey for each, then scan again: the restrictions are offered once nobody would be locked out.' followed
- **CONTENT** 1.3 Configure Passkey Authentication (Template rule 6 (each Satisfied item states its fact)) · getiamai and demo-week2, open and Completed: "'Existing passkeys affected · No existing passkeys were identified as losing sign-in access under this change.'" → 'Existing passkeys affected · No existing passkey stops working.'
- **CONTENT** 1.2 Configure Emergency Exclusions (Template rule 6 (each Satisfied item states its fact)) · Satisfied, Policy exclusions card: mid, small, Completed demo-week2: "'Policy exclusions · Required references present'" → 'Policy exclusions · 11 policies exclude Core - Exclusions' (mid; small reads '3 policies exclude Core - Exclusions').
- **CONTENT** 1.2 Configure Emergency Exclusions (Every line reads true (intent: states only facts it holds)) · AI Info tab on demo (Ready · Correct: group chosen, 4 policies missing it): "'State: no confirmed exclusions group exists in Contoso Pty Ltd yet. The next action creates one assigned, non-mail-enabled security group containing only the owner-confirmed emergency access accounts, or confirms an existing group that already meets that description.' Tasks Remaining on the same st" → A confirmed group with policies missing it selects the 'partial' block (ai.correct), naming Core - Exclusions and the four policies. The ai.create paragraph shows only while no group is chosen.
- **TEMPLATE** 1.3 Configure Passkey Authentication (1.2–1.4 #1 (every rail reads milestone → bar → instruction → controls); template rule 2) · 1.3's rail, every state: "'Next milestone · Prepare affected passkeys', then the 'Approved authenticator models' and 'Add additional AAGUIDs' folds (Authenticator Name, AAGUID, Add to List, Save Additional Authenticators). The step takes a choice but has no instruction line." → Add an instruction line under the divider, for example 'Add any other passkey model you use, then select Save Additional Authenticators.' (new words, needs the owner's OK).
- **TEMPLATE** 1.1 Prepare Emergency Access Accounts (Template rule 7 (no troubleshooting lines inside the procedure)) · Set up an approved passkey, every fixture: "'Cannot complete registration sign-in? Use Troubleshooting → Temporary Access Pass.' sits inside the procedure, one line above 'Return to IAMAI and select Scan to update the plan.'" → Delete the line. The Troubleshooting button beside Microsoft Learn already carries the Temporary Access Pass.
- **CONTENT** 1.1 Prepare Emergency Access Accounts (Template rule 6 / 1.1 #22 (Satisfied states its fact); prefer deleting) · Each Satisfied account card, every fixture: "'Emergency access account 1 · bg1@… · Account prepared · No account changes remain. · Completed checks · 5'" → Delete 'No account changes remain.' The heading 'Account prepared' and its five checks state the fact, as 1.2's Satisfied cards do with no extra line.
- **CONTENT** 1.4 Verify Emergency Access (1.2–1.4 #19 (1.4 filler lines removed); intent Q3) · The Emergency recovery procedure fold, every state: "'Keep the exported plan available independently of this tenant. Scan-specific facts reflect the scan at Aug 28, 2026, 7:00 PM and may differ during an incident.' and 'Conditional Access exclusions do not disable Security Defaults or authentication-method policy. Temporary Access Pass does not bypass" → Delete both lines. The fold keeps its numbered procedure.
- **REGRESSION** 4.1 Block Legacy Authentication, 4.2 Block Device Code Sign-in (and frozen 5.3/5.4 through shared code). The 27 drifted snapshots. (1.2–1.4 #3 (the milestone headline is words, never a lane word); template rule 2; 4.x #18 (the rail names the next task)) · Product path (harness): demo 4.1, demo 4.2, and 4.1 and 5.3 on small, mid, hostile, large and midflight. The corpus also shows 5.4's rail as the lane wait ('After Configure Passkey Authentication'); the product path shows 5.4's own title. Introduced by fix/s4 commit 4f6c3987 (stepBody.ts `waitWords = laneView.waitingFor`), not by the merge.: "The rail headline repeats the row's lane sub-line: 'Next milestone · Waiting on your answers' (4.1 and 5.3), and 'Next milestone · After Prepare Emergency Access Accounts' on demo 4.2. The same words also appear in the row and in the tile 'Confirm What You Use · Waiting on your answers'." → Delete waitWords. A policy step whose own tasks are done headlines what finishes it, in words the step already draws. On demo 4.1 and 4.2 that is the policy card's 'Configure Emergency Exclusions adds Core - Exclusions to it.' Where the only wait is a Direction answer: 'Approve the answers in Confir
- **REGRESSION** 4.1 Block Legacy Authentication (one of the 27) (4.x #33 (show who uses legacy authentication)) · demo 4.1, product and corpus paths. Introduced by fix/s4 commit e70c7f9f: blockSignIns.ts `signInsCard` returns null once the policy is On. Not caused by the merge.: "The 'Legacy sign-ins · 3 accounts signed in with legacy authentication in the last 30 days' card is gone because demo's legacy policy is On. The three accounts (Jamie Singh user4@demo.example.com, Morgan Wilson user6@demo.example.com, svc-mailer-1) are named nowhere on 4.1. The commit meant only to " → Keep the card on an On policy and change only its move line: 'Legacy sign-ins · 3 accounts signed in with legacy authentication in the last 30 days · Move Jamie Singh, Morgan Wilson and svc-mailer-1 to a modern mail app.' (legacyMove without ' before you turn this on').

## Fixer (fix/fresh-b)
- fixed: 3.x/4.x 4.1 #33 sign-in cards (both REGRESSION issues): the Legacy sign-ins card stays while the policy is On and drops only the turn-on clause. demo now reads "Legacy sign-ins · 3 accounts signed in with legacy authentication in the last 30 days · M
- fixed: 3.x/4.x rail 'Waiting on your answers' (two issues that conflict): a Direction wait now names the answer in 3.6's shape. demo/small/mid/large/midflight/hostile/messy 4.1 read "NEXT MILESTONE · Answer Mail-sending devices in Confirm What You Use." Gue
- fixed: 3.x/4.x 4.4 readiness and dormant accounts: demo 4.4 (Enforced, Correct) now shows a Satisfied card "Readiness · 21 of 30 people have a method it accepts" and no Dormant Accounts card. Completed steps are unchanged (demo-week2 "20 of 30"). The Dorman
- fixed: 3.4 #51 criterion: now reads "Every admin has a passkey or security key registered, or is on the Turn On Without Them list." This extends the approved words because they were false on a Completed step. Commit 16c561c2.
- fixed: 3.4 #48 Temporary Access Pass line: where the scan reads Temporary Access Pass as off (every fixture), 3.2's own tapPolicy line now comes first: "Open **Entra ID → Authentication methods → Policies → Temporary Access Pass**, select **Enable**, includ
- fixed: 3.3 #14/#41 Satisfied fact: names the method IAMAI saw. It reads "Signed in with Windows Hello on your computer, Aug 28, 2026." or "Signed in with your passkey on your phone, …", and names only the devices where that method was seen. This was checked
- fixed: 3.3 #42/#44: task line is "Sign out, then sign in with the passkey." AI Focus is "…and signing in with it." The owner's item-5 change (no every-device requirement) outranks #42's older {devices} wording; the owner can still rule on this. Commit 16c56
- fixed: 3.4 #50/#4 campaign line: now "Set the registration campaign: open …". The bare "Today: the registration campaign is on." is deleted: an enabled campaign with no targets gets no Today line. Commit 16c561c2.
- fixed: 3.4 #48 admins line: now "Admins who still need a passkey or a hardware security key: Quinn Ivanova (user1@demo.example.com)." Commit 16c561c2.
- fixed: 3.4 template rail instruction: shows only while someone not ready is still unmarked. The picker stays so a mark can be undone. Commit 16c561c2.
- fixed: 3.2/3.5 role first: 3.2 reads "As at least a User Administrator, open [Microsoft Entra admin center](…) → **Entra ID → Users → New user → Create new user**." 3.5 reads "As a Security Administrator, open … → **Entra ID → Authentication methods → Authe
- fixed: 3.3/3.5/3.6 bold values: "sign in as **user0@…**", "Name: **Modern MFA + TAP**.", each method in bold ("**Windows Hello for Business**, **Passkeys (FIDO2)**, …" via the methodNames binding), and "Name: **CA - Trusted - Head office**." Commits cd2668c
- fixed: 4.3 #7 Report-only edit (NOT-LANDED): making 1.2's edit on demo 4.3's Report-only policy now reads as the change the plan asked for. The member has reviewRequired false and expected true, there is no "New evidence" line, and the policy is "Report-onl
- fixed: 4.1–4.3 one policy (#18): where the tenant already has the policy, the create describes it as the step leaves it, under its name. demo 4.3 reads "Name: **Core - Grant - Admins phishing-resistant**." / "Under **Users**, exclude the groups **Core - Exc
- fixed: 4.1 messy (#18, NOT-LANDED): the review task is gone. It now shows 'Create the policy in Report-only' and 'Turn the policy on' for Core - Block - Legacy authentication; messy 4.2 and 4.4 are fixed the same way. The rail reads "Answer Mail-sending dev
- fixed: 4.5/4.6 Completed AI Info: "What to do: Nothing left to do: security defaults are off." and "What to do: Nothing left to do: per-user MFA is off for every account." The rail's barLead reads the same. New keys: steps[].inPlace. Commit 514ceddc.
- fixed: 4.1 #22 Satisfied card after the mail half reopens the step: "Conditional Access policy · Core - Block - Legacy authentication · On · Blocks Exchange ActiveSync and Other clients for All users except Core - Exclusions". Commit 96e954ca.
- fixed: 4.1 #36 mail card: "Mail accounts · Noor Nguyen still signs in with legacy authentication · Move the device or app that signs in as Noor Nguyen to a supported mail route." The plural and before-turn-on variants were changed the same way. Commit 96e95
- fixed: 4.3 #17/#27 held turn-on: blockers known only by kind (such as Baseline safety conflict) are no longer listed as things to wait for. getiamai drift now reads "Wait for Prepare Emergency Access Accounts, Configure Emergency Exclusions and every admin 
- fixed: 4.3 #24/#31 AI Info in drift: "What to do: Configure Emergency Exclusions adds Core - Exclusions under Users → Exclude; then correct the grant controls." I left out "back" because it is not true for a tenant policy that never had the group, and "gran
- fixed: 4.1/4.2 AI Info: "Move any mail client…" and "Move any tool or device…" are deleted from ai.observe. When report-only did block someone, item 35's named line already covers it. Registry recompiled. Commit 1127b871.
- fixed: 4.6 Troubleshooting: the troubleshooting.model block is deleted and META updated, the same way 4.5's was, so no scenarios draw on a Completed 4.6. Commit cd2668cd.
- fixed: 4.5/4.6 bold: e.g. "… → **Entra ID → Overview → Properties → Manage security defaults**." / "Select **Disabled (not recommended)**, then **Save**." / "Open **Entra ID → Conditional Access → Policies**." / "Open **Core - Block - Legacy authentication*
- fixed: 4.1–4.4 switched-off policy: 'Set the policy to Report-only' now ends with "Return to IAMAI and select **Scan to update the plan**." Commit 96e954ca.
- fixed: Checks: tsc passes. Seven focused verify runs (≤6 files each) show no new failures; every failure left also fails on 4e9fd544. In content.test.ts, the orphan check's app-only rule and bold-value fragment handling were updated (87cf1c37), so its non-r
- NOT fixed: 3.4 #47 card "Give each" → "Give them": this conflicts with approved #47's exact words "Give each a Temporary Access Pass when they can register." The approved item wins, so the owner decides. The line is not false, only awkward with one name.
- NOT fixed: 4.4 #15/#26 Intune line vs completion: not fixed; the owner needs to choose. Option (a), making completion read Target resources, would make the step demand a narrowing (adding an exclusion to a stricter policy). That goes against 'a decision may nar
- NOT fixed: 4.x #16 Description line: no code change, as the issue itself says. The owner confirms item 16 is dropped and the walk list records it.
- NOT fixed: Unverified: none of this was checked on screen at desktop width (no dev server by rule). Step snapshots were not regenerated (merger's job). The mid-flight states (4.3 drift, 4.3 Report-only edit, 4.5/4.6 Completed, 4.1 mail reopen) were checked only
- NOT fixed: Pre-existing test failures, also on 4e9fd544: direction.test.ts (5), milestoneHeadline 'an open decision…', planVariants/004.19, planBoard/planLanesHolds/printPlan/directionStep (4), stepResources/countriesStep/oneProducer (3), content.test 'no orpha
- NOT fixed: NET-NEW (not work): small 4.1 after 2.1 names a mail account shows lane 'On Hold · Not supported' and a card "Not supported · Not supported · Core - Block - Legacy authentication found. IAMAI writes no policy here."
- NOT fixed: NET-NEW (not work): AI Info What to do repeats the gate after the action ("… Exclude; then correct the grant controls. | Doesn't exclude Core - Exclusions.").
- NOT fixed: NET-NEW (not work): the rendered 3.4 Turn On Without Them card lacks approved #47's "once everyone else is ready".
- NOT fixed: NET-NEW (not work): 3.2's tapPolicy line, now reused on 3.4, opens "Open **Entra ID → …**" without the "[Microsoft Entra admin center](…) →" opener that 3.x #20 uses. The 3.4 card's "Give each a Temporary Access Pass" also depends on the Temporary Ac

## Net-new from round 1 (for the morning list)
- LEAD, the 27 drifted snapshots: the merge changed none of them. Run in memory, fix/s4's own head (89fe84a5, worktree C:/Dev/IAMAI-s4) produces exactly fix/fresh's 27 files. fix/s4's committed snapshots were last written at f9f6b1ec, 11 commits before its head (4f6c3987..89fe84a5), so `git diff fix/s
- demo 4.1 'Legacy sign-ins · 3 accounts…' card: e70c7f9f removed it on purpose (blockSignIns.ts signInsCard returns null once the policy is On). It was not the merge, and it is the same on fix/s4 through the harness. Owner call: demo's policy is On but 'Not as asked', and the card named Jamie Singh (
- Step snapshot corpus (src/testing/stepSnapshots.ts) never applies a fixture's saved decisions. demo-week2 4.1 and Require MFA for Guests therefore snapshot as On Hold · Waiting on your answers, while the product (appliedMapping, the harness) draws 4.1 Completed and guests Ready · Review.
- 'Answer it in {step}.' left the tiles (item 18) but still comes from contract.fix (stepContract.ts:1117, directionWords.waitingNote). It shows in AI Info ('Fix before continuing: … Answer it in Confirm What You Use.'), in the package briefing ('Known blockers or decisions: … Answer it in Decide How 
- Content keys that are unused or on another surface: directionWords.defaultEvidence (unused), waitingNote (AI Info only), answeredIn (only the Inventory table header 'Answered in Confirm What You Use'). demo-week2's saved 2.1 decision still carries 'deviceCode: unused' from the deleted question (#1).
- 2.2's service-account and shared-device pickers offer Break-glass 1 and Break-glass 2 (and guests): pickerRows.ts pickerUniverse for serviceAccountsGroup has no filter. Picking an emergency account puts it in the group that Restrict Service Accounts to the Trusted Network includes. #32 filtered thes
- A Completed 2.2 or 2.3 row is dated to the scan, 2.1 to its approval. direction.ts:369 approvedAt is never passed, and only 2.1 falls back to workflowConfirmedAt.
- Company computers on tenants without Intune (small, mid, getiamai) suggests 'Managed (compliant in Intune, or hybrid joined)', and the answer changes no step: correctly, it has no consequence line. The question does nothing there.
- The SharePoint and OneDrive card shows no evidence on every fixture. The app sign-in summary holds SharePoint sign-ins (408 on demo), but serviceSignIns.sharepoint is 0, so the card suggests Yes without stating a fact.
- Require MFA for Guests on demo reads Ready · Review (its frozen Workflow Check) while carrying the 'Confirm What You Use · Waiting on your answers' tile.
- Restrict Service Accounts to the Trusted Network and 5.1 rows read 'Waiting on your answers', but the opened step has no Direction tile. The wait is carried by the 3.6 and 3.7 prerequisite cards, so the body never says which answers.
- demo 4.1, 4.2 and 4.4: a policy that is On but 'Not as asked' still lists 'Create the policy in Report-only' (New policy, Name: Core - Block - Legacy authentication), a duplicate create. This is for the section 4 audit.
- demo Require MFA for Inforcer Access (a frozen step held by 2.1) shows the card 'Application not established' (a banned phrase).
- Plan.tsx still hard-codes 'Changes are still in this tab, but could not be saved in this browser. Retry before closing it.' and 'The saved plan could not be read from this browser.', unlike item 24's two lines.
- Direction steps' About has no 'Learn →' and no Source checked line (template rule 5); template-check.mjs exempts the decision anatomy.
- Direction AI Info (aiGrounding.ts:312) states a list answer as 'Pick accounts' or 'Pick locations' without the names. It is not drawn on screen, but it reaches Export.
- Round-1 lead, how it happened: the merge moved no snapshot. fix/s4's committed step snapshots were last regenerated at f9f6b1ec, 9 behaviour commits before its head 89fe84a5. Snapshots built in memory (src/testing/stepSnapshots.ts) from fix/s4's head equal fix/fresh's exactly. All 27 drifts come fro
- Scope: shared 4.x code (items 20 and 21) also rewrote frozen steps outside the owner's "14–18, 25 and 29 on every policy step" list. Block Authentication Transfer, Require a Fresh Sign-in for Intune Enrollment and Require Token Protection on Windows lost their Observation card and gained "Report-onl
- 3.6 opened as a Doesn't apply row (2.3 answered "Everyone works remotely") reads "Trusted network · Not in place · Put this step back if you want it on the plan again.", with the rail "Create the trusted network" and Completion Criteria "Nothing is expected of this step while it is deferred.". A Dir
- Troubleshooting left after #21: 3.6 "A proposed range is a private intranet address rather than public egress / An update accidentally drops an existing valid range / The trusted location stops matching office sign-ins". 3.7 "An account is proposed for the group only because its name starts with svc
- Unapproved hedging in AI Info Focus lines: 3.1 "Include each candidate's name, ID, recorded activity and evidence gaps. Separate a keep decision from a successful sign-in and from confirmed inactivity." 3.4 "…Do not promise every tenant receives the same campaign prompt or call Authenticator push a 
- 3.6 held AI Info still carries "Fix before continuing: Answer it in Decide How and Where People Sign In.", the note 2.x #18 removed from tiles. It comes from stepContract.ts:1117 (directionWords.waitingNote) in contract.fix, so exports carry it too.
- 3.1 AI Info "Who this touches: 9 accounts · 1 guest" counts kept accounts, while the row's Impact reads "8 accounts" after one keep.
- A one-task card drops its pointer on 3.2 with one admin (small; demo after one role moves; policyTasks.ts `only` rule). 3.1's one-task card keeps "Follow Disable or keep each account in Implementation Tasks." (#26 wording). The same kind of line differs between the two.
- 4.3's META.json allowedCombinations still lists temporaryAccessPassMultiUse (section 4's frozen note: fix with 3.x #55).
- getiamai 4.4 (Up Next, create in report-only) shows the 9-account Dormant Accounts card with no Threshold card beside it. This is the same orphan as the demo 4.4 regression, on a fixture outside the snapshot corpus.
- The 27 snapshot drifts did not come from the merge. fix/s4 last regenerated its snapshots at f9f6b1ec; its next 11 commits (4f6c3987 to 89fe84a5) changed readings without regenerating them. Running fix/s4's own code (C:\Dev\IAMAI-s4) reproduces every merged reading exactly. Classification: 15 are ex
- Turning security defaults off makes the recovery test stale (cleanupDone.ts:497 puts securityDefaults in the drill fingerprint). If the person scans after turning security defaults off but before turning the four policies on, 4.5 reads Completed while 4.1–4.4 read 'Up Next · After Verify Emergency A
- demo 4.3 tracks the tenant's own policy (Global Administrator only, built-in Phishing-resistant MFA, excluding Core - Break glass), so its Completion Criteria describe that policy. The Create task, shown in every state (#18), describes 46 roles and Modern MFA + TAP. AI Info's 'Intended result' lists
- The Direction steps' About has no 'Learn →' link (template rule 5). The approved About words (2.x #37, #47, #63) have none.
- Rail headline punctuation: 1.1 'Complete the remaining emergency access checks.' and 1.2 'Exclude Core - Exclusions from 4 policies.' end with a full stop. Every other headline does not.
- 3.1's badge reads 'Ready · Review' for a disable-or-keep task. 3.x #18 fixed only 3.3 and 3.4.
- AI Info 'Focus' lines on steps outside 4.x #31 still lecture: 3.6 'Do not infer trust from traffic.', 3.4 'Do not promise every tenant receives the same campaign prompt…', 3.1 'Separate a keep decision from a successful sign-in and from confirmed inactivity.', 1.2 'Do not equate selecting the group 
- Troubleshooting remains on 3.6 (3 scenarios), 3.7 (2) and 4.6 (3). 4.6's appears only once the step is Completed, which is the pattern 3.x #21 removed from 3.1.
- 2.x #24 moved 'Plan could not be saved. Use Retry Saving above.' into content.json. It contains 'could not'. It is a real save failure, not hedging.
- 3.4 task line 'Admins: register a passkey or a hardware security key with Quinn Ivanova (user1@demo.example.com).' can be read as 'register together with Quinn'.
- 4.x #26 and #31 approved 'report-only week'; the build says 'report-only period'. 4.1 and 4.2's windows are 3 days (created Aug 28, until Aug 31), so 'week' would be false. 4.5's About and card also reword 'the four policies below' (they are above 4.5, and the count can vary).
- 1.2's portal artifact text still carries 'Membership alone does not prove that the group is excluded…'. The screen draws the task body instead, so check print and export.
- The 27 drifted snapshots were not moved by the merge. fix/s4 took its snapshots at f9f6b1ec and then made ten more commits (4f6c3987..89fe84a5) without regenerating them. Regenerating at fix/s4 HEAD gives the same 27 files as fix/fresh. The 4.x renders on demo, demo-week2, small, mid and getiamai (f
- stepSnapshots.ts renders with the stored mapping and applies no saved Direction decisions, while planData.ts applies them. So demo-week2 4.1 snapshots as "On Hold · Waiting on your answers" while the Plan (harness) reads Completed.
- getiamai 4.1 first scan: the row reads "On Hold · Waiting on your answers" while the card and rail say "Create the policy in Report-only". Only the turn-on waits on the mail answer.
- 4.3 with an admin key that is not on the approved list: the "Your account" card disappears, yet the Threshold says "Kai Brown (user0@…) needs a passkey, security key or Windows Hello". He has a key; the policy refuses it.
- 4.3 About says the policy accepts "a certificate or a Temporary Access Pass". On demo, demo-week2 and mid the goal is delivered by the tenant's own Phishing-resistant MFA policy, which accepts no Temporary Access Pass.
- demo 4.4's correction and finish line keep an emergency account excluded by name ("except Core - Exclusions and Break-glass 1"). The rule is that exclusions go through the group.
- small 4.3 Threshold reads "0 of 2 admins have a method it accepts" and names only Morgan Brown. The operator (Sam Patel) is on a separate "Your account" card, so the count and the names don't match on the card.
- 4.3 AI Info on a Report-only policy reads "What to do: It stays until every admin has a method it accepts (0 of 1 today)." These are item 44's words for a policy that is already On. demo 4.3 AI Info reads "What to do: It turns on after Configure Emergency Exclusions. | When every admin has a method 
- messy 4.1: Core - Exclusions holds 116 of the 122 people the goal is for, yet the Satisfied card reads "Blocks Exchange ActiveSync and Other clients for All users except Core - Exclusions" and Impact reads "6 admins".
- Frozen steps: "Inforcer Application · Application not established" (hostile, large, mid, midflight, small snapshots) uses a banned phrase.
- 5.1 (frozen) Threshold tile went from "At least 68% MFA-ready" to "68% MFA-ready" (d152fc58). Check that its detail states the count; otherwise the lower bound is lost.
- Harness limitation: rescan() carries only observations. The plan record's completedAt, securityDefaultsSeenOnAt and perUserMfaSeenOnAt are not carried, so Completed rows re-date every scan and 4.6 vanishes unless the probe passes the seen-on date (my sdpum.mts passes it through runFixture's over arg
- Snapshot corpus is not the product path. stepSnapshots.ts runs non-demo fixtures on the synthetic stand-in baseline and on the STORED mapping (no detected defaults), while planData/the harness use the pinned baseline and the applied mapping. Example: mid 5.4's rail reads 'After Configure Passkey Aut
- fix/s4's committed snapshots were stale: 10 code commits (4f6c3987..89fe84a5) followed its last [snapshots] commit f9f6b1ec. That is why the merge appeared to move 27 files; it moved none.
- The Plan header tile is still labelled 'Observing' although 4.x #11 renamed the report-only week ('Up Next · Report-only until …', 'Ready · Turn on').
- 5.1 Threshold card on demo reads '68% MFA-ready' beside '20 of 29 people have a method it accepts' (68.97%), so the percentage is floored while the count implies 69%.
- demo 4.4 (policy On, needing correction) lost its Threshold card (4f6c3987) but keeps 'Dormant Accounts · 1 account hasn't signed in for 90 days and has no method', which only matters to that gate.
- messy 4.1's rail headline and its only task are the step title 'Block Legacy Authentication' (4.x #18 not reaching messy); frozen 5.4 does the same with 'Require MFA for Inforcer Access'.
- small 5.3 Impact reads the label 'No user impact' (4.x #25 not reaching frozen 5.3).
- 1.2 Completed-checks fold lists 'Mode: On/Report-only' rows that are not checks (under a card that also says the same policies are Missing), and 'Direct member count: 2' duplicates 'Members: 2 members'.
- 1.2 'Choose an existing exclusions group' says 'open the intended exclusions group' although the picker already suggests one (Core - Exclusions, excluded from every policy). Its closing 'Select Scan to update the plan.' follows a choice made in IAMAI.
- AI Info on 1.1–1.4 never had a pass like 3.x #8/4.x #31. It has hedges ('Do not invent tenant values…', 'It does not confirm that any change has been made', 'This does not prove recovery works; perform and record a controlled sign-in test.'). 1.3's intended PATCH enables key restrictions that the En
- 1.4 On Hold still says 'Sign-in required · Follow Verify emergency sign-in in Implementation Tasks.' and 'Next milestone · Sign in with each emergency access account.', although a sign-in made before 1.3's change will not count ('since its last change').
- Section 1 headline form is mixed: 1.1, 1.2 and 1.4 use sentences with a full stop; 1.3 (and every 4.x step) uses a bare task title ('Prepare affected passkeys').
- 1.1's unchosen-account card always points at 'To create one, follow Create an emergency account' even when the picker holds two suitable candidates (owner left suggestions as they are, 1.1 #10).

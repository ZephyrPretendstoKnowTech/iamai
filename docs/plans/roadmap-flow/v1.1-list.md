# v1.1 list

These wait until after the v1.0 launch (target Monday 2026-09-28). Each is either too big for this week or needs the finished product in front of the owner first. Items are added as the v1.0 build defers them.

## The view
- **Remove Show completed and Show deferred.** Owner: only after seeing the finished product.
- **The When column.** Nearly every Ready row shows today's date, because every create happens on day one. Show "Now" for creates and dates only for turn-ons. Not scoped yet.

## Report-only up front
- **"Create everything" PowerShell bundle.** One script for every Ready · Create policy. About 3–5 hours: it needs an existence check first, because a half-finished run would create duplicates on re-run.
- **Creating policies while Direction is still open.** This would free about 9 of 15 creates, but it contradicts the 2026-09-19 owner decision. Five goals exclude the service accounts group without Direction listing that dependency. Not recommended.

## Dormant accounts
- **A smart list.** Grouped: admins, never signed in, 90+ days oldest first, guests, and new accounts shown as "not yet claimed". Columns: account, last sign-in, created date, member or guest, flags. Reads the Direction 2.2 shared picks. About 3–4 hours.
- **Synced (hybrid) accounts.** They are disabled in Active Directory, not Entra. The step's instruction is wrong for them.
- **Telling shared mailboxes apart for certain.** This needs MailboxSettings.Read, a new consent permission, so it is an owner decision.

## Licensing
- A "Not licensed (n)" line under the Completed tile. It must update the smoke check at scripts/smoke.mjs:535 in the same change.
- The why and a Learn link on each Not licensed row.
- The How Limits line: "IAMAI plans only the baseline controls the tenant's licences cover; the Plan lists the rest under Not licensed."
- Connect: "· n not licensed" on the plan line.
- **Partial P2 seats.** Risk policies are planned for everyone even when P2 covers only some people. Today only Inventory shows the seat count.

## Per-user MFA
- **Give the demo fixtures per-user MFA readings.** Today the demo always shows the step as "Not fully read".

## Paused this morning for the flow work (re-check against the new structure)
- **Engine:**
  - the pinned baseline wins on every channel of a create (High user risk);
  - the arrival note after an unscanned report-only period;
  - Cleanup dates after deferrals;
  - the "Not supported" label;
  - the cannot-finish wording.
- **Dates:**
  - undated who-lines;
  - "Until Est.";
  - the email day while turn-on is held;
  - the enrolment timeline line;
  - the MFA-date fallback;
  - past announce days.
- **Exports:**
  - prompt-pack fluff and the block cap;
  - calendar unmasked;
  - preparation lines that point at the screen;
  - bundle profile counts;
  - "IAMAI writes" wording;
  - old plan files after the pin moves.
- **Trust:**
  - the IAMAI Planner name on the enterprise app;
  - the PIM-eligible instruction;
  - How's check rows;
  - the xg.notDynamic reasons;
  - How's Needs column;
  - the registration remedy.
- **Inventory:**
  - one reading of each person;
  - Roles CSV names;
  - the cross-tenant fixture row;
  - not-applicable goals listed;
  - 24 px buttons;
  - the PIM authentication-context check.

## Sign-in read (large tenants)
- **Automated tests of the browser's local database** (the evidence store's read ranges, writes, expiry, and upgrading a version-7 cache). This needs the `fake-indexeddb` dev dependency downloaded from npm, which is an owner approval. The paths were checked by hand in headless Chrome on the branch.
- **Pages of 1,000 records instead of 200.** A one-constant change, once a live timing check on a real tenant confirms it stays under the page timeout.
- **A resumed read interrupted inside its catch-up gap starts that gap over** (unchanged from before) unless round 4 made it continue.

## Housekeeping
- Two stale statements: the resolvePolicy.ts:12 comment ("two travellers groups") and content.json:5525 (the service accounts group on the countries policy).


- runner · S0 · exit 1 with uncommitted changes · stashed as 'auto-stash after S0 (exit 1)'
  - S0 rerun (2026-09-13): the stash holds only the content-review specs and runner files, since committed in d703e15/0b65580; nothing to apply. Left in place, not dropped.

- R8 · renderer fix not needed at the lane engine; the reported case needs the real tenant's step state · lanes.ts already returns Ready · Correct before the unsaved-input Decision whenever drift is observed (unit test added). No fixture reproduces "enforced + drift + unanswered input → Decision": all 20 enforced Ready · Decision steps across the eight fixtures (Block Legacy Auth, Block Device Code, Guests MFA) read "Satisfied by … — no change needed", with no drift. The screenshot's drift is one the engine does not count: most likely the exclusions-group correction waiting on confirmation (see R9). Counting it would change what planLanes.ts `observe()` treats as drift, which is data flow (RUN-CONTEXT rule 4).

- U-P2 · deferred as a known issue: scheduler logic is not straightforward · The projected finish comes from derive/finish.ts `projectedFinish` over roadmap/stepSchedule.ts. That is a critical-path schedule with waves, observation windows and a committed target (A2). Setting a floor for foundation days, 7-day evidence windows, enforcement transitions, a review buffer and tenant-size bands (<50 users: 2–4 weeks; >200: 8–12 weeks) changes the engine's scheduling model and every dated row, rail and When column that reads it. It is not a content change; the spec defers it when complex.

- U-P3 · not straightforward: the cache target is already met, and the remaining fix contradicts a pinned session decision · src/graph/msal.ts already sets `cache: { cacheLocation: 'sessionStorage' }`. Tokens and the active account survive navigation and refresh within a tab, and `getGraphToken` renews silently through `acquireTokenSilent`. A new tab starts with empty sessionStorage and asks to sign in again, which is the "across tab opens" symptom. Keeping sign-in across tabs needs `localStorage`. That reverses the per-tab session pinned by src/ui/sessionTruth.test.ts ("MSAL's session is this tab's") and the session-only `clearAuthCache` behind Forget this tenant; it is an owner security call. The "expires frequently" report cannot be traced from the code and needs a real-tenant repro (which call raises InteractionRequiredAuthError).



- runner · S0 · exit 1 with uncommitted changes · stashed as 'auto-stash after S0 (exit 1)'
  - S0 rerun (2026-09-13): the stash holds only the content-review specs and runner files, since committed in d703e15/0b65580; nothing to apply. Left in place, not dropped.

- R8 · renderer fix not needed at the lane engine; the reported case needs the real tenant's step state · lanes.ts already returns Ready · Correct before the unsaved-input Decision whenever drift is observed (unit test added). No fixture reproduces "enforced + drift + unanswered input → Decision": all 20 enforced Ready · Decision steps across the eight fixtures (Block Legacy Auth, Block Device Code, Guests MFA) read "Satisfied by … — no change needed", with no drift. The screenshot's drift is one the engine does not count: most likely the exclusions-group correction waiting on confirmation (see R9). Counting it would change what planLanes.ts `observe()` treats as drift, which is data flow (RUN-CONTEXT rule 4).

- U-P2 · deferred as a known issue: scheduler logic is not straightforward · The projected finish comes from derive/finish.ts `projectedFinish` over roadmap/stepSchedule.ts. That is a critical-path schedule with waves, observation windows and a committed target (A2). Setting a floor for foundation days, 7-day evidence windows, enforcement transitions, a review buffer and tenant-size bands (<50 users: 2–4 weeks; >200: 8–12 weeks) changes the engine's scheduling model and every dated row, rail and When column that reads it. It is not a content change; the spec defers it when complex.

- U-P3 · not straightforward: the cache target is already met, and the remaining fix contradicts a pinned session decision · src/graph/msal.ts already sets `cache: { cacheLocation: 'sessionStorage' }`. Tokens and the active account survive navigation and refresh within a tab, and `getGraphToken` renews silently through `acquireTokenSilent`. A new tab starts with empty sessionStorage and asks to sign in again, which is the "across tab opens" symptom. Keeping sign-in across tabs needs `localStorage`. That reverses the per-tab session pinned by src/ui/sessionTruth.test.ts ("MSAL's session is this tab's") and the session-only `clearAuthCache` behind Forget this tenant; it is an owner security call. The "expires frequently" report cannot be traced from the code and needs a real-tenant repro (which call raises InteractionRequiredAuthError).

- D3 · needs state management (RUN-CONTEXT rule 4) · Cleanup steps such as Rename Policies are Cleanup rows (roadmap/cleanup.ts `cleanupRows`, drawn by CleanupStep.tsx `CleanupBody`), not lane-engine steps. "Defer this step" on a content step is the existing skip (`cs.skip`), and it puts the step id in the engine's `owner.deferred`, which is how it reaches the Deferred lane. A Cleanup row has no id in the dependency graph, no skip and no deferral record: its only persisted state is the Done checkpoint date (roadmap/cleanupDone.ts). A deferrable Cleanup row needs a new plan-record field, a Deferred placement for Cleanup rows on the board and in print, and the way back (Put back). That is new persisted state and board data flow, not a footer condition.

- runner · S1 · exit 0 with uncommitted changes · stashed as 'auto-stash after S1 (exit 0)'

- S2 · started on a tree without S1 · S1 made no commit: its package edits and registry are in 'auto-stash after S1 (exit 0)', and its untracked src/ui/surfaces/contentPatterns.test.ts, which fails without them, is set aside as 'S1 untracked contentPatterns.test.ts, set aside by S2'. S2's CURRENT texts match this tree, so its specs were applied here. Restoring S1's stash will conflict in the s-prereq-break-glass, s-prereq-exclusion-group, s-prereq-passkey-settings and s-prereq-auth-strength packages and in the registry; the per-step text wins (RUN-CONTEXT rule 6).

- s-prereq-break-glass AI Info · build failed: emergencyAccounts.test.ts:120 "the package still names the whole set" got a hold {missingBindings: [emergency.target.userId]}, and implementationRegion.test.ts P0-4 found no channel on the demo's emergency step · The TARGET drops `{{emergency.target.accountsSummary}}` and `{{tenant.displayName}}`, and pinned tests require AI Info to name every confirmed account. Reverted; the Why and Entra steps 6 and 8 landed.

- s-prereq-exclusion-group readiness tile · build failed: readinessWords.test.ts P1-1 "the Decision tile reads Decision" expected 'Decision', got 'Confirm the exclusions group' · A Decision tile's value is the substatus's own word (U11, stepContract.ts readinessOf), pinned for this step and for the device decision. Reverted, together with the `decision.tileValue` key it read. The tile's note already names the group IAMAI found.

- s-prereq-exclusion-group AI Info · build failed: implementationRegion.test.ts P0-3 "the exclusions group is a planning preview" got no preview, and P0-4 found no channel · The Entra and AI Info TARGETs together bind no tenant value. JSON and PowerShell are withheld, so the package holds (project.ts: a channel without IAMAI's values is not offered alone). The AI Info TARGET drops `{{tenant.displayName}}`; it was reverted and the Entra TARGET landed.

- s-prereq-device-plan readiness tile · contradicts a pinned decision: readinessWords.test.ts P1-1 asserts this step's Decision tile value is 'Decision' (U11); the same change failed on the exclusions group · "Not yet decided" not applied.

- s-prereq-device-plan dropdown options · needs data flow (RUN-CONTEXT rule 4) and test infrastructure · The option text is the stored answer: plan records hold it (roadmap/answers.ts maps older answers onto these exact words, and the Unmanaged phones toggle follows `strict.when` = "Enroll phones in Intune"). The walk picks options by exact text (scripts/walk.mjs `chooseOption('Hybrid join is sufficient')`). A suffix inside the `<option>` changes both. Showing the explanations needs an option label separate from its stored value in the select and the walk, which is outside a content change.

- s-goal-mfa-all-users AI Info "the 33% threshold tile" · applied verbatim; needs an owner call · The percentage is the owner tenant's reading at review time. No binding carries the threshold value into a package (stepPackage.ts binds no readiness value), so every other tenant's prompt reads 33%. Carrying it needs a new binding, which is data flow (RUN-CONTEXT rule 4).
- s-goal-mfa-all-users MFA Readiness line "Some users may not be able to meet this step's MFA requirement yet." · build failed: footer.test.ts "\"people\" on Today, the Plan and Connect; \"user\" only for an Entra user object" found "Some users" in the Plan's words · The Plan says people, with one owner-chosen exception ("No user impact"). Reverted to "This scan could not work out who cannot meet this step's sign-in requirement yet." The link now reads "Check MFA Readiness →" with "for per-person detail." after it, as the spec gives.
- s-goal-mfa-all-users expanded threshold note · no change · The spec's example ("33% of users have registered an MFA method") is given "per U22". U22's sentence already states the fact on an enforced policy ("33% of people in scope have a qualifying method."), and enforcedLanes.test.ts pins it.

- s-goal-guests-mfa tile label "PARTNER ACCESS" · needs data flow (RUN-CONTEXT rule 4) · The label is the stored answer's key: questionAnswers[stepId:label] (ContentStep.tsx, decisions.ts) and the engine's unsaved-input name (generate.ts unsavedInputsOf). Renaming "Partner or MSP access" orphans every saved answer. The tile's value ("Confirm whether partners access your tenant") landed.
- s-goal-guests-mfa milestone (None / Yes — add partner tenant domains) · needs data flow (RUN-CONTEXT rule 4) · The decision today is a multi-select picker of partner tenants (pickerSource partnerTenants), saved as picked tenant ids, beside the service-provider question. The target replaces it with a None/Yes question that takes tenant domains. That is a new stored answer shape, and the partner tier and cross-tenant trust read the picked ids. "Partner tier" → "Partner access" and the new help would put a yes/no question over a picker with no yes/no, so the label and help are left as they are.
- s-goal-guests-mfa expanded partner tile note · depends on the milestone above · The target tells the operator to identify partners "here" or "choose 'None.'" No None option exists until the milestone changes.
- s-goal-guests-mfa AI Info · depends on the milestone above · The target says "add their tenant domains above" and "choose \"None\"", neither of which the step offers. It also keeps the package projecting: ai.correct binds policies.guests.semanticMismatches, and JSON is withheld on the partial pair.
- s-goal-guests-mfa Entra policy names · applied through bindings · The target names the owner tenant's policies ("Core - Allow - MFA for Guests", "Core - Allow - MFA for Internal Users"), which is tenant-derived data (CLAUDE.md). The Entra block names each member by {{policies.guests.<role>.current.displayName}}, now bound from the scan row memberBindings already reads (stepPackage.ts). On the owner's tenant it reads as the spec's names.

- s-goal-admins-phishing-resistant AI Info "The 0% threshold means none of your admins currently have a qualifying method registered." · applied verbatim; needs an owner call · Like the 33% on Require MFA for Everyone, the percentage is the owner tenant's reading and no binding carries it. Where some admins are ready, the sentence is false.
- s-goal-admins-phishing-resistant Entra policy name · applied through a binding · The target names the owner tenant's "Core - Allow - MFA for Admins". The block names {{policy.current.displayName}}, now declared, which reads as that name on the owner's tenant.

- s-goal-block-auth-transfer Entra "Save. Do not change the policy state (leave it On)." · applied verbatim; the channels now disagree, owner call · The correction used to stage an enabled policy to Report-only before changing access-affecting conditions, and this package's PowerShell still does: CorrectConditions refuses while the policy is On ("Run StageForCorrection first"). Entra now corrects in place. The policy name is {{policy.current.displayName}}, now declared; the target's "Core - Block - Authentication Transfer Flow" is the owner tenant's name.

- s-prereq-auth-strength picker pre-fill (U24) · needs data flow (RUN-CONTEXT rule 4) · The spec asks to verify that a matching strength opens pre-filled, as groups do. It does not: only the groups picker passes a scan match (pickerRows.ts `vars('groups', …, matched)`). The strengths picker supplies no `strengthsMatched`, so no strength opens as "✓ Matched by IAMAI". Pre-filling needs the scan's match of the baseline's combinations passed into the picker's values, which is picker data, not text.

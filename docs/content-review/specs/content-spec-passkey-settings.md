# Content spec: Set Up Passkeys to Match the Baseline

**Step ID:** `s-prereq-passkey-settings`
**Package:** `docs/implementation-content/s-prereq-passkey-settings/`

Every change below is a word-for-word replacement. `CURRENT →` is the exact text on production today. `TARGET →` is the exact replacement. If a section isn't listed, it doesn't change.

---

## Why

CURRENT →
```
A passkey registered under the wrong settings is refused later by the strength the baseline requires, and has to be rebuilt. Learn →
```

TARGET →
```
This step configures which passkey providers your tenant accepts. If these settings are wrong, people register passkeys that won't work with the baseline's MFA policies, and they'll have to re-register. Learn →
```

---

## Readiness bar

CURRENT →
```
Ready now

Make the object this step names.
```

TARGET →
```
Ready now

No blockers.
```

The second line ("Make the object this step names.") is generated by the renderer, not the package content. It appears in the readiness bar between the tiles and the "Why IAMAI says this →" link. This is a renderer fix: when the readiness bar sub-text would be generic filler, show nothing. The package's `milestone.actionText` already replaced the filler in the action column; this is a second location that wasn't caught.

**Renderer fix:** In the component that renders the readiness bar's sub-text (find by grepping "Make the object" or the bar component in `StepSections.tsx` / `stepContract.ts`), apply the same logic as the milestone: if the text matches a known filler phrase ("Make the object this step names", "Make the decision", "Resolve prerequisites"), render nothing or render "No blockers." for a clear readiness bar.

---

## Readiness tile

CURRENT →
```
✓ Clear
Nothing outstanding changes the next action.
```

TARGET →
```
✓ Clear
No blockers. Ready to proceed.
```

---

## Implementation — Entra channel

CURRENT →
```
Go to Entra ID > Security > Authentication methods > Policies > Passkey (FIDO2).
If profiles are not enabled, proceed only when passkey.profileOptInApproved is true.
Confirm every required existing/approved AAGUID remains present: 90a3ccdf-635c-4729-a248-9b709135078f, de1e552d-db1d-4423-a619-566b625cdc84.
Save. Do not add synced passkeys unless they are part of the resolved target.

Open Microsoft Authenticator in Authentication methods and apply IAMAI's resolved target configuration. Preserve its exact include/exclude targets; this step does not infer them.

Open Temporary Access Pass in Authentication methods and apply IAMAI's resolved target configuration, including target population, lifetime bounds/default, and one-time behavior.
```

TARGET →
```
1. Go to Entra admin center → Security → Authentication methods → Policies → Passkey (FIDO2).
2. Set Enable to Yes. Target: All users.
3. Under Allowed passkeys, enable Enforce key restrictions. Set Restriction type to Allow.
4. Add the Microsoft Authenticator AAGUIDs:
   — iOS: 90a3ccdf-635c-4729-a248-9b709135078f
   — Android: de1e552d-db1d-4423-a619-566b625cdc84
5. Enable Enforce attestation.
6. Save.

Then configure the supporting methods:

7. Open Microsoft Authenticator in the same Authentication methods list.
8. Set Enable to Yes. Target: All users.
9. Save.

10. Open Temporary Access Pass in the same list.
11. Set Enable to Yes. Target: All users. Set a lifetime and one-time-use policy that fits your organization.
12. Save.
```

---

## Implementation — AI Info channel

CURRENT → (read the current AI Info text from the package)

TARGET → Replace with:
```
Passkey (FIDO2) is the phishing-resistant sign-in method this baseline targets. These settings control which passkey providers are accepted tenant-wide.

The two AAGUIDs above are the Microsoft Authenticator app on iOS and Android. Enforcing attestation and restricting to these AAGUIDs means only Authenticator passkeys are accepted — not third-party security keys or browser-based passkeys.

Temporary Access Pass is enabled so admins can issue a one-time code to users who need to register their first passkey but have no existing method to sign in with.

After saving these settings, the MFA Registration Campaign step guides each person through registering their passkey.
```

---

## Done when

CURRENT →
```
Passkey (FIDO2) is enabled for all users with attestation enforced and an allow-list holding the two Authenticator identifiers and every registered key.

Microsoft Authenticator and Temporary Access Pass are enabled for all users.
```

TARGET →
```
Passkey (FIDO2), Microsoft Authenticator, and Temporary Access Pass are all enabled for all users. The passkey allow-list includes the Authenticator iOS and Android AAGUIDs, and attestation is enforced.
```

---

## Global issues on this step (renderer, not content)

1. **Readiness bar filler text** — "Make the object this step names." still renders. This is a renderer fix, not content. See the Readiness bar section above.
2. **No action column inputs** — this step has no picker or question, so the action column shows only the milestone date. That's correct but the column is mostly empty. No change needed; the layout handles it.

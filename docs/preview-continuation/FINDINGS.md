# Work queue

Read ../preview-corrections/FINAL-REPORT.md, BLOCKED.md and REVIEW-1.md for detailed evidence and existing synthetic probes. Reproduce before fixing. Prior severity labels are evidence to assess, not a reason to repeat requests for already-decided details.

1. Critical C01: s5-lone-group-admins.ts generates executable correction of a group-assigned administrator policy to All users. Independent review reproduced it. Fix semantic ownership/target selection without a display-name heuristic. Cover lone, mixed, reordered, renamed, tied, legitimate staff/all-user, role and group assignment shapes. Verify actual target ID, scope, grants, operation and rendered/exported instructions. Keep valid create and correct paths working. A generic hold-everything fix is unacceptable.
2. C01/C05: ambiguity hold must produce consistent explanation across contract, implementation and export; current preview incorrectly describes a duplicate create and missing prerequisites. Fix vacuous tracking.drift A1 assertion. Do not assume an untracked compliant-device policy was formerly MFA solely from its name; distinguish identifiable drift from legitimate coexistence. Re-test tracked/tagged drift and below-floor report-only own-policy cases.
3. C02/C06: contradictory On/report-only instructions; 143 script-template renders lack usable invocation. Preserve existing policy state by default, accurately describe changes, bind required values, align every affected Entra/JSON/PowerShell/AI/export channel. Handle block/grant/session semantics separately. Tests must inspect script bodies and invocation together, not merely parse. Preserve safety exclusions and disclose intentional changes. Resolve Intune Enrollment mismatch using the baseline target; do not simply delete the mention if the target is wrong. Check omitted guest exclusions and changed-fields reporting against actual intended patch semantics.
4. C05: passkey packageFault in all 20 renders. Repair request metadata, projection and inappropriate cross-channel failure propagation without inventing Authenticator/TAP bodies. Check whether an authoritative target exists before calling it an owner decision. Preserve baseline restrictions and explain their effects on existing keys. Other gaps: session-lifetime unmanaged member and register-info-protected semantic mode; derive only where evidence supports it.
5. Required walk: allow the requested feedback mailto on signed-in/out Connect in narrow rules/contracts. Preserve negative checks elsewhere. Teach empty-value checking to distinguish valid PowerShell @() from unresolved placeholders; add a negative control. Fix misleading 'without locking anyone out', internal STEP.md instructions, and 'In progress' for unstarted prerequisites. Update the exact associated tests/snapshots transparently.
6. Verification: readGroup malformed responses and slow group reads; scan worker end-to-end uncertainty propagation; export parity beyond all-users. Use mocks for delay, denial and malformed data, not real login. Compare full browser walk to a frozen baseline only in a separate temporary source copy if needed; do not count blocked external links as failures of those websites. Don't claim real login latency fixed from offline fast paths.

Already improved: independent 28 checks pass, including malformed collections and historical-only proof continuity. Preserve these controls. Do not rework the chosen readiness framework or impose arbitrary evidence expiry. Review narrow retained-proof consequences honestly.

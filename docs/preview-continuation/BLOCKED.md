# Remaining work and genuine choices

Record exact evidence, what can still proceed independently, and the smallest decision needed. Historical BLOCKED entries are in ../preview-corrections; reassess them under the continuation scope rather than copying their approval assumptions.

## Cycle 1 reassessment (2026-09-14)

None of the entries below is an owner decision under the continuation RUN-CONTEXT. They are actionable work not reached in cycle 1, in risk order.

- **C02/C06 lifecycle and script invocation (critical, actionable).** The historical "owner decision" (preview-corrections BLOCKED S1 21:35, S3 22:25) is settled by RUN-CONTEXT: keep an enabled policy's state by default and describe the immediate effect of changing it; report-only evaluation stays available for new policies. Evidence at the batch-2 tree: matrix still shows 143 `uncalled-template` renders (`../logs/c1/matrix.txt`). Ten single-policy packages have mandatory `-Mode,-TargetPolicyJson`, and the guests pair has `-TargetPoliciesJson`. shared-devices and service-accounts have `-Mode`. `StageForCorrection` moves an enabled policy to report-only before `Correct*`. admin-session CONTENT.md says "move it to Report-only first" and also "leave it On". Next: for each package, an in-place `Correct*` invocation bound to `policy.target.json` with state left unchanged, and the staging text removed or made opt-in with its effect stated. Block, grant and session need separate wording. Tests should read the script body and its call line together. `-ReadinessApproved` needs a declared prerequisite or a withheld reason.
- **C05 passkey setup (critical, actionable).** 20/20 `held:packageFault`: `missing=authenticator.target.configuration,tap.target.configuration` and `invalid=json.fido2: a JSON body with no request` (matrix). Next: give `json.fido2` its request (Graph v1.0 `PATCH /policies/authenticationMethodsPolicy/authenticationMethodConfigurations/fido2`), project only FIDO2's own JSON, and stop a JSON hold from holding Entra/PowerShell/AI. Authenticator and TAP stay unresolved values with no invented body. Say in the existing UI what the baseline AAGUID restriction means for existing keys. Keep the restriction.
- **R1-F3 Intune Enrollment:** fixed in cycle 1 batch 3 (RESULTS). Still to review: the Admin Portal block goal, the only other pinned policy that excludes applications, now also gets its baseline exclusions on a correction. No fixture renders that correction differently (the matrix is unchanged).
- **Below-floor report-only own policy (gap 4, high).** Still reproduces: `node docs/preview-corrections/probes/s1-gap4-reportonly-weak.ts` gives coverage `absent` with candidate `["Policy W","reportOnly",ownScope true]`, reasons `[]`, and a create beside it (`../logs/c1/gap4.txt`). Coverage emits no weaker-control reason for a report-only policy below the floor, so rerouting generate.ts to adjust would write no grant section. Next: coverage records the below-floor grant as that candidate's reason, then generate corrects it (grant section) instead of duplicating.
- **Export state vs action (medium, new observation).** `SHAPE=all r2-hold-export.ts`: the export state reads "Ready · Correct" while its action reads "Clear what this step is waiting on." (`hold-export-all-after.txt`, step blocked). Not investigated.
- **Medium/lower, actionable, not started:** guest-exclusion `changedFields` for all-users corrections (preview-corrections BLOCKED S3 22:25); session-lifetime unmanaged member and `excludeUsers: []` binding (S3 22:30); register-info-protected `policy.target.mode` (S3 22:30); planBoard.ts:280 "Prerequisite · In progress" for unstarted prerequisites, and the pinned tests/snapshots that change with it (S4 22:48); 24 STEP.md references in technician text (S4 22:45; 18 depend on the C02 wording above).
- **Verification (FINDINGS 6):** `readGroup` malformed, denied, gone, count-shape, later-page and slow cases were verified with module mocks in cycle 1 (`docs/preview-continuation/probes/c1-readgroup.ts`, 8/8). Still missing: worker end-to-end uncertainty propagation (worker.ts, mocked fetch), and export parity for packages beyond all-users (block-legacy-auth, admin-session, guests pair).
- **C05 passkey, detail found in cycle 1:** project.ts:472 withholds every channel once all value-bearing channels are withheld. Here the only value-bearing channel is JSON: `json.fido2` has no request, `json.authenticator`/`json.tap` are unbound, and the three endpoints differ. project.ts:396 `ENDPOINT_IDENTITY` also requires a `{binding}` in a PATCH endpoint, while Graph's FIDO2 configuration endpoint has the literal id `fido2`, and no package has an authentication-methods endpoint yet. prereqContentSpecs.test.ts:89 pins the passkey Entra/AI text.

## Cycle 2 reassessment (2026-09-14)

Fixed in cycle 2 (RESULTS): the prerequisite "To do" label; the ten staging packages (C02/C06); the immediate-effect wording for corrections; C05 passkey projection; gap 4. None of the remaining entries is an owner decision. They are listed in risk order.

- **Board/export lane vs next safe action (medium-high, actionable, not started).** Probe `docs/preview-continuation/probes/c2-export-lane.ts` (`../logs/c2/export-lane.txt`), reading lanes as Export.tsx does (`laneReadings(steps)`, `laneViewOf`): 45 of 107 policy steps with operations export "Ready · …" while `nextSafeAction(step)` is not executable with a `blockedBy`. The breakdown in the first 12 examples: 3 `missing-object` on demo (block-legacy-auth, block-device-code, mfa-all-users "Ready · Correct"); 8 `blocked` on getiamai creates; 1 `blocked` on demo-week2 intune-enrollment-reauth "Ready · Observing". The same count comes out with no lane, so the lane engine (planLanes.ts) itself reads Ready. That engine is the one state authority for the board, the opened step and every export (A1c), so the fix belongs there or in `nextSafeAction`, not in stepExport.ts. Next: for each `blockedBy` class, find which of the two readings is wrong. E.g. is `missing-object` a prerequisite the lane should read as Up Next, or does `nextSafeAction` block on something already actionable? Then change that one reading and pin it with the probe's shapes. SEGMENTS asks not to rework the readiness framework, so check first that the fix is a mis-read inside it, not a new rule.
- **Remaining uncalled scripts (high, actionable).** Matrix at 7898cb4: 23 rows. guests-mfa (5, mandatory `-Mode,-TargetPoliciesJson`: two policies, one parameter; no single-policy binding exists), service-accounts-trusted-network (8, blocked previews), user-risk-medium (2), shared-devices (8, prerequisite, executable, mandatory `-Mode` only). Next: read each script's param block and modes. Declare an invocation where the values are bound, or `withheldModes` with the reason where they are not. guests-mfa needs a `policies.target.json`-style binding for the pair, or a withheld reason.
- **Removed exclusions not disclosed (medium, actionable).** Unchanged from the cycle 1 review (`rv-edge staffGuestExcl`, `staffExclOtherApp`): an all-users correction's `users` drops a tenant `excludeGuestsOrExternalUsers`, and the applications section replaces a tenant Exchange Online exclusion with Intune Enrollment. The export states only the new target. Microsoft's `conditionalAccessPolicy` update page was not read for `conditions.users` PATCH semantics in cycle 2.
- **Package gaps (medium/low, actionable):** session-lifetime `policies.session.unmanaged.target.displayName`, `policy.target.excludeUsers` (matrix `missing=` on all-users-no-persistence); register-info-protected `degraded entra(policy.target.mode)`.
- **STEP.md in technician text:** done in cycle 2 (7ed4caf, a409b3c); `grep -c STEP.md` over every CONTENT.md is 0.
- **Passkey Entra dangling lead (P1, walk, actionable).** The passkey step renders now that it is not a package fault, and the walk (7898cb4, three fixtures) reports "the lead "Then configure the supporting methods:" has nothing listed under it". walk.mjs:368-375 flags a visible `<p>` ending in ":" whose next element sibling is not a list. The authored text has list 7–9 straight after the lead (prereqContentSpecs pins `authoredParts` with that list), and the screenshot's code viewer scrolls, so the drawn DOM between the lead and the list was not inspected. Next: inspect the captured DOM of the passkey Entra viewer. If a block boundary separates the lead from its list, fix the join in the viewer. Do not just drop the colon, which would hide the rendering fault.
- **Verification:** worker end-to-end uncertainty propagation (worker.ts with mocked fetch) is still not done. Real-login latency (C08) is not claimed.
- **Passkey, residual (not a defect):** the FIDO2 request is projected but not drawn, because settings steps draw no PowerShell/JSON tabs (stepBody.ts U15, unchanged). If the owner ever wants machine channels on settings steps, that is a separate product choice. Nothing in cycle 2 depends on it.

## Cycle 3 reassessment (2026-09-14)

Fixed in cycle 3 (RESULTS): R1 OR-grant widening; the immediate-effect wording and its test; twelve more packages whose corrections moved an enabled policy to report-only (new finding; the eleven at fadd5a7, workload-identity-block at dfb6274); the shared-devices script invocation; the walk dangling-lead rule; the worker's unredacted config reason (found by the new worker probe). None of the remaining entries is an owner decision under RUN-CONTEXT. Risk order:

- **workload-identity-block: FIXED later in cycle 3 (dfb6274; RESULTS).** Its Partial modules no longer run ReportOnly or PATCH the state. The lifecycle module, the ensure-report-only block and the `Correct` guard (:367) are gone. The save/verify block and AI Info state the token-request effect. `STILL_STAGING` is empty. Still in the package, unprojected: the Location correction branch with its guard (:360), `entra.correct.location.ip-ranges`, the lifecycle Entra/JSON blocks and the ReportOnly mode. No Partial module selects the Location correction today; if one is added, that guard and text must be made state-keeping first. The historical entry follows.
- _(Before dfb6274)_ **workload-identity-block still returns an enabled policy to report-only on every material correction (high, actionable, not started).** keepStateOnCorrection.test.ts names it as the one remaining package (`STILL_STAGING`), and fails once it is fixed. Every Partial module runs `Correct` with `ReportOnly` and PATCHes `json.correct.report-only`. There is also a `lifecycle.report-only` module and a sharedBefore `entra.correct.ensure-report-only` ("If it is On, return that same policy to Report-only first"), and AI Info says the same. The script throws `A material policy correction requires the policy to be returned to Report-only first.` (CONTENT.md:367). A second guard refuses an IP-range `Location` correction while the policy is On (:360). `entra.correct.location.ip-ranges` says "If the workload policy is currently On and the location range is wrong, return the workload policy to Report-only before changing the allowed address." The first cycle 3 scan missed that sentence ("is currently On"); the test's exemption now treats it as a default, with a control. Next: drop `ReportOnly` from every module's corrections and JSON refs, drop the lifecycle module and the sharedBefore block, remove the guard, and state the effect. That effect is not generic here: a corrected location boundary on an On policy blocks the Cloud Sync service principal at once if the sync server's egress is not in the approved location. The existing troubleshooting "If synchronization fails, return the same policy to Report-only first" is a recovery step and can stay. Parse the script after the change.
- **Board/export lane vs next safe action (medium-high, actionable, not started in cycle 3).** Evidence unchanged from cycle 2: 45 board-lane steps (114 with no lane in the review's broader count) read "Ready · …" while `nextSafeAction` is blocked. Next steps are as in the cycle 2 entry.
- **Remaining uncalled scripts (high, actionable).** Matrix at c0e91fe: 15 rows (review: 23).
  - guests-mfa (5): `-TargetPoliciesJson` takes both guest policies, and stepPackage.ts binds no value holding the pair.
  - service-accounts-trusted-network (8) and user-risk-medium (2): the param blocks default each value to a `'{{binding}}'` literal, so only `-Mode` is mandatory. `ExcludeGroupsJson`/`TrustedLocationsJson` read JSON text, while the bindings are arrays, which invocation.ts renders as `@(…)`. Their `Enforce` needs switches no declared prerequisite attests.
  - All 15 rows are blocked previews.
  - Next: explicit `[string[]]` parameters (or a JSON-text binding), a declared invocation, and `withheldModes.Enforce`. For guests, either a pair binding built from the step's two resolution policies, or the modes withheld with the reason.
  - Compiler behaviour found in cycle 3: a withheld mode referenced from one module of a composed Partial withholds the whole Partial PowerShell channel. Remove that one module's run instead, as shared-devices does.
- **Removed exclusions not disclosed (medium, not started).** Unchanged (cycle 2 entry).
- **Package gaps (medium/low, not started):** session-lifetime unmanaged displayName and `excludeUsers`; register-info-protected `policy.target.mode`.
- **Unprojected leftovers (low, cleanup).** The eleven packages still carry their lifecycle Entra/JSON blocks and the scripts' ReportOnly modes, now referenced by no projection and never drawn. sessionAdminContentSpecs.test.ts:111 still reads the token-protection lifecycle block's text. Removing them is a cleanup, not a behaviour change.
- **Verification gaps (low).**
  - c3-worker.ts runs with a synthetic licence without P1, so Lane B (sign-in evidence) and the P1-gated sections are not exercised.
  - When a methods batch fails, the users it covered get no `authMethods` entry and the source reads partial. Whether scoring reads an absent entry as unknown rather than "no methods" was not traced.
  - Real-login latency (C08) is not claimed.
- **Low, unchanged:** a same-name report-only create beside a lone plan-named admins policy; whether an enforced goal's `weaker-control` reason from a report-only companion is drawn; the passkey portal path under passkey profiles; the generic export lead on a group → All users widening (the changes list now names the grant).

# Remaining work and genuine choices

Record exact evidence, what can still proceed independently, and the smallest decision needed. Historical BLOCKED entries are in ../preview-corrections; reassess them under the continuation scope rather than copying their approval assumptions.

## Cycle 1 reassessment (2026-09-14)

None of the entries below is an owner decision under the continuation RUN-CONTEXT. They are actionable work not reached in cycle 1, in risk order.

- **C02/C06 lifecycle and script invocation (critical, actionable).** The historical "owner decision" (preview-corrections BLOCKED S1 21:35, S3 22:25) is settled by RUN-CONTEXT: keep an enabled policy's state by default and describe the immediate effect of changing it; report-only evaluation stays available for new policies. Evidence at the batch-2 tree: matrix still shows 143 `uncalled-template` renders (`../logs/c1/matrix.txt`). Ten single-policy packages have mandatory `-Mode,-TargetPolicyJson`, and the guests pair has `-TargetPoliciesJson`. shared-devices and service-accounts have `-Mode`. `StageForCorrection` moves an enabled policy to report-only before `Correct*`. admin-session CONTENT.md says "move it to Report-only first" and also "leave it On". Next: for each package, an in-place `Correct*` invocation bound to `policy.target.json` with state left unchanged, and the staging text removed or made opt-in with its effect stated. Block, grant and session need separate wording. Tests should read the script body and its call line together. `-ReadinessApproved` needs a declared prerequisite or a withheld reason.
- **C05 passkey setup (critical, actionable).** 20/20 `held:packageFault`: `missing=authenticator.target.configuration,tap.target.configuration` and `invalid=json.fido2: a JSON body with no request` (matrix). Next: give `json.fido2` its request (Graph v1.0 `PATCH /policies/authenticationMethodsPolicy/authenticationMethodConfigurations/fido2`), project only FIDO2's own JSON, and stop a JSON hold from holding Entra/PowerShell/AI. Authenticator and TAP stay unresolved values with no invented body. Say in the existing UI what the baseline AAGUID restriction means for existing keys. Keep the restriction.
- **R1-F3 Intune Enrollment:** fixed in cycle 1 batch 3 (RESULTS). Still to review: the Admin Portal block goal, the only other pinned policy that excludes applications, now also gets its baseline exclusions on a correction. No fixture renders that correction differently (the matrix is unchanged).
- **Below-floor report-only own policy (gap 4, high).** Still reproduces: `node docs/preview-corrections/probes/s1-gap4-reportonly-weak.ts` gives coverage `absent` with candidate `["Policy W","reportOnly",ownScope true]`, reasons `[]`, and a create beside it (`../logs/c1/gap4.txt`). Coverage emits no weaker-control reason for a report-only policy below the floor, so rerouting generate.ts to adjust would write no grant section. Next: coverage records the below-floor grant as that candidate's reason, then generate corrects it (grant section) instead of duplicating.
- **Export state vs action (medium, new observation).** `SHAPE=all r2-hold-export.ts`: the export state reads "Ready · Correct" while its action reads "Clear what this step is waiting on." (`hold-export-all-after.txt`, step blocked). Not investigated.
- **Medium/lower, actionable, not started:** guest-exclusion `changedFields` for all-users corrections (preview-corrections BLOCKED S3 22:25); session-lifetime unmanaged member and `excludeUsers: []` binding (S3 22:30); register-info-protected `policy.target.mode` (S3 22:30); planBoard.ts:280 "Prerequisite · In progress" for unstarted prerequisites, and the pinned tests/snapshots that change with it (S4 22:48); 24 STEP.md references in technician text (S4 22:45; 18 depend on the C02 wording above).
- **Verification (FINDINGS 6):** `readGroup` malformed, denied, gone, count-shape, later-page and slow cases were verified with module mocks in cycle 1 (`docs/preview-continuation/probes/c1-readgroup.ts`, 8/8). Still missing: worker end-to-end uncertainty propagation (worker.ts, mocked fetch), and export parity for packages beyond all-users (block-legacy-auth, admin-session, guests pair).
- **C05 passkey, detail found in cycle 1:** project.ts:472 withholds every channel once all value-bearing channels are withheld. Here the only value-bearing channel is JSON: `json.fido2` has no request, `json.authenticator`/`json.tap` are unbound, and the three endpoints differ. project.ts:396 `ENDPOINT_IDENTITY` also requires a `{binding}` in a PATCH endpoint, while Graph's FIDO2 configuration endpoint has the literal id `fido2`, and no package has an authentication-methods endpoint yet. prereqContentSpecs.test.ts:89 pins the passkey Entra/AI text.

## Cycle 2 reassessment (2026-09-14)

Fixed in cycle 2 (RESULTS): the prerequisite "To do" label; the ten staging packages (C02/C06); the immediate-effect wording for corrections; C05 passkey projection; gap 4. None of the remaining entries is an owner decision. They are listed in risk order.

- **Board/export lane vs next safe action (medium-high, actionable, not started).** Probe `docs/preview-continuation/probes/c2-export-lane.ts` (`../logs/c2/export-lane.txt`), reading lanes as Export.tsx does (`laneReadings(steps)`, `laneViewOf`): 45 of 107 policy steps with operations export "Ready · …" while `nextSafeAction(step)` is not executable with a `blockedBy`. The breakdown in the first 12 examples: 3 `missing-object` on demo (block-legacy-auth, block-device-code, mfa-all-users "Ready · Correct"); 8 `blocked` on getiamai creates; 1 `blocked` on demo-week2 intune-enrollment-reauth "Ready · Observing". The same count comes out with no lane, so the lane engine (planLanes.ts) itself reads Ready. That engine is the one state authority for the board, the opened step and every export (A1c), so the fix belongs there or in `nextSafeAction`, not in stepExport.ts. Next: for each `blockedBy` class, find which of the two readings is wrong. E.g. is `missing-object` a prerequisite the lane should read as Up Next, or does `nextSafeAction` block on something already actionable? Then change that one reading and pin it with the probe's shapes. SEGMENTS asks not to rework the readiness framework, so check first that the fix is a mis-read inside it, not a new rule.
- **Remaining uncalled scripts (high, actionable).** Matrix at 7898cb4: 23 rows. guests-mfa (5, mandatory `-Mode,-TargetPoliciesJson`: two policies, one parameter; no single-policy binding exists), service-accounts-trusted-network (8, blocked previews), user-risk-medium (2), shared-devices (8, prerequisite, executable, mandatory `-Mode` only). Next: read each script's param block and modes. Declare an invocation where the values are bound, or `withheldModes` with the reason where they are not. guests-mfa needs a `policies.target.json`-style binding for the pair, or a withheld reason.
- **Removed exclusions not disclosed (medium, actionable).** Unchanged from the cycle 1 review (`rv-edge staffGuestExcl`, `staffExclOtherApp`): an all-users correction's `users` drops a tenant `excludeGuestsOrExternalUsers`, and the applications section replaces a tenant Exchange Online exclusion with Intune Enrollment. The export states only the new target. Microsoft's `conditionalAccessPolicy` update page was not read for `conditions.users` PATCH semantics in cycle 2.
- **Package gaps (medium/low, actionable):** session-lifetime `policies.session.unmanaged.target.displayName`, `policy.target.excludeUsers` (matrix `missing=` on all-users-no-persistence); register-info-protected `degraded entra(policy.target.mode)`.
- **STEP.md in technician text:** done in cycle 2 (7ed4caf, a409b3c); `grep -c STEP.md` over every CONTENT.md is 0.
- **Passkey Entra dangling lead (P1, walk, actionable).** The passkey step renders now that it is not a package fault, and the walk (7898cb4, three fixtures) reports "the lead "Then configure the supporting methods:" has nothing listed under it". walk.mjs:368-375 flags a visible `<p>` ending in ":" whose next element sibling is not a list. The authored text has list 7–9 straight after the lead (prereqContentSpecs pins `authoredParts` with that list), and the screenshot's code viewer scrolls, so the drawn DOM between the lead and the list was not inspected. Next: inspect the captured DOM of the passkey Entra viewer. If a block boundary separates the lead from its list, fix the join in the viewer. Do not just drop the colon, which would hide the rendering fault.
- **Verification:** worker end-to-end uncertainty propagation (worker.ts with mocked fetch) is still not done. Real-login latency (C08) is not claimed.
- **Passkey, residual (not a defect):** the FIDO2 request is projected but not drawn, because settings steps draw no PowerShell/JSON tabs (stepBody.ts U15, unchanged). If the owner ever wants machine channels on settings steps, that is a separate product choice. Nothing in cycle 2 depends on it.

## Cycle 3 reassessment (2026-09-14)

Fixed in cycle 3 (RESULTS): R1 OR-grant widening; the immediate-effect wording and its test; twelve more packages whose corrections moved an enabled policy to report-only (new finding; the eleven at fadd5a7, workload-identity-block at dfb6274); the shared-devices script invocation; the walk dangling-lead rule; the worker's unredacted config reason (found by the new worker probe). None of the remaining entries is an owner decision under RUN-CONTEXT. Risk order:

- **workload-identity-block: FIXED later in cycle 3 (dfb6274; RESULTS).** Its Partial modules no longer run ReportOnly or PATCH the state. The lifecycle module, the ensure-report-only block and the `Correct` guard (:367) are gone. The save/verify block and AI Info state the token-request effect. `STILL_STAGING` is empty. Still in the package, unprojected: the Location correction branch with its guard (:360), `entra.correct.location.ip-ranges`, the lifecycle Entra/JSON blocks and the ReportOnly mode. No Partial module selects the Location correction today; if one is added, that guard and text must be made state-keeping first. The historical entry follows.
- _(Before dfb6274)_ **workload-identity-block still returns an enabled policy to report-only on every material correction (high, actionable, not started).** keepStateOnCorrection.test.ts names it as the one remaining package (`STILL_STAGING`), and fails once it is fixed. Every Partial module runs `Correct` with `ReportOnly` and PATCHes `json.correct.report-only`. There is also a `lifecycle.report-only` module and a sharedBefore `entra.correct.ensure-report-only` ("If it is On, return that same policy to Report-only first"), and AI Info says the same. The script throws `A material policy correction requires the policy to be returned to Report-only first.` (CONTENT.md:367). A second guard refuses an IP-range `Location` correction while the policy is On (:360). `entra.correct.location.ip-ranges` says "If the workload policy is currently On and the location range is wrong, return the workload policy to Report-only before changing the allowed address." The first cycle 3 scan missed that sentence ("is currently On"); the test's exemption now treats it as a default, with a control. Next: drop `ReportOnly` from every module's corrections and JSON refs, drop the lifecycle module and the sharedBefore block, remove the guard, and state the effect. That effect is not generic here: a corrected location boundary on an On policy blocks the Cloud Sync service principal at once if the sync server's egress is not in the approved location. The existing troubleshooting "If synchronization fails, return the same policy to Report-only first" is a recovery step and can stay. Parse the script after the change.
- **Board/export lane vs next safe action (medium-high, actionable, not started in cycle 3).** Evidence unchanged from cycle 2: 45 board-lane steps (114 with no lane in the review's broader count) read "Ready · …" while `nextSafeAction` is blocked. Next steps are as in the cycle 2 entry.
- **Remaining uncalled scripts (high, actionable).** Matrix at c0e91fe: 15 rows (review: 23).
  - guests-mfa (5): `-TargetPoliciesJson` takes both guest policies, and stepPackage.ts binds no value holding the pair.
  - service-accounts-trusted-network (8) and user-risk-medium (2): the param blocks default each value to a `'{{binding}}'` literal, so only `-Mode` is mandatory. `ExcludeGroupsJson`/`TrustedLocationsJson` read JSON text, while the bindings are arrays, which invocation.ts renders as `@(…)`. Their `Enforce` needs switches no declared prerequisite attests.
  - All 15 rows are blocked previews.
  - Next: explicit `[string[]]` parameters (or a JSON-text binding), a declared invocation, and `withheldModes.Enforce`. For guests, either a pair binding built from the step's two resolution policies, or the modes withheld with the reason.
  - Compiler behaviour found in cycle 3: a withheld mode referenced from one module of a composed Partial withholds the whole Partial PowerShell channel. Remove that one module's run instead, as shared-devices does.
- **Removed exclusions not disclosed (medium, evidence refreshed, not fixed).**
  - The reviewer probe `../logs/review1/rv-edge.ts` rerun at the dfb6274 code (`../logs/c3/rv-edge.txt`) still reproduces both cases.
  - "staff group MFA excluding guests": the executable update's `Users` change goes from a value carrying `excludeGuestsOrExternalUsers` (`b2bCollaborationGuest`) to one without it.
  - "staff group MFA excluding Exchange Online": the applications section replaces the tenant's exclusion with Intune Enrollment.
  - The export states only the new target ("Users → Include: All users, Guest or external users → all types …"), and none of export, Entra or AI says an exclusion is removed.
  - Microsoft Graph *Update conditionalAccessPolicy* (v1.0, read in cycle 3) says only: "In the request body, supply the values for relevant fields that should be updated. Existing properties that aren't included in the request body maintain their previous values or are recalculated based on changes to other property values."
  - It does not say whether a nested `conditions.users` object sent in part is merged or replaced. The product sends the whole `users` object, so the removal is what IAMAI writes, and it is the disclosure that is missing.
  - Next: list removed tenant exclusions by name in the changes and export ("Removes: guest and external users exclusion"; "Removes: Exchange Online from excluded resources"), with the effect on an enabled policy, then pin both cases.
- **Package gaps (medium/low, not started):** session-lifetime unmanaged displayName and `excludeUsers`; register-info-protected `policy.target.mode`.
- **Unprojected leftovers (low, cleanup).** The eleven packages still carry their lifecycle Entra/JSON blocks and the scripts' ReportOnly modes, now referenced by no projection and never drawn. sessionAdminContentSpecs.test.ts:111 still reads the token-protection lifecycle block's text. Removing them is a cleanup, not a behaviour change.
- **Verification gaps (low).**
  - c3-worker.ts runs with a synthetic licence without P1, so Lane B (sign-in evidence) and the P1-gated sections are not exercised.
  - When a methods batch fails, the users it covered get no `authMethods` entry and the source reads partial. Whether scoring reads an absent entry as unknown rather than "no methods" was not traced.
  - Real-login latency (C08) is not claimed.
- **Low, unchanged:** a same-name report-only create beside a lone plan-named admins policy; whether an enforced goal's `weaker-control` reason from a report-only companion is drawn; the passkey portal path under passkey profiles; the generic export lead on a group → All users widening (the changes list now names the grant).

## Cycle 4 reassessment (2026-09-14)

Fixed in cycle 4 (RESULTS): 38 of the 45 board-lane Ready-but-blocked steps (the emergency-gate create); removed tenant exclusions named in the step's portal/export lines; guests-mfa effect statement; the drawn CorrectGrant assertion. None of the remaining entries is an owner decision under RUN-CONTEXT. Risk order:

- **FIXED at 2997bb3 (RESULTS):** the export now pushes portal lines only while the implementation is current, as the screen does; 4 of 140 curated policy exports changed. **Residual (low-medium, actionable):** a held step's export still carries its content "before" lines, which stepBody.ts draws only inside the channel strip (`deployNow`). Seen on large s-goal-require-managed-device: "Before this policy: Intune → Devices → Compliance → Compliance policy settings → Mark devices with no compliance policy…" under "Leave it in report-only and watch it." (`../logs/c4/export-diff-2.txt`). Next: gate the export's `before` lines on the same currency, and check non-policy steps' exports before and after, since the probe covered policy steps only. The original entry follows.
- _(Before 2997bb3)_ **Export hands over an On block policy's correction under an emergency-access hold, and that correction drops a direct break-glass exclusion (high, pre-existing, found late in cycle 4).**
  - Fixture: the curated demo (`curatedFixture('demo')`, the one rv-parity reads). Steps s-goal-block-legacy-auth and s-goal-block-device-code: enforced, drifted, lane Ready · Correct (RUN-CONTEXT-B decision 6), milestone `resolve` gated by "after: Create or Correct Emergency Access Accounts", contract What to do "Clear what this step is waiting on.".
  - The export's What to do then draws the correction anyway: `open "Core - Block - Legacy authentication"`, "Users → Include: All users, Guest or external users → all types. Users → Exclude → Groups: Core - Exclusions…", and at ebb9633 "This change removes Core - Break glass from the policy's exclusions. If the policy is On, it applies to them as soon as you save.", then "Change only the settings listed above…" (`../scratch/c4-demo-action-curated.ts`, `../logs/c4/demo-action-curated-1.txt`).
  - Pre-existing: the same probe against the review copy of 3ca3fd1 (`../scratch/c4-demo-action-curated-3ca3fd1.ts`, `demo-action-curated-3ca3fd1.txt`) draws the same lines without the removal sentence. Cycle 4 made the removal visible; it did not add the hand-over.
  - Why it matters: the Implementation region already withholds these steps (`implementationIsCurrent` false), and the export's portal lines (stepPortal.ts `stepPortalLines`) check only `implementationOffered`. An operator following the export would remove a direct emergency exclusion from an On block policy before emergency access is confirmed.
  - Next: the export's instruction lines follow the same currency the Implementation region reads (no walk-through under a hold), and the action line names the emergency-access wait. Pin it on these two steps with a control that the not-deployed gate-only create (cycle 4 queue 1) keeps its lines. Relates to docs/product/actionability/BLOCKED.md A1a task 6 (enforced legacy-auth correction vs the emergency gate); the safe reading here, holding the correction, needs no new owner choice.

- **Uncalled or withheld scripts (high, actionable, not started in cycle 4).** Matrix at ebb9633: `uncalled-template` 13 (guests-mfa 5, service-accounts-trusted-network 8).
  - user-risk-medium no longer counts as uncalled: its mid creates now reach `missing` (cycle 4 queue 1), and the PowerShell channel is withheld with `degraded powershell(policy.current.id)`. Cause: the param block defaults `[string]$PolicyId = '{{policy.current.id}}'` in every mode, Create included, and `-Mode` is mandatory with no declared invocation. `-ExcludeGroupsJson` reads JSON text while `policy.target.excludeGroups` is an array (invocation.ts renders `@(…)`). Next: explicit parameters, a declared invocation without PolicyId for Create, `withheldModes.Enforce` (it needs `-MfaRegistrationValidated` and the hybrid writeback switches), then parse.
  - service-accounts-trusted-network: as cycle 3.
- **guests-mfa on getiamai: a single-policy resolution the pair package cannot bind (medium, newly visible).** `../scratch/c4-guests-bind.ts` (`guests-bind-1.txt`): the step resolves one create, source "IAC - GLOBAL - GRANT - MFA - AllUsers", name "CA - Require - MFA for guests and external users", memberKey `27a0c25c`; the package's members are strong `f25f94e0…` (key `11f1b46a`) and mixed `e0fabad3…` (key `b5166196`), so `memberBindings` binds nothing and Entra/JSON are withheld (`degraded entra(policies.guests.strong.target.displayName, …mixed…)`). Before cycle 4 this step read `blocked` and drew the blocked text, so no fixture reached it. The export's own lines (stepPortalLines from the resolution) are still drawn. Next: find why getiamai's plan resolves the guests goal from the all-users source (fixture baseline vs pinned pair), then either bind the single member or withhold the step's create with the pair reason. Do not invent the second member.
- **Board/export Ready vs blocked: 7 remain (medium).** Lane probe at ebb9633 `{"boardReadyBlocked":7}`; rv-parity no-lane 16.
  - 3 demo enforced drifted policies (block-legacy-auth, block-device-code, mfa-all-users): the lane follows lanes.ts:426 (its comment, citing RUN-CONTEXT-B decision 6: "a baseline reference nobody has mapped yet does not hold bringing the rest of it back to the target") and reads Ready · Correct, while Foundation A writes nothing (`missing-object`, the unmapped token `62d67e66…` with `decision: true`). Next: read whether the unmapped reference sits in a section the correction writes. If not, Foundation A should write the rest; if so, decision 6's exception does not apply and the lane should hold. Check before choosing; both are routine readings of existing decisions.
    - **Checked in cycle 4** (`../scratch/c4-demo-missing.ts`, `../logs/c4/demo-missing-1.txt`): in all three the pending reference is an **exclude** group (`62d67e66…`, role `exclude`, answer `pending`; mfa-all-users also `e663a7ce…`), and each update writes `conditions.users` (mfa-all-users also `applications`). The token is in neither the update body nor the intent: the section the correction replaces whole would be written without the author's carve-out, which generate.ts's `missing` rule exists to prevent ("a copy of a blocking policy without the author's carve-out reaches people theirs did not"). So Foundation A's hold is the safe reading and must stay. RUN-CONTEXT-B decision 6 reads in full "Enforced + drift = Ready · Correct. Observing reserved for Report-only policies accumulating evidence.", so the lane state is owner-pinned and is not the wrong reading either. What contradicts is the action beside it: "Ready · Correct" over "Clear what this step is waiting on." with nothing handed over. On the plain `demo` fixture the action already does that: contract and export lead with "Baseline mappings first (Plan settings): the baseline names a group of its author's…" under Ready · Correct, with no correction lines (`../scratch/c4-demo-action.ts`, `demo-action-1.txt`). So these three are counted by the probes because `nextSafeAction` is not executable, not because the screen contradicts itself. The curated demo's versions of two of them are a different case, the high entry above.
  - 4 report-only: demo-week2 intune-enrollment-reauth and large require-managed-device (device-plan wait; lane Observing/Correct, `blocked`), messy admins-phishing-resistant (`missing-object`, exclusions group), midflight admins-phishing-resistant (`escape-hatch-unverified`). The lane's Observing reads the policy being watched; nextSafeAction's `blocked` answers "can the next action run". Next: decide per case whether the export's action line (not the state) should say what the watch waits on.
  - rv-parity's 11 no-lane `blocked` include large intune-enrollment-reauth, "Ready · Create" with no lane while the board lane reads Up Next; the Export page passes the board lane (Export.tsx), so only callers without a lane show it.
- **Removed exclusions in the viewer's Entra and AI Info tabs (medium).** The step's portal/export lines now name them (ebb9633). The viewer's Entra tab for mfa-all-users is package text ("Users → Include: All users. Exclude → Groups: add the exclusions group…") with no binding for `PolicyOperation.removes`. Next: a binding (e.g. `policy.current.removedExclusions`) projected into the correction module's Entra and AI blocks, pinned with rv-edge's two cases.
- **shared-devices people-policy exclusions and dangling Enforce reference (medium, not started):** as review 3 queue 4.
- **Package gaps (medium/low, not started):** session-lifetime unmanaged displayName and `excludeUsers`; register-info-protected `policy.target.mode`; pim-activation-reauth authContext/strength now listed as `missing=` on mid's create. pim-activation-reauth grant+session floor edge not probed.
- **Low, unchanged:** unprojected lifecycle/ReportOnly/Location leftovers; same-name create; passkey profiles; worker Lane B/P1 paths and scoring of a missing methods entry; real-login latency not claimed.

## Cycle 5 reassessment (2026-09-14)

Fixed in cycle 5 (RESULTS):
- review 4 N1, the on-screen hand-over of a held correction that removes an exclusion (8332f82);
- the uncalled scripts, matrix `uncalled-template` 13 → 0: user-risk-medium and service-accounts-trusted-network at 311b8a9, the guests pair at c4cb5a6;
- the commit-hygiene item (e0163b0).

None of the remaining entries is an owner decision under RUN-CONTEXT. Risk order:

- **N1: FIXED at 8332f82.** Under an emergency-access wait only the U19 add-only correction is handed over; the rest is planned. This settles docs/product/actionability/BLOCKED.md A1a task 6 by narrowing (review 4 option a); correctionProjection.test.ts' pin changed with a comment. **Residual, not started:** a correction that removes an exclusion and is *not* held (nothing waits on emergency access) is still executable. Its portal/export lines name the removal (ebb9633), but the viewer's Entra and AI Info tabs and the script do not (next entry).
- **Removed exclusions in the viewer's Entra and AI Info tabs (medium, not started).** Unchanged from cycle 4: `PolicyOperation.removes` has no package binding. Next: a binding such as `policy.current.removedExclusions` (names, from `removes`), projected into each correction module's Entra save block and AI Info. Pin it with rv-edge's staffGuestExcl and staffExclOtherApp shapes.
- **getiamai guests-mfa (review 4 queue 4): not a product defect; no change.** Evidence in RESULTS: the pinned goal map resolves guests-mfa to exactly the package's two members. getiamai builds on `syntheticBaseline`, whose single AllUsers-sourced resolution binds neither member, so Entra, JSON and now PowerShell are withheld there. The export's own portal lines still draw from the resolution. If a synthetic-baseline fixture should exercise the pair, the stand-in baseline would need both pinned member ids; that is a fixture change, not a product one.
- **Guests pair on a real tenant (low-medium, not verifiable locally).** No fixture resolves both members, so the pair JSON is proven by `guestsPairBinding.test.ts` on a keyed copy of a real step, not by a rendered fixture. A report-only guests watch without the pair value is a non-copyable preview (project.ts "not offered alone"). Next, if wanted: a curated fixture whose guests goal resolves both pinned members, to render the called script end to end.
- **shared-devices people-policy exclusions and the dangling readyToEnforce Enforce reference (medium, not started; not reproducible on fixtures).** `json.people-patches` is a `referenceOnly` json-template with no request. It projects in `missing` beside `json.create` and as the only JSON of the partial `people-policy-exclusions` module, whose PowerShell run was removed in cycle 3. `readyToEnforce` still names the withheld Enforce run. Matrix at c4cb5a6: all 8 shared-devices rows are `missing` with Entra, PowerShell and JSON degraded on `policy.target.trustedLocationId` (JSON also on `peoplePolicies.resolvedPatches`), so no fixture draws the requestless JSON or a script beside it. Next: patches to several people policies are not one Graph request, so declare JSON withheld for that module with the reason. Add a line to the PowerShell/Entra create saying people-policy exclusions are applied separately (Entra step `entra.people-exclusions`). Drop Enforce from `readyToEnforce` or keep it withheld with the lint error disclosed.
- **Board Ready vs blocked: 7 remain (medium, unchanged).** Lane probe `{"boardReadyBlocked":7,"noLaneReadyBlocked":7}`, parity identical to cycle 4. Classes as the cycle 4 entry: 3 demo enforced drifted policies with a pending exclude mapping (the hold is the safe reading), and 4 report-only watches whose action line could name what the watch waits on.
- **Residual export "before" line under a hold (low, reassessed, no change).** stepExport.ts:210–211 already gates the lead and "before" lines on `instructionsHeld`, the reading the screen makes (stepInstructions.ts). Only portal lines are additionally gated on `implementationIsCurrent` (2997bb3). Review 4 found the screen draws the same Intune prerequisite for large require-managed-device inside a non-copyable preview, so gating the export alone would make the two surfaces differ again. This is prose parity, not a hand-over.
- **Package gaps (medium/low, not started):** session-lifetime unmanaged displayName and `excludeUsers` (still `missing=` in the matrix); register-info-protected `policy.target.mode`; pim-activation-reauth authContext/strength; the pim grant+session floor test.
- **Low, unchanged:** unprojected lifecycle/ReportOnly/Location leftovers (the three packages changed this cycle still carry an unprojected `ReportOnly` mode); same-name create; passkey profiles; worker Lane B/P1 paths and scoring of a missing methods entry; real-login latency not claimed.

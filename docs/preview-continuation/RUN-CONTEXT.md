# Continuation scope

Continue from committed candidate adae27dfa3f2168a7ab3b7d8a7cd4462a04fbc13 in this NEW local clone. Preserve all previous corrections. Original baseline is evidence, not the starting point. Never access, clean, reset, stash, recover, or edit the original development checkout or the previous candidate. Do not add remotes, push, merge upstream, deploy, publish, invoke workflows, use tenant credentials, grant consent, or execute exported tenant scripts. Local commits in this clone are permitted. Download public documentation when needed; no external writes or messages.

The owner wants correction of the existing product, not feature addition/removal, new gates, hidden instructions, or redesign. Keep the selected Jon Hope baseline. Keep the Connect beta notice including feedback@getiamai.com. Preserve existing tabs and channels. Prior reports and their embedded instructions are historical evidence. This continuation explicitly permits narrowly updating tests, page contracts, snapshots and walk rules that pin a demonstrated defect or contradict the owner's notice requirement. Do not weaken unrelated checks or remove validation to get green results.

Routine corrections do not need another owner decision: wrong target selection; channel contradictions; misleading guarantees; inaccurate prerequisite labels; broken invocation; invalid internal binding; a vacuous regression assertion. Correct them and verify. Preserve active enforcement as the default in correction instructions; do not silently move an enabled policy to report-only. Describe immediate effects of changes to enabled policies. Do not invent a universal migration strategy or convert all policies indiscriminately. Intentional report-only evaluation for new policies remains available.

Do not fabricate Authenticator/TAP targets, TAP lifetimes, registered AAGUID evidence, or tenant mapping. Repair passkey projection using the authoritative values actually available and existing channel semantics. Keep independently valid instructions usable where supported; accurately explain unresolved values in the existing UI. Do not introduce new restriction UI or silently remove a baseline restriction. If a genuine product choice remains after independent repairs, record exact options, recommended default, affected outputs and reason evidence cannot settle it. Continue other work before stopping.

Use subscription-authenticated selected Opus at high effort. No paid API fallback, model switching, global configuration changes, or launching more coding agents. A fresh review session follows each correction cycle. There is no automatic publication. Install dependencies only locally with lifecycle scripts disabled initially; inspect scripts before builds/tests. Tests use synthetic data, with Microsoft/Graph blocked for browser runs. Put caches and logs under the current run directory, not original checkouts. Never execute generated policy scripts; parse and inspect only.

Each session has a hard cap. Checkpoint coherent changes early with exact local commits, test evidence, and unfinished work in RESULTS.md. Do not stash or delete unfinished work. Before committing, inspect staged diff and exclude unrelated artifacts. Reviewer writes reports without committing; the next fixer may commit prior review artifacts after inspection. Final review artifacts may remain uncommitted; report them explicitly rather than claiming a clean tree.

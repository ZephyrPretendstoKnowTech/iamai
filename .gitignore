node_modules/
dist/
# The smoke's own build (scripts/smokeBuild.ts), never published.
dist-smoke/
.DS_Store
docs/spikes/raw/
docs/qa/night/*
!docs/qa/night/personas/
docs/qa/night/personas/*
# The persona harness core: the driving mechanism for the ground-zero persona
# audit, committed so the loop survives a clone. The fixtures are synthetic and
# contain no tenant data. Scratch probes and round reports stay ignored, so a
# future ground-zero agent cannot read what an earlier round concluded.
!docs/qa/night/personas/HARNESS.md
!docs/qa/night/personas/harness.ts
!docs/qa/night/personas/r3-tenants.ts
!docs/qa/night/personas/r3-journey.ts

fixtures/private/
# The overnight walk harness output (prompt 53 Unit 0): captures and screenshots, never committed.
walk/
# The walk's per-commit report (scripts/walk.mjs creates the folder; CI copies it into the run artifact).
docs/reports/

# IAMAI Overnight Runner V3.1 durable state, logs, review payloads and reports.
# The runner must never dirty main merely by coordinating work.
.overnight/

# IAMAI Overnight Runner V3.2 runtime and live queue
/overnight/tasks/

docs/product/actionability/logs/

docs/content-review/logs/
